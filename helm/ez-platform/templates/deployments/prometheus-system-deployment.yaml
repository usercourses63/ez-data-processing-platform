# Prometheus System - Kubernetes Deployment
# Infrastructure and system metrics collection

apiVersion: v1
kind: Service
metadata:
  name: prometheus-system
  namespace: ez-platform
  labels:
    app: prometheus-system
    metrics-type: system
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    name: http
  selector:
    app: prometheus-system

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-system
  namespace: ez-platform
  labels:
    app: prometheus-system
    metrics-type: system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-system
  template:
    metadata:
      labels:
        app: prometheus-system
        component: monitoring
        metrics-type: system
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: prometheus-system
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
              - ALL
        image: prom/prometheus:latest
        ports:
        - containerPort: 9090
          name: http
        args:
        - '--config.file=/etc/prometheus/prometheus.yml'
        - '--storage.tsdb.path=/prometheus'
        - '--storage.tsdb.retention.time=15d'
        - '--web.enable-remote-write-receiver'
        resources:
          requests:
            cpu: "500m"
            memory: "2Gi"
          limits:
            cpu: "2000m"
            memory: "4Gi"
        volumeMounts:
        - name: prometheus-data
          mountPath: /prometheus
        - name: prometheus-config
          mountPath: /etc/prometheus
        - name: prometheus-alerts
          mountPath: /etc/prometheus/alerts
      volumes:
      - name: prometheus-data
        persistentVolumeClaim:
          claimName: prometheus-system-data-pvc
      - name: prometheus-config
        configMap:
          name: prometheus-system-config
      - name: prometheus-alerts
        configMap:
          name: prometheus-alerts

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-system-data-pvc
  namespace: ez-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
