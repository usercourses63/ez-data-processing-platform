---
phase: 01-file-simulator-integration
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/testing/file-simulator-setup.md
  - src/Frontend/playwright.config.ts
autonomous: false

must_haves:
  truths:
    - "All 6 E2E test scenarios pass with file-simulator configuration active"
    - "Test setup documentation exists explaining file-simulator connection"
    - "E2E test configuration includes file-simulator prerequisites"
  artifacts:
    - path: "docs/testing/file-simulator-setup.md"
      provides: "Setup documentation for file-simulator integration"
      contains: "verify-file-simulator.ps1"
    - path: "src/Frontend/playwright.config.ts"
      provides: "E2E test configuration"
      contains: "baseURL"
  key_links:
    - from: "docs/testing/file-simulator-setup.md"
      to: "scripts/verify-file-simulator.ps1"
      via: "documentation reference"
      pattern: "verify-file-simulator"
---

<objective>
Validate E2E tests pass against file-simulator-configured environment and create setup documentation.

Purpose: Prove file-simulator integration works by running all existing E2E tests and documenting the setup process for future use.
Output: All 6 E2E tests passing, setup documentation for team reference.
</objective>

<execution_context>
@C:\Users\UserC\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-file-simulator-integration/01-RESEARCH.md
@.planning/phases/01-file-simulator-integration/01-01-SUMMARY.md
@src/Frontend/playwright.config.ts
@src/Frontend/tests/e2e/datasource.spec.ts
@src/Frontend/tests/e2e/invalid-records.spec.ts
@src/Frontend/tests/e2e/metrics.spec.ts
@src/Frontend/tests/e2e/alerts.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file-simulator setup documentation</name>
  <files>docs/testing/file-simulator-setup.md</files>
  <action>
Create comprehensive setup documentation for file-simulator integration.

Include these sections:

1. **Overview** - What file-simulator is and why EZ Platform uses it
2. **Prerequisites** - Required software, cluster setup
3. **Quick Start** - Get file-simulator running in 5 minutes
4. **ConfigMap Configuration** - How endpoints are configured
5. **Verification** - How to verify connectivity works
6. **Troubleshooting** - Common issues and solutions
7. **Protocol Reference** - All endpoints, credentials, ports

Key content to include:

```markdown
# File-Simulator Integration Guide

## Overview

File-simulator provides realistic protocol servers (FTP, SFTP, S3, SMB, NFS, HTTP) for testing
EZ Platform's file I/O operations. It runs in a separate Minikube profile and communicates
with EZ Platform via NodePort services.

## Prerequisites

- Minikube with Hyper-V driver (for file-simulator)
- Minikube with Docker driver (for EZ Platform)
- Helm 3.x
- PowerShell 7.x (for verification scripts)
- file-simulator-suite repo: C:\Users\UserC\source\repos\file-simulator-suite

## Quick Start

### 1. Start file-simulator cluster

minikube start --profile file-simulator --driver=hyperv --memory=8192 --cpus=4 \
  --mount --mount-string="C:\simulator-data:/mnt/simulator-data"

### 2. Deploy file-simulator

cd C:\Users\UserC\source\repos\file-simulator-suite
helm upgrade --install file-sim .\helm-chart\file-simulator \
  --kube-context=file-simulator \
  --namespace file-simulator \
  --create-namespace

### 3. Apply NFS fix (required)

kubectl --context=file-simulator patch deployment file-sim-file-simulator-nas \
  -n file-simulator --patch-file nfs-fix-patch.yaml

### 4. Verify connectivity

cd C:\Users\UserC\source\repos\EZ
.\scripts\verify-file-simulator.ps1 -Apply

## ConfigMap Configuration

The file-simulator-config ConfigMap contains all protocol endpoints.
Located at: k8s/configmaps/file-simulator-config.yaml

Key environment variables:
- FileAccess__Ftp__Host - FTP server hostname
- FileAccess__Sftp__Host - SFTP server hostname
- FileAccess__S3__Endpoint - S3-compatible endpoint URL
- FileAccess__Http__BaseUrl - HTTP server URL
- FileSimulator__Enabled - Feature flag for file-simulator mode

## Verification

Run the verification script to test all endpoints:

.\scripts\verify-file-simulator.ps1 -TestOnly  # Test without applying
.\scripts\verify-file-simulator.ps1 -Apply     # Test and apply ConfigMap

Expected output:
  FTP        : OK - FTP Server
  SFTP       : OK - SFTP Server
  S3/MinIO   : OK - S3-compatible API
  HTTP       : OK - HTTP Server
  WebDAV     : OK - WebDAV Server
  S3 Health  : OK - MinIO responding
  Results: 6/6 tests passed

## Protocol Reference

| Protocol | NodePort | Username | Password |
|----------|----------|----------|----------|
| FTP | 30021 | ftpuser | ftppass123 |
| SFTP | 30022 | sftpuser | sftppass123 |
| S3/MinIO | 30900 | minioadmin | minioadmin123 |
| HTTP | 30088 | httpuser | httppass123 |
| WebDAV | 30089 | httpuser | httppass123 |

### NFS Servers (Multi-NAS topology)

| Server | NodePort | Purpose |
|--------|----------|---------|
| NAS Input 1 | 32150 | Input server 1 |
| NAS Input 2 | 32151 | Input server 2 |
| NAS Input 3 | 32152 | Input server 3 |
| NAS Output 1 | 32154 | Output server 1 |
| NAS Output 2 | 32155 | Output server 2 |
| NAS Output 3 | 32156 | Output server 3 |
| NAS Backup | 32153 | Backup server |

## Troubleshooting

### Issue: Connection refused

**Cause:** file-simulator cluster not running or pods not ready

**Solution:**
1. Check cluster: minikube status -p file-simulator
2. Check pods: kubectl --context=file-simulator get pods -n file-simulator
3. Wait for pods: kubectl --context=file-simulator wait --for=condition=ready pod -l app=file-simulator -n file-simulator --timeout=300s

### Issue: IP address changed

**Cause:** Minikube assigns dynamic IPs; file-simulator IP changed after restart

**Solution:**
1. Re-run verification: .\scripts\verify-file-simulator.ps1 -Apply
2. Restart services: kubectl --context=minikube rollout restart deployment -n ez-platform

### Issue: SMB not working

**Cause:** SMB requires standard port 445, not NodePort

**Solution:**
1. Run tunnel: minikube tunnel -p file-simulator (in separate terminal)
2. Use LoadBalancer IP for SMB connections
3. See RESEARCH.md for SMB-specific configuration

### Issue: NFS files not visible

**Cause:** Init container only syncs at pod start

**Solution:**
1. Place test files in C:\simulator-data\nas-input-1\
2. Restart NAS pod: kubectl --context=file-simulator delete pod -n file-simulator -l nas-server=nas-input-1
3. Wait 30 seconds for sync
```
  </action>
  <verify>
```powershell
Test-Path "docs/testing/file-simulator-setup.md"
Select-String -Path "docs/testing/file-simulator-setup.md" -Pattern "verify-file-simulator.ps1"
Select-String -Path "docs/testing/file-simulator-setup.md" -Pattern "NodePort"
```
  </verify>
  <done>Setup documentation exists with Overview, Prerequisites, Quick Start, ConfigMap Configuration, Verification, Protocol Reference, and Troubleshooting sections</done>
</task>

<task type="auto">
  <name>Task 2: Run all E2E tests to validate file-simulator integration</name>
  <files>src/Frontend/playwright.config.ts</files>
  <action>
Run all 6 E2E test scenarios to confirm they pass with the current environment configuration.

**Important context about E2E tests:**
The existing E2E tests (datasource.spec.ts, invalid-records.spec.ts, metrics.spec.ts, alerts.spec.ts)
are primarily **UI tests via Playwright** that test the frontend React application. They do NOT
directly call file-simulator protocol servers.

The file-simulator ConfigMap affects:
1. Backend services (FileDiscovery, FileProcessor, etc.) when they poll for files
2. Connection test functionality in the UI (when user clicks "Test Connection" for FTP/SFTP)
3. Future E2E tests that exercise the full pipeline

**Verification approach:**
1. Ensure EZ Platform is running (port-forwards active)
2. Run Playwright E2E tests
3. Verify all tests pass

Steps:

```powershell
# 1. Ensure port-forwards are running (from EZ Platform)
# Note: The user should have port-forwards active via start-port-forwards.ps1

# 2. Run E2E tests
cd src/Frontend
npm run test:e2e

# Or for headed mode (visible browser):
npm run test:e2e:headed
```

The E2E tests should pass regardless of file-simulator configuration because they test UI behavior,
not backend file I/O. This validates that the ConfigMap changes don't break existing functionality.

If any tests fail, document the failures in the summary - they are existing issues, not caused by
file-simulator integration.

**Note on playwright.config.ts:**
Review the existing configuration. If it specifies a baseURL pointing to localhost:7000 (frontend port),
no changes are needed. The E2E tests connect to the frontend, which then makes API calls to backend
services. The backend services read file-simulator configuration from environment variables (ConfigMap).

Do NOT modify playwright.config.ts unless there's a specific issue preventing tests from running.
  </action>
  <verify>
```powershell
cd src/Frontend
npm run test:e2e 2>&1 | Tee-Object -Variable testOutput
# Check for "passed" in output
if ($testOutput -match "(\d+) passed") {
    Write-Host "E2E tests completed: $($Matches[0])"
} else {
    Write-Host "Check test output for details"
}
```
  </verify>
  <done>All E2E tests pass (datasource.spec.ts, invalid-records.spec.ts, metrics.spec.ts, alerts.spec.ts) OR failures are documented as pre-existing issues unrelated to file-simulator integration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
File-simulator integration complete:
1. ConfigMap created with all protocol endpoints (FTP, SFTP, S3, HTTP, NFS)
2. Service deployments updated to reference ConfigMap
3. Connectivity verification script created
4. Setup documentation created
5. E2E tests run to validate no regressions
  </what-built>
  <how-to-verify>
1. Check file-simulator connectivity:
   ```powershell
   .\scripts\verify-file-simulator.ps1 -TestOnly
   ```
   Expected: 6/6 tests passed

2. Review ConfigMap:
   ```powershell
   kubectl --context=minikube get configmap file-simulator-config -n ez-platform -o yaml
   ```
   Expected: All FileAccess__* keys present with file-simulator IP

3. Review documentation:
   Open docs/testing/file-simulator-setup.md in browser/editor
   Expected: Complete guide with Quick Start, Verification, Troubleshooting

4. Confirm E2E tests pass:
   ```powershell
   cd src/Frontend && npm run test:e2e
   ```
   Expected: All tests pass (or documented pre-existing failures)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. File-simulator ConfigMap applied to ez-platform namespace
2. Service deployments reference file-simulator-config
3. Verification script confirms 6/6 endpoints reachable
4. Setup documentation exists and is comprehensive
5. All E2E tests pass (or failures documented as pre-existing)
</verification>

<success_criteria>
- [ ] docs/testing/file-simulator-setup.md exists with complete setup guide
- [ ] E2E test results documented (all pass, or failures explained)
- [ ] Human verification confirms:
  - Connectivity script shows 6/6 passed
  - ConfigMap is applied with correct IP
  - Documentation is useful and complete
  - E2E tests run successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-file-simulator-integration/01-02-SUMMARY.md`
</output>
