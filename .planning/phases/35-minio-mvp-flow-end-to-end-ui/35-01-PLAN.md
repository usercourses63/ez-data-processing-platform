---
phase: 35-minio-mvp-flow-end-to-end-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
  - src/Frontend/src/pages/admin/components/ServerModal.tsx
  - src/Frontend/src/pages/admin/components/__tests__/ServerModal.s3port.test.tsx
  - src/Frontend/src/locales/he/translation.json
  - src/Frontend/src/locales/en/translation.json
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "An admin creating a MinIO input server is steered to the S3 API port (30900), not the console port (30901)"
    - "Submitting the input-server form with port 30901 surfaces a clear, blocking validation message before the request is sent"
    - "The bugs-must-fix register exists and tracks G1-G6 with status"
  artifacts:
    - path: "src/Frontend/src/pages/admin/components/ServerModal.tsx"
      provides: "S3 console-port (30901) guard + API-port guidance on the input/output server form"
      contains: "30901"
    - path: ".planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md"
      provides: "Bugs-must-fix register seeded with G1-G6"
      contains: "BLOCKER"
    - path: "src/Frontend/src/pages/admin/components/__tests__/ServerModal.s3port.test.tsx"
      provides: "Unit test for the console-port guard"
  key_links:
    - from: "src/Frontend/src/pages/admin/components/ServerModal.tsx"
      to: "Port form field (S3 branch)"
      via: "validator rule that rejects/warns on 30901 and the console-port range"
      pattern: "30901"
---

<objective>
Establish the bugs-must-fix register as the phase backlog and close **G4**: the MinIO input/output
server form (`ServerModal`) currently accepts the MinIO **Console port 30901**, after which Test
Connection fails with the cryptic MinIO error "S3 API Requests must be made to API port". This is the
first vertical-slice step of the MVP flow — "create a MinIO input server via the UI whose Test
Connection succeeds" — by ensuring the form steers the admin to the **S3 API port 30900** and blocks
the console-port misconfiguration with a clear message before submit.

Purpose: SC-1 (form audit + register) and SC-2 (MinIO input server via UI, console-port misconfig
prevented/surfaced).
Output: Seeded BUGS-MUST-FIX.md, a console-port guard on the S3 server form, i18n strings, unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm and finalize the bugs-must-fix register</name>
  <read_first>
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md (the seeded register)
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md (the G1-G6 table, lines 51-59)
  </read_first>
  <action>
    The register file already exists with G1-G6 seeded. Verify every row matches the CONTEXT.md
    G1-G6 table (IDs, status, severity) and that the closure-gate section references SC-7/SC-8.
    Do NOT change statuses except to confirm G4 and G5 are `OPEN` and `BLOCKER`/`MAJOR` as in CONTEXT.
    This task is the SC-1 anchor: the register is the iterate backlog every later plan updates.
    Add a one-line note under the "Newly discovered bugs" table instructing executors to append rows
    when mapping forms / running the flow turns up new gaps.
  </action>
  <acceptance_criteria>
    - BUGS-MUST-FIX.md contains rows for G1, G2, G3, G4, G5, G6 with a Status and Sev column.
    - G4 status = OPEN, G5 status = OPEN and Sev = BLOCKER.
    - The closure gate section names SC-7 and SC-8.
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw '.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md'; if (($c -match 'G4') -and ($c -match 'G5') -and ($c -match 'BLOCKER') -and ($c -match 'SC-7')) { 'OK' } else { throw 'register incomplete' }"</automated>
  </verify>
  <done>Register confirmed complete and seeded with G1-G6 + closure gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a MinIO console-port (30901) guard + API-port guidance to ServerModal (G4)</name>
  <read_first>
    - src/Frontend/src/pages/admin/components/ServerModal.tsx (the S3 branch: `isS3`, the Port Form.Item ~lines 273-287, the S3 divider section ~lines 306-359)
    - src/Frontend/src/locales/he/translation.json (admin.servers.fields.* keys)
    - src/Frontend/src/locales/en/translation.json
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md (decision: endpoint :30900 is the S3 API port, :30901 is the console — never use it for S3; lines 42-43)
  </read_first>
  <behavior>
    - Test: when ServerType is `s3` and Port is `30901`, the form validation rejects submit with a
      message that names the S3 API port (30900) vs console port (30901).
    - Test: when ServerType is `s3` and Port is `30900`, validation passes (no console-port error).
    - Test: a non-S3 server type (e.g. `ftp`) with port 30901 is NOT blocked by this rule (guard is S3-only).
  </behavior>
  <action>
    On the `Port` Form.Item, add an S3-only async/sync validator rule (active only when
    `serverType?.toLowerCase() === 's3'`) that rejects the MinIO **console port 30901** (and any port
    in the MinIO console range if cheaply detectable — keep it to the literal 30901 to avoid false
    positives) with a clear message: the S3 endpoint must use the **API port (default 30900)**, not the
    console port (30901). Add an inline helper `Typography.Text type="secondary"` under the S3 divider
    (the `isS3` block ~line 307) stating "MinIO: use the S3 API port (e.g. 30900). The console port
    (30901) is not an S3 endpoint." Add the i18n keys `admin.servers.s3.consolePortError` and
    `admin.servers.s3.apiPortHint` to both `he` and `en` translation.json (Hebrew primary, English
    fallback) and reference them with `t(...) || '<fallback>'` as the existing code does. Do NOT change
    the request mapping or the PascalCase `TypeSpecificConfig` contract (34-01) — this is form
    validation + guidance only. Keep the port InputNumber `className="ltr-field"`. Update the G4 row in
    BUGS-MUST-FIX.md to `FIX-IN-CODE` and note the live re-verify happens in 35-03.
  </action>
  <acceptance_criteria>
    - `ServerModal.tsx` Port field has a validator that fires only for S3 and rejects 30901 with a message naming 30900.
    - Vitest `ServerModal.s3port.test.tsx` passes the three behavior cases above.
    - Both translation.json files contain `admin.servers.s3.consolePortError` and `admin.servers.s3.apiPortHint`.
    - `npx tsc --noEmit` is clean for the touched files.
    - BUGS-MUST-FIX.md G4 row updated to FIX-IN-CODE.
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npx vitest run src/pages/admin/components/__tests__/ServerModal.s3port.test.tsx</automated>
  </verify>
  <done>S3 server form blocks port 30901 with API-port guidance; tests green; G4 marked FIX-IN-CODE.</done>
</task>

</tasks>

<verification>
- The bugs-must-fix register is the authoritative backlog (SC-1).
- The S3 server form prevents the 30901 console-port misconfiguration and guides to 30900 (SC-2).
- Live Test-Connection success against file-simulator MinIO is re-verified in 35-03 (needs the running cluster).
</verification>

<success_criteria>
- BUGS-MUST-FIX.md seeded with G1-G6 + closure gate (SC-1).
- ServerModal rejects S3 port 30901 with a clear API-port message; unit tests pass (SC-2).
</success_criteria>

<output>
Create `.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-01-SUMMARY.md` when done.
</output>
