---
phase: 35-minio-mvp-flow-end-to-end-ui
plan: 04
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - tests/IntegrationTests/Pipeline/MinioOutputPipelineTests.cs
  - tests/IntegrationTests/TestConfiguration.cs
  - src/Frontend/tests/e2e/protocols/s3-mvp-flow.spec.ts
  - src/Frontend/tests/webapp-testing/minio_mvp_flow_comprehensive_test.py
autonomous: true
requirements: [SC-6]
must_haves:
  truths:
    - "An integration test asserts the converted JSON object appears in the MinIO output bucket (not a Kafka topic)"
    - "A Playwright E2E spec drives UI create-input-server -> output-server -> datasource -> run -> verify"
    - "A webapp-testing comprehensive script exercises the three forms with edge cases"
    - "The G6 region-omitted behavior has a regression guard"
  artifacts:
    - path: "tests/IntegrationTests/Pipeline/MinioOutputPipelineTests.cs"
      provides: "Integration test asserting the MinIO output object via list+fetch over the RabbitMQ pipeline reality"
      contains: "ez-phase34-output"
    - path: "src/Frontend/tests/e2e/protocols/s3-mvp-flow.spec.ts"
      provides: "Playwright E2E for the UI create->run->verify MVP flow"
      contains: "ez-phase34"
    - path: "src/Frontend/tests/webapp-testing/minio_mvp_flow_comprehensive_test.py"
      provides: "Comprehensive Python Playwright UI verification of the three MinIO forms"
  key_links:
    - from: "tests/IntegrationTests/Pipeline/MinioOutputPipelineTests.cs"
      to: "MinIO output bucket object"
      via: "S3 ListObjectsV2 + GetObject assertion (skips cleanly if MinIO unreachable)"
      pattern: "ListObjectsV2|GetObject"
---

<objective>
Deliver the test coverage that proves the MVP flow and guards the fixed mappings (SC-6): unit tests
already live with their fixes (35-01 ServerModal port guard, 35-02 frontend s3Config + backend
output-S3 bridge). This plan adds the **integration test updated to the RabbitMQ pipeline reality
asserting the MinIO output object** (not Kafka topics, per the locked decision), a **Playwright E2E**
for the UI create→run→verify flow, and a **webapp-testing comprehensive** pass over the three forms.
All live-dependent tests must SKIP CLEANLY when the cluster / file-simulator MinIO is unreachable (the
authoritative live execution is the 35-03 checkpoint), matching the 34-04/34-05 capability-guard pattern.

Purpose: SC-6 (tests: unit + integration asserting MinIO output object + Playwright E2E + webapp-testing).
Output: MinioOutputPipelineTests.cs, s3-mvp-flow.spec.ts, minio_mvp_flow_comprehensive_test.py,
TestConfiguration note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration test — assert the MinIO output object (RabbitMQ pipeline reality, not Kafka)</name>
  <read_first>
    - tests/IntegrationTests/Pipeline/PipelineFlowTests.cs (existing pipeline test patterns; the Kafka-topic subscriptions that no longer match the live RabbitMQ bus)
    - tests/IntegrationTests/Connectors/S3ConnectorTests.cs (how the suite reaches file-simulator MinIO: FILE_SIMULATOR_IP, appuser creds, SkippableFact capability guard)
    - tests/IntegrationTests/TestConfiguration.cs (the existing RabbitMQ-vs-Kafka note from 34-06; PipelineWaitTimeout)
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md (decisions: verify the MinIO output object NOT Kafka topics; buckets ez-phase34-input/output; creds; path-style :30900)
  </read_first>
  <action>
    Add `MinioOutputPipelineTests.cs` with a `[Category=S3EndToEnd]` SkippableFact that: seeds a CSV
    into `ez-phase34-input` via the S3 API (header `id,name,amount,date`, unique filename to dodge
    Hazelcast dedup), creates/uses a datasource wired MinIO-in -> MinIO-out (`ez-phase34-output/processed`)
    with a persisted S3Config (build the request the same shape 35-02 produces), triggers the pipeline,
    then **polls `ListObjectsV2` on `ez-phase34-output/processed/` and `GetObject`** the produced object,
    asserting the body is the CSV-derived JSON array. The test MUST `Skip` (not fail) when
    FILE_SIMULATOR_IP / MinIO is unreachable or Mongo is down (mirror the S3ConnectorTests guard). Do
    NOT subscribe to Kafka topics for the assertion. Extend the TestConfiguration note to point future
    readers at this test as the canonical pipeline-output assertion.
  </action>
  <acceptance_criteria>
    - `MinioOutputPipelineTests.cs` asserts via S3 ListObjectsV2 + GetObject against `ez-phase34-output`, never a Kafka topic.
    - The test Skips cleanly (green) when MinIO/Mongo unreachable.
    - `dotnet test tests/IntegrationTests --filter "FullyQualifiedName~MinioOutputPipeline" --list` enumerates it without build errors.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build tests/IntegrationTests</automated>
  </verify>
  <done>Integration test asserts the MinIO output object over the RabbitMQ pipeline reality; skips cleanly offline.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E + webapp-testing comprehensive for the MVP flow</name>
  <read_first>
    - src/Frontend/tests/e2e/protocols/s3.spec.ts and s3-server.spec.ts (existing S3 spec structure, selectors, skip-when-unreachable guards)
    - src/Frontend/tests/webapp-testing/s3_server_comprehensive_test.py (reconnaissance-first comprehensive pattern; env overrides BASE_URL/SERVERS_API/MINIO_*)
    - src/Frontend/playwright.config.ts (baseURL http://localhost:7000)
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md (form maps section A/B for selectors; G4 console-port, G5 output S3Config)
  </read_first>
  <action>
    Create `s3-mvp-flow.spec.ts` enumerating the UI slice: create MinIO input server (assert the 30901
    console-port block + 30900 acceptance, G4), create MinIO output server, create a datasource wired
    MinIO-in + MinIO-out (assert the output S3 destination shows its bucket), and (when the backend is
    reachable) trigger a run. Use `test.skip` when the servers API / frontend is unreachable so it stays
    green pre-live (match s3-server.spec.ts). Create `minio_mvp_flow_comprehensive_test.py` (Python
    Playwright, reconnaissance-first: discover DOM then verify) exercising edge cases on the three forms:
    console-port error text, masked secret round-trip, output destination s3Config persistence on
    reopen, "file path missing" must NOT appear when filePath is set (G2), connection survives a schema
    edit (G3). Both must `py_compile`/`--list` clean and skip cleanly offline. Authoritative live runs
    happen in the 35-03 checkpoint; here they only need to be authored + statically valid.
  </action>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/protocols/s3-mvp-flow.spec.ts --list` enumerates tests with no compile errors.
    - `python -m py_compile tests/webapp-testing/minio_mvp_flow_comprehensive_test.py` exits 0.
    - Both assert the MinIO output object / S3Config persistence, not Kafka topics; both skip cleanly when the app is down.
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/protocols/s3-mvp-flow.spec.ts --list</automated>
  </verify>
  <done>Playwright E2E + webapp-testing comprehensive specs authored, statically valid, skip cleanly offline.</done>
</task>

</tasks>

<verification>
- Integration assertions verify the MinIO output object (list+fetch), never Kafka topics (locked decision).
- All live-dependent tests skip cleanly offline; the authoritative live pass is the 35-03 checkpoint.
- G6 region-omitted behavior is implicitly guarded by the output-write succeeding with no Region set.
</verification>

<success_criteria>
- Unit (35-01/35-02) + integration (MinIO output object) + Playwright E2E + webapp-testing coverage exists and is statically valid (SC-6).
</success_criteria>

<output>
Create `.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-04-SUMMARY.md` when done.
</output>
