---
phase: 35-minio-mvp-flow-end-to-end-ui
plan: 05
type: execute
wave: 3
depends_on: ["35-03", "35-04"]
files_modified:
  - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
  - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md
autonomous: false
requirements: [SC-6, SC-7, SC-8]
must_haves:
  truths:
    - "Every BLOCKER/MAJOR bug in the register is VERIFIED or explicitly WONTFIX-MVP"
    - "Zero open MVP-blocking bugs remain"
    - "The MVP proof is a clean, repeatable, UI-driven demo producing a fresh MinIO output object"
  artifacts:
    - path: ".planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md"
      provides: "Final closure-gate review with every row resolved"
      contains: "VERIFIED"
  key_links:
    - from: "BUGS-MUST-FIX.md closure gate"
      to: "35-MVP-PROOF.md"
      via: "final review confirms the repeatable UI-driven demo"
      pattern: "MVP"
---

<objective>
Close the phase: run the bugs-must-fix review→fix loop to completion and sign off the MVP proof. This
is the SC-7 closure gate — the phase may close ONLY when every BLOCKER/MAJOR bug is `VERIFIED` (or an
explicit `WONTFIX-MVP` with rationale) and zero open MVP-blocking bugs remain — plus a final SC-8
confirmation that the UI-driven demo is clean and repeatable. If any bug is still open or any new bug
surfaced in 35-03/35-04, this plan either drives it closed (small fixes) or recommends a follow-up plan
(loop / iterate).

Purpose: SC-7 (bugs-must-fix closed, zero open blockers), SC-8 (repeatable MVP proof), SC-6 (full
test pass confirmation).
Output: finalized BUGS-MUST-FIX.md closure gate + signed-off 35-MVP-PROOF.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-03-SUMMARY.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review the register, resolve or escalate every remaining bug</name>
  <read_first>
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md (all rows incl. any appended during 35-03/35-04)
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md (the live proof from 35-03)
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-03-SUMMARY.md and 35-04-SUMMARY.md
  </read_first>
  <action>
    Walk every row in BUGS-MUST-FIX.md. For each BLOCKER/MAJOR that is not yet `VERIFIED`: if it is a
    small, in-scope fix, fix it and re-verify (re-run the relevant unit/integration test or re-do the
    UI step), then mark VERIFIED with the proof reference; if it requires substantial new work, mark it
    OPEN and write a `## PHASE NOT CLOSED — follow-up needed` note naming the bug and recommending a
    new plan (do NOT mark VERIFIED without proof). Any row legitimately out of MVP scope gets
    `WONTFIX-MVP` with a one-line rationale (must align with the CONTEXT Deferred list — Kafka-topic
    test rewrite, observability tests, FTP/SFTP seed sidecars, non-MinIO UX polish). Confirm the full
    test set referenced by SC-6 is accounted for (unit from 35-01/35-02, integration + E2E + webapp
    from 35-04).
  </action>
  <acceptance_criteria>
    - Every BLOCKER/MAJOR row is VERIFIED or WONTFIX-MVP (with rationale).
    - No BLOCKER row remains OPEN/FIX-IN-CODE; if any does, a follow-up plan is named.
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c = Get-Content '.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md' | Where-Object { $_ -match '^\| G\d' -and $_ -match 'BLOCKER' }; $open = $c | Where-Object { $_ -match 'OPEN' }; if ($open) { throw 'open blocker remains' } else { 'OK' }"</automated>
  </verify>
  <done>All blocker/major bugs resolved or explicitly deferred; closure gate satisfiable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The bugs-must-fix register is fully resolved and the MVP proof is recorded. This checkpoint is the
    final SC-8 sign-off that the UI-driven demo is clean and repeatable.
  </what-built>
  <how-to-verify>
    Using only the UI (a fresh browser session at http://localhost:7000), repeat the MVP flow once more
    end to end with a NEW datasource name and a fresh input CSV (unique filename): create/select MinIO
    input + output servers, create the data source, run it, and confirm a fresh JSON object lands in
    `ez-phase34-output`. Confirm it took no manual Mongo edits and no kubectl workarounds. Confirm the
    register shows zero open MVP blockers.
  </how-to-verify>
  <resume-signal>Type "approved — MVP repeatable" or describe what still breaks (loop back to Task 1 / file a follow-up).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize the closure gate and MVP proof</name>
  <read_first>
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md
  </read_first>
  <action>
    Stamp the BUGS-MUST-FIX.md closure-gate section as satisfied (date + the SC-8 repeatable-demo
    confirmation) and append the second repeatable-run object key to 35-MVP-PROOF.md. If the checkpoint
    did NOT pass, instead record exactly what failed and leave the gate unsatisfied with a named
    follow-up.
  </action>
  <acceptance_criteria>
    - BUGS-MUST-FIX.md closure gate is marked satisfied (or explicitly not, with a follow-up).
    - 35-MVP-PROOF.md records two independent UI-driven runs producing MinIO output objects.
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw '.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md'; if (([regex]::Matches($c,'ez-phase34-output')).Count -ge 2) { 'OK' } else { throw 'need two repeatable runs' }"</automated>
  </verify>
  <done>Closure gate finalized; MVP proven repeatable through the UI.</done>
</task>

</tasks>

<verification>
- SC-7: zero open MVP-blocking bugs in the register.
- SC-8: two independent UI-driven runs each produced a fresh MinIO output object.
</verification>

<success_criteria>
- Bugs-must-fix register fully resolved; MVP proof repeatable (SC-6, SC-7, SC-8).
</success_criteria>

<output>
Create `.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-05-SUMMARY.md` when done.
</output>
