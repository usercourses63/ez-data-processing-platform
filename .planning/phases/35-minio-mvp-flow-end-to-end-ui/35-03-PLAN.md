---
phase: 35-minio-mvp-flow-end-to-end-ui
plan: 03
type: execute
wave: 2
depends_on: ["35-01", "35-02"]
files_modified:
  - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md
  - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
autonomous: false
requirements: [SC-2, SC-3, SC-4, SC-5, SC-8]
user_setup:
  - service: file-simulator-minio
    why: "The MVP flow reads from / writes to the Session-B file-simulator MinIO over the S3 API"
    env_vars:
      - name: FILE_SIMULATOR_IP
        source: "CLUSTER-COORDINATION.md OWNERSHIP table (currently 172.24.80.23; drifts on host reboot)"
must_haves:
  truths:
    - "An admin can create a MinIO input server, a MinIO output server, and a wired data source entirely through the UI"
    - "Test Connection on the MinIO input server succeeds; the console-port (30901) misconfig is prevented (G4)"
    - "The data source's filePath persists (G2), output S3Config persists (G5), connection survives a schema edit (G3), no 'can't allocate input' (G1)"
    - "Running the data source writes a fresh converted JSON object into the MinIO output bucket"
    - "The output object content is the CSV-derived JSON array (verified by fetching the object)"
  artifacts:
    - path: ".planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md"
      provides: "Recorded live proof: datasource id, output object key, fetched object content, Mongo S3Config dump"
      contains: "ez-phase34-output"
  key_links:
    - from: "UI-created data source (MinIO in + MinIO out)"
      to: "s3://ez-phase34-output/<prefix>/<object>.json"
      via: "trigger run -> FileDiscovery -> FileProcessor CSV->JSON -> Validation -> Output S3 write"
      pattern: "ez-phase34-output"
---

<objective>
Prove the MVP flow end-to-end **through the UI** against the running cluster, on top of the G4 fix
(35-01) and the G5 fix (35-02). Deploy the rebuilt frontend + DataSourceManagement images, then drive
the full slice: create a MinIO input server (Test Connection succeeds, console-port blocked), create a
MinIO output server (S3 config persists), create a data source wired to both, edit its schema and
confirm the connection survives, run it, and verify a fresh converted JSON object lands in the MinIO
output bucket by listing + fetching the object. This is the authoritative MVP proof (SC-8) and the
live re-verify of G1-G5.

Purpose: SC-2, SC-3, SC-4, SC-5, SC-8 — the run-and-verify slice that makes the MVP real.
Output: 35-MVP-PROOF.md with the persisted Mongo S3Config + the fetched MinIO output object content;
updated BUGS-MUST-FIX statuses to VERIFIED.

CRITICAL coordination (CLAUDE.md + CLUSTER-COORDINATION.md): this is Session A — operate only on the
`minikube` profile / `ez-platform` namespace with explicit `--context=minikube`. NEVER run
kubectl/minikube/helm against `file-simulator`; consume its MinIO ONLY via the S3 API
(`http://<FILE_SIMULATOR_IP>:30900`, path-style, creds `appuser`/`Appsecret123`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-01-SUMMARY.md
@.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically rebuild + redeploy frontend and DataSourceManagement; reset run preconditions</name>
  <read_first>
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/35-CONTEXT.md (redeploy pattern lines 129; Hazelcast dedup map clear line 131; port-forwards line 130)
    - C:\Users\Brian\.claude\shared\CLUSTER-COORDINATION.md (Session A lane rules; frontend Dockerfile gotcha = src/Frontend/Dockerfile NOT docker/Frontend.Dockerfile; env overrides Hazelcast__Server / FileDiscovery__DeduplicationTTLHours must survive)
    - CLAUDE.md (port-forward script; surgical set-image pattern)
  </read_first>
  <action>
    Build the frontend image from `src/Frontend/Dockerfile` (NOT `docker/Frontend.Dockerfile` — it is
    broken) with a fresh tag (e.g. `frontend:phase35-mvp-<date>`), and the DataSourceManagement image
    from its Dockerfile with tag `datasource-management:phase35-mvp-<date>`. `minikube image load -p
    minikube` both, then `kubectl --context=minikube -n ez-platform set image deployment/frontend ...`
    and `deployment/datasource-management ...`. Preserve env overrides (do NOT helm upgrade). Wait for
    both rollouts ready. Ensure port-forwards are up via `scripts/start-port-forwards.ps1` (frontend
    7000, DSM 5001, output 5009, Mongo 27017, Hazelcast 5701). Re-seed fresh CSVs into the MinIO input
    bucket `ez-phase34-input` (header `id,name,amount,date`) via the S3 API only (the object data is
    ephemeral). Clear the Hazelcast dedup map `file-hashes-<dsId>` OR plan to use unique filenames so
    the run is not deduped. Record the FILE_SIMULATOR_IP in use.
  </action>
  <acceptance_criteria>
    - `kubectl --context=minikube -n ez-platform get pods` shows frontend + datasource-management pods 1/1 on the new tags, 0 restarts.
    - Frontend reachable at http://localhost:7000 (HTTP 200); DSM /health 200 on :5001.
    - At least one fresh CSV present in `ez-phase34-input` via S3 API list.
    - No kubectl/minikube/helm command targeted the `file-simulator` profile.
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "(Invoke-WebRequest -UseBasicParsing http://localhost:5001/health).StatusCode" </automated>
  </verify>
  <done>Rebuilt frontend + DSM deployed on minikube; preconditions reset; MinIO input seeded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The G4 (input-server API-port guard) and G5 (output S3Config persistence) fixes are deployed. This
    checkpoint drives the full MVP flow through the UI and verifies the persisted Mongo S3Config and the
    real MinIO output object.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:7000. Go to System Settings -> Input Servers -> create a MinIO **input**
       server: Host = file-simulator IP, **Port = 30901** first → confirm the form BLOCKS it with the
       API-port message (G4). Change Port to **30900**, ServerType S3, AccessKey `appuser`, SecretKey
       `Appsecret123`, Bucket `ez-phase34-input`, path-style ON, HTTP ON. Save → click **Test
       Connection** → expect CONNECTION SUCCESSFUL (SC-2).
    2. System Settings -> Output Servers -> create a MinIO **output** server (Direction Output): same
       creds/endpoint, Bucket `ez-phase34-output`, path-style ON, HTTP ON. Save → reopen → confirm the
       S3 config persisted (bucket shows, secret masked) (SC-3).
    3. Create a **data source**: Basic Info; Input Settings tab → Protocol S3 → select the MinIO input
       server → Bucket/Prefix (`filePath`) = `ez-phase34-input` → Test Connection SUCCESSFUL (no "can't
       allocate input", G1); Output tab → add destination → protocol S3 → select the MinIO output server
       → Bucket/Prefix `ez-phase34-output/processed`. Save (SC-4).
    4. Edit the data source, change the Schema, Save, reopen → confirm the input connection is still set
       (G3) and `filePath` still shows the bucket (G2).
    5. In Mongo (`mongosh` on :27017, db `ezplatform`), dump the datasource's `Output.Destinations[0].S3Config`
       → confirm `Bucket` = `ez-phase34-output`, `Endpoint` set, `AccessKeyId`/`SecretAccessKey` present,
       `Region` null/empty, `UsePathStyle` true (G5 persisted).
    6. **Run** the data source (trigger a poll). Wait, then list `s3://ez-phase34-output/processed/` via
       the S3 API → confirm a fresh `.json` object appeared. **Fetch it** → confirm the content is the
       CSV-derived JSON array (e.g. `[{"id":...,"name":...,"amount":...,"date":...}, ...]`) (SC-5, SC-8).
    Paste the datasource id, the S3Config Mongo dump, the output object key, and the fetched object body.
  </how-to-verify>
  <resume-signal>Type "approved" with the pasted proof, or describe which step failed (file a new BUGS-MUST-FIX row).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the MVP proof and update bug statuses</name>
  <read_first>
    - .planning/phases/35-minio-mvp-flow-end-to-end-ui/BUGS-MUST-FIX.md
  </read_first>
  <action>
    Write `35-MVP-PROOF.md` capturing: datasource id, the persisted `Output.Destinations[].S3Config`
    Mongo dump, the MinIO output object key, and the fetched object content, plus the input Test
    Connection result and the G4 console-port block screenshot/notes. In BUGS-MUST-FIX.md set G1, G2,
    G3, G4, G5 to `VERIFIED` (referencing this proof) IF the checkpoint passed each; for any step that
    failed, leave the row OPEN/FIX-IN-CODE and append a new row describing the failure with a closing
    plan (loop back — do not mark VERIFIED without proof).
  </action>
  <acceptance_criteria>
    - 35-MVP-PROOF.md exists and contains the output object key under `ez-phase34-output` + the fetched JSON body.
    - BUGS-MUST-FIX.md reflects the checkpoint outcome (VERIFIED only where proven).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw '.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-MVP-PROOF.md'; if ($c -match 'ez-phase34-output') { 'OK' } else { throw 'proof missing output object' }"</automated>
  </verify>
  <done>MVP proof recorded; bug register updated to reflect live verification.</done>
</task>

</tasks>

<verification>
- Authoritative proof = a fresh converted JSON object in `ez-phase34-output` (NOT a Kafka topic) + the persisted Mongo S3Config.
- G1-G5 are re-verified live in the UI; any failure loops back into the register.
</verification>

<success_criteria>
- Full UI-driven flow: MinIO input server + output server + data source created, run, fresh JSON object in MinIO output bucket (SC-2..SC-5, SC-8).
</success_criteria>

<output>
Create `.planning/phases/35-minio-mvp-flow-end-to-end-ui/35-03-SUMMARY.md` when done.
</output>
