---
phase: 31-feature-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts
autonomous: true
requirements:
  - TIER-2-I18N
must_haves:
  truths:
    - "Hebrew translation keys exist for clone, import, and completeness features"
    - "RTL layout renders correctly on datasource list and create form pages"
    - "Technical fields (file patterns, paths, regex) remain LTR in Hebrew mode"
    - "Clone confirmation modal renders Hebrew text"
    - "Import preview modal renders Hebrew labels"
    - "Completeness sidebar renders Hebrew tab names"
  artifacts:
    - path: "src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts"
      provides: "i18n/RTL regression test suite for v0.4.0/v0.5.0 features"
      min_lines: 120
  key_links:
    - from: "i18n-rtl-regression.spec.ts"
      to: "http://127.0.0.1:7000"
      via: "page.goto with Hebrew locale verification"
      pattern: "[\u05D0-\u05EA]"
---

<objective>
Create a new i18n/RTL regression test suite for v0.4.0/v0.5.0 features.

Purpose: While rtl-visual.spec.ts covers generic RTL layout, there are no tests verifying that the new features (clone, import, completeness) render correctly in Hebrew. This plan creates targeted regression tests checking Hebrew text presence, RTL layout, and LTR technical field behavior for the new features.

Output: New i18n-rtl-regression.spec.ts with 6-8 tests covering Hebrew rendering for clone modal, import modal, completeness sidebar, and technical field LTR behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Frontend/tests/e2e/sanity.spec.ts (patterns)
@src/Frontend/tests/e2e/rtl-visual.spec.ts (existing RTL tests — don't duplicate)
@src/Frontend/src/i18n/locales/he.json (Hebrew translation keys)

<interfaces>
<!-- Hebrew i18n keys for new features (from he.json) -->

Clone keys:
  - datasources.clone.confirmTitle
  - datasources.clone.confirmMessage
  - datasources.clone.confirmOk / confirmCancel
  - datasources.clone.button
  - datasources.clone.namePrefix = "העתק של"

Import keys:
  - datasources.importFromFile = "מקור נתונים חדש מקובץ"
  - datasources.import.* (modal labels, continue button, etc.)

Completeness keys:
  - completeness.* (tab names, progress labels, missing fields text)

RTL verification selectors:
  - html[dir="rtl"] or body[dir="rtl"]
  - .ltr-field class for technical inputs
  - .ant-input[dir="ltr"] for path/regex inputs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n-rtl-regression.spec.ts for v0.4.0/v0.5.0 features</name>
  <files>src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts</files>
  <read_first>
    - src/Frontend/tests/e2e/sanity.spec.ts (lines 1-75 for helpers and constants)
    - src/Frontend/tests/e2e/rtl-visual.spec.ts (existing RTL tests — avoid duplication)
    - src/Frontend/src/i18n/locales/he.json (search for "clone", "import", "completeness" sections)
    - src/Frontend/src/pages/datasources/DataSourceList.tsx (clone button tooltip, import button text)
  </read_first>
  <action>
Create new `i18n-rtl-regression.spec.ts`:

1. **Header**: Import test/expect/Page/APIRequestContext from @playwright/test. Define FRONTEND_URL, DATASOURCE_API constants. Copy extractId, antTabClick, antSelect helpers from sanity.spec.ts.

2. **Setup**: `beforeAll` creates apiContext. `beforeEach` sets viewport 1440x900.

3. **Test: I18N-01 — Hebrew document direction on datasource pages**: Navigate to /datasources. Verify `html[dir="rtl"]` or body direction is RTL. Verify Ant Design table has RTL styling (direction CSS property).

4. **Test: I18N-02 — Clone button tooltip renders Hebrew**: Navigate to /datasources. Wait for table. Hover over `.anticon-copy` button. Verify tooltip contains Hebrew characters matching `datasources.clone.button` key (use regex `[\u05D0-\u05EA]`).

5. **Test: I18N-03 — Clone confirmation modal renders Hebrew**: Click clone icon on first row. Wait for `.ant-modal-confirm`. Verify modal title contains Hebrew chars. Verify OK/Cancel buttons have Hebrew text. Dismiss modal.

6. **Test: I18N-04 — Import button renders Hebrew text**: Navigate to /datasources. Verify import button contains Hebrew text matching "מקור נתונים חדש מקובץ" or similar.

7. **Test: I18N-05 — Completeness sidebar renders Hebrew tab names**: Navigate to /datasources/new. Wait for `div.ant-progress-circle`. Get sidebar card. Verify tab items text contains Hebrew characters. Verify at least "מידע בסיסי" (Basic Info) or equivalent Hebrew tab label is present.

8. **Test: I18N-06 — Technical fields are LTR in Hebrew mode**: Navigate to /datasources/new. Switch to Connection tab (antTabClick 'connection'). Find file pattern input (`input#filePattern`). Verify it has LTR direction (check computed style or class). Switch to Schema tab. Verify Monaco editor or schema textarea has LTR direction.

9. **Test: I18N-07 — Create form labels are Hebrew**: Navigate to /datasources/new. Verify form labels contain Hebrew text (not English). Check that label for name field contains Hebrew chars.

10. **Cleanup**: `afterAll` disposes apiContext.

Key patterns:
- Hebrew character regex: `/[\u05D0-\u05EA]/` matches any Hebrew letter
- Do NOT duplicate generic RTL tests from rtl-visual.spec.ts (document direction, Ant Design config)
- Focus on NEW feature-specific Hebrew rendering
- Use `page.evaluate(() => getComputedStyle(el).direction)` for LTR verification
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/i18n-rtl-regression.spec.ts --list 2>&1 | grep -c "test"</automated>
  </verify>
  <acceptance_criteria>
    - New file created at src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts
    - At least 6 test cases listed
    - Tests verify Hebrew characters with regex pattern [\u05D0-\u05EA]
    - Tests cover clone modal, import button, completeness sidebar Hebrew rendering
    - Tests verify LTR direction on technical fields
    - grep confirms: `grep -c "05D0.*05EA" src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts` returns >= 2
    - grep confirms: `grep -c "FRONTEND_URL" src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts` returns >= 3
  </acceptance_criteria>
  <done>i18n/RTL regression test suite created with 6+ tests covering Hebrew rendering for clone, import, and completeness features, plus LTR verification for technical fields.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npx playwright test tests/e2e/i18n-rtl-regression.spec.ts --list` shows 6+ tests
- `grep -c "05D0" src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts` returns >= 2
- File exists and is not empty: `wc -l src/Frontend/tests/e2e/i18n-rtl-regression.spec.ts` >= 120
</verification>

<success_criteria>
New i18n/RTL regression suite covers Hebrew rendering for all v0.4.0/v0.5.0 features and verifies technical fields stay LTR. No duplication with existing rtl-visual.spec.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/31-feature-test-coverage/31-04-SUMMARY.md`
</output>
