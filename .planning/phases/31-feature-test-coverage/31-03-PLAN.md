---
phase: 31-feature-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/tests/e2e/completeness-checklist.spec.ts
autonomous: true
requirements:
  - TIER-2-COMPLETENESS
must_haves:
  truths:
    - "Create form shows sidebar with progress ring and 9 tab items"
    - "Progress ring updates as required fields are filled"
    - "Sidebar tab click navigates to corresponding form tab"
    - "Active tab highlighted with background color in sidebar"
    - "Edit form sidebar reflects existing datasource completeness"
    - "List page shows mini progress ring per row"
    - "Per-field data-completeness-status attributes present on form items"
  artifacts:
    - path: "src/Frontend/tests/e2e/completeness-checklist.spec.ts"
      provides: "Completeness checklist E2E test suite"
      min_lines: 350
  key_links:
    - from: "completeness-checklist.spec.ts"
      to: "CompletenessPanel.tsx"
      via: "div.ant-progress-circle selector"
      pattern: "ant-progress-circle"
---

<objective>
Upgrade completeness-checklist E2E tests with production patterns and API context.

Purpose: The existing completeness test suite (553 lines, 15 tests including gap closure) is the most comprehensive existing test file. It already uses env-var URLs and good selectors. The main upgrade needed is adding API context for reliable datasource creation/cleanup in edit-mode tests and ensuring antTabClick is used consistently.

Output: Production-quality completeness-checklist.spec.ts maintaining all existing coverage with upgraded patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Frontend/tests/e2e/sanity.spec.ts (patterns: extractId, extractData, antTabClick, apiContext)
@src/Frontend/tests/e2e/completeness-checklist.spec.ts (existing 553-line test suite)
@src/Frontend/src/components/datasource/completeness/CompletenessPanel.tsx
@src/Frontend/src/components/datasource/completeness/completenessConfig.ts
@src/Frontend/src/components/datasource/completeness/types.ts

<interfaces>
<!-- Completeness panel DOM structure -->

Sidebar card: `.ant-card` containing `.ant-progress`
Progress ring: `div.ant-progress-circle` with `aria-valuenow` attribute (0-100)
Tab items: `div[style*="padding: 8px 12px"]` inside sidebar card (9 items)
Active tab: background-color rgb(230, 244, 255) = #e6f4ff
Status icons:
  - .anticon-check-circle (complete)
  - .anticon-close-circle (required incomplete)
  - .anticon-warning (recommended incomplete)
  - .anticon-minus-circle (optional)
Field borders: [data-completeness-status] attribute with values:
  - "required-empty" | "filled" | "optional-empty"
List mini ring: `.ant-table-tbody .ant-progress-circle`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade completeness-checklist.spec.ts with API context and cleanup</name>
  <files>src/Frontend/tests/e2e/completeness-checklist.spec.ts</files>
  <read_first>
    - src/Frontend/tests/e2e/sanity.spec.ts (lines 1-75 for helpers and API patterns)
    - src/Frontend/tests/e2e/completeness-checklist.spec.ts (full file — preserve all 15 tests)
    - src/Frontend/src/components/datasource/completeness/CompletenessPanel.tsx (DOM structure)
  </read_first>
  <action>
Upgrade `completeness-checklist.spec.ts` with these specific changes:

1. **Add API context**: Import `APIRequestContext, request` from @playwright/test. Add `apiContext` in `beforeAll`/`afterAll`. This enables reliable datasource creation for edit-mode tests instead of depending on seeded data.

2. **Add helpers from sanity.spec.ts**: Copy extractId, extractData, antTabClick, antSelect helpers. These already exist as local `antSelect` — consolidate to match sanity.spec.ts version exactly.

3. **Upgrade Test 7 (edit form)**: Instead of clicking edit on first seeded row (fragile), create a test datasource via API with known fields, then navigate to its edit URL directly. After test, cleanup via API with `?deletedBy=SanityTest`. This makes the test deterministic.

4. **Keep all existing tests unchanged** (Tests 1-6, 8-10, GAP-01 through GAP-05). They use DOM selectors that work correctly and don't need API context.

5. **Ensure FRONTEND_URL and DATASOURCE_API constants** are at the top (already present — verify they match sanity.spec.ts values).

6. **Keep navigateAndWait helper** — it works well for these UI-heavy tests.

7. **Keep getSidebarCard and getTabItems helpers** — they correctly identify completeness DOM elements.

Key changes summary:
- Add APIRequestContext import + apiContext lifecycle
- Add extractId, extractData helpers
- Upgrade Test 7 to use API-created datasource
- Keep everything else intact (15 tests total)
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/completeness-checklist.spec.ts --list 2>&1 | grep -c "test"</automated>
  </verify>
  <acceptance_criteria>
    - File contains apiContext with beforeAll/afterAll lifecycle
    - File contains extractId helper
    - Test 7 (edit form) creates datasource via API and cleans up with ?deletedBy=SanityTest
    - All 15 existing tests preserved (grep for test count)
    - grep confirms: `grep -c "apiContext" src/Frontend/tests/e2e/completeness-checklist.spec.ts` returns >= 3
    - grep confirms: `grep -c "deletedBy=SanityTest" src/Frontend/tests/e2e/completeness-checklist.spec.ts` returns >= 1
    - `npx playwright test tests/e2e/completeness-checklist.spec.ts --list` shows 15 tests
  </acceptance_criteria>
  <done>Completeness checklist E2E tests upgraded with API context for deterministic edit-mode testing. All 15 existing tests preserved. API-based datasource creation and cleanup for Test 7.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npx playwright test tests/e2e/completeness-checklist.spec.ts --list` shows 15 tests
- `grep -c "apiContext" src/Frontend/tests/e2e/completeness-checklist.spec.ts` returns >= 3
- `grep -c "deletedBy=SanityTest" src/Frontend/tests/e2e/completeness-checklist.spec.ts` returns >= 1
</verification>

<success_criteria>
Completeness E2E tests have API context for deterministic testing, all 15 original tests preserved, edit-mode test uses API-created datasource with cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/31-feature-test-coverage/31-03-SUMMARY.md`
</output>
