---
phase: 31-feature-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/tests/e2e/clone-datasource.spec.ts
autonomous: true
requirements:
  - TIER-2-CLONE
must_haves:
  truths:
    - "Clone from list page triggers confirmation modal and navigates to pre-filled form"
    - "Clone from details page header works identically"
    - "Cloned form has name prefixed with 'העתק של', supplier and category copied"
    - "Clone preserves connection settings across tabs"
    - "Test cleanup removes all created datasources via API"
  artifacts:
    - path: "src/Frontend/tests/e2e/clone-datasource.spec.ts"
      provides: "Clone datasource E2E test suite"
      min_lines: 150
  key_links:
    - from: "clone-datasource.spec.ts"
      to: "http://127.0.0.1:5001/api/v1/datasource"
      via: "API cleanup with ?deletedBy=SanityTest"
      pattern: "deletedBy=SanityTest"
---

<objective>
Rewrite the clone-datasource E2E test to production quality using sanity.spec.ts patterns.

Purpose: The existing clone test uses a hardcoded minikube URL (172.30.22.206:32608), duplicates helpers, and lacks API-based verification/cleanup. This plan rewrites it to match the established sanity.spec.ts patterns with env-var URLs, shared helpers, API context for verification, and proper cleanup.

Output: Production-quality clone-datasource.spec.ts with 5-6 tests covering list clone, details clone, field preservation, and cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Frontend/tests/e2e/sanity.spec.ts (patterns: extractId, extractData, antTabClick, antSelect, apiContext, DATASOURCE_API constants)
@src/Frontend/tests/e2e/clone-datasource.spec.ts (existing tests to rewrite)
@src/Frontend/playwright.config.ts

<interfaces>
<!-- Key patterns from sanity.spec.ts that must be reused -->

Constants:
```typescript
const DATASOURCE_API = 'http://127.0.0.1:5001';
const FRONTEND_URL = 'http://127.0.0.1:7000';
```

Helpers (copy from sanity.spec.ts, do not import — separate test file):
```typescript
const extractId = (body: any): string | undefined => {
  return body?.Data?.ID || body?.ID || body?.data?.ID || body?.data?.id || body?.id;
};
const extractData = (body: any): any => {
  return body?.Data || body?.data || body;
};
const antTabClick = async (page: Page, tabKey: string) => { ... };
const antSelect = async (page: Page, inputId: string, optionMatcher?: string | RegExp, optionIndex = 0) => { ... };
```

Clone UI entry point in DataSourceList.tsx:
- CopyOutlined icon button in actions column
- Triggers handleClone -> modal.confirm -> navigate('/datasources/new', { state: { cloneData } })
- Clone data prepared by prepareCloneData() from helpers.ts
- Name prefix: i18n key `datasources.clone.namePrefix` = "העתק של"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite clone-datasource.spec.ts with production patterns</name>
  <files>src/Frontend/tests/e2e/clone-datasource.spec.ts</files>
  <read_first>
    - src/Frontend/tests/e2e/sanity.spec.ts (lines 1-75 for helpers and constants)
    - src/Frontend/tests/e2e/clone-datasource.spec.ts (current tests to understand coverage)
    - src/Frontend/src/pages/datasources/DataSourceList.tsx (clone button and handleClone flow)
    - src/Frontend/src/components/datasource/shared/helpers.ts (prepareCloneData function)
  </read_first>
  <action>
Rewrite `clone-datasource.spec.ts` from scratch following sanity.spec.ts patterns:

1. **Header**: Copy constants (DATASOURCE_API, FRONTEND_URL) and helpers (extractId, extractData, antTabClick, antSelect) from sanity.spec.ts. Use `APIRequestContext` for all API calls.

2. **Setup**: `beforeAll` creates apiContext. `beforeEach` sets viewport 1440x900, navigates to `/datasources`, waits for table rows.

3. **Test: CLONE-01 — Actions column has clone icon button**: Verify first row has `.anticon-copy` button visible. Hover to check tooltip shows clone text.

4. **Test: CLONE-02 — Clone from list shows modal and navigates to pre-filled form**: Click copy icon on first row. Wait for `.ant-modal-confirm`. Verify modal has clone confirmation text (Hebrew). Click primary button. Wait for URL `/datasources/new`. Verify name input contains "העתק של" prefix. Verify supplierName is populated (non-empty).

5. **Test: CLONE-03 — Clone preserves category via API verification**: Create a source datasource via API with known fields (Name, SupplierName, Category='Finance', FilePattern='*.csv', RetentionDays=30). Navigate to list. Find and click clone icon on that row. Confirm modal. On create form, verify name has prefix. Use API to verify source still exists. Cleanup: delete source via API with `?deletedBy=SanityTest`.

6. **Test: CLONE-04 — Clone from details page**: Navigate to datasource details page (click eye icon on first row). Verify `.anticon-copy` button in header. Click it. Confirm modal. Verify navigation to `/datasources/new`.

7. **Test: CLONE-05 — Cleanup API works**: Create temp datasource via API, delete it with `?deletedBy=SanityTest`, verify 404 on re-fetch.

8. **Cleanup**: `afterAll` disposes apiContext.

Key patterns to follow:
- Use `test.describe.configure({ mode: 'serial' })` since tests may share page state
- Use env-var-friendly constants (not hardcoded IPs)
- All API calls through apiContext (not fetch)
- PascalCase for API request bodies
- Wrap tests that create data in try/finally with API cleanup
- Use `page.waitForTimeout(1000)` after antTabClick for RTL rendering
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/clone-datasource.spec.ts --list 2>&1 | grep -c "test"</automated>
  </verify>
  <acceptance_criteria>
    - File uses DATASOURCE_API='http://127.0.0.1:5001' and FRONTEND_URL='http://127.0.0.1:7000' (not hardcoded minikube IP)
    - File contains extractId, extractData, antTabClick helpers
    - At least 4 test cases listed
    - All tests use apiContext for API operations
    - All tests that create datasources have cleanup with ?deletedBy=SanityTest
    - grep confirms: `grep -c "deletedBy=SanityTest" src/Frontend/tests/e2e/clone-datasource.spec.ts` returns >= 1
    - grep confirms: `grep -c "apiContext" src/Frontend/tests/e2e/clone-datasource.spec.ts` returns >= 3
  </acceptance_criteria>
  <done>Clone datasource E2E tests rewritten with production patterns: env-var URLs, API context, shared helpers, proper cleanup. At least 4 test cases covering list clone, details clone, field verification, and cleanup.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npx playwright test tests/e2e/clone-datasource.spec.ts --list` shows 4+ tests
- `grep -c "172.30" src/Frontend/tests/e2e/clone-datasource.spec.ts` returns 0 (no hardcoded IPs)
- `grep -c "deletedBy=SanityTest" src/Frontend/tests/e2e/clone-datasource.spec.ts` returns >= 1
- `grep -c "extractId\|extractData" src/Frontend/tests/e2e/clone-datasource.spec.ts` returns >= 2
</verification>

<success_criteria>
Clone E2E tests follow sanity.spec.ts patterns, use configurable URLs, have API-based cleanup, and cover the full clone workflow from both list and details pages.
</success_criteria>

<output>
After completion, create `.planning/phases/31-feature-test-coverage/31-01-SUMMARY.md`
</output>
