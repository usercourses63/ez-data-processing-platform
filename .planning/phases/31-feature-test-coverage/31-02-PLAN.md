---
phase: 31-feature-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/tests/e2e/import-from-file.spec.ts
autonomous: true
requirements:
  - TIER-2-IMPORT
must_haves:
  truths:
    - "Import button visible on list page next to Create Datasource"
    - "CSV upload triggers preview modal with data table and field checklist"
    - "Headerless CSV detected and field name editing works"
    - "Continue button navigates to pre-filled create form with inferred schema"
    - "JSON and XML files show appropriate preview formats"
  artifacts:
    - path: "src/Frontend/tests/e2e/import-from-file.spec.ts"
      provides: "Import from file E2E test suite"
      min_lines: 180
  key_links:
    - from: "import-from-file.spec.ts"
      to: "src/Frontend/test-data/import-test-with-headers.csv"
      via: "page.setInputFiles for Ant Design Upload"
      pattern: "setInputFiles"
---

<objective>
Upgrade import-from-file E2E tests to production quality with sanity.spec.ts patterns.

Purpose: The existing import test mostly works but lacks API context for verification, proper cleanup, and consistent helper usage. This plan upgrades it to match the established patterns while preserving the good test coverage already in place.

Output: Production-quality import-from-file.spec.ts with 5-6 tests covering upload triggers, CSV/JSON/XML preview, headerless detection, and form pre-fill flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Frontend/tests/e2e/sanity.spec.ts (patterns: extractId, extractData, antTabClick, apiContext)
@src/Frontend/tests/e2e/import-from-file.spec.ts (existing tests to upgrade)
@src/Frontend/src/components/datasource/import/ImportFromFileButton.tsx
@src/Frontend/src/components/datasource/import/ImportPreviewModal.tsx

<interfaces>
<!-- Import flow architecture -->

List page has ImportFromFileButton next to "Add datasource" button.
Button uses Ant Design Upload with hidden input[type="file"].
Upload triggers client-side file analysis (fileAnalyzer).
Analysis result shown in ImportPreviewModal with:
  - DataPreviewTable (CSV: table rows, JSON/XML: pre block)
  - ExtractionChecklist (field names with types)
  - HeaderlessFieldEditor (toggle + editable inputs for headerless CSV)
Continue button navigates to /datasources/new with state.importData.

Test data files available:
  - import-test-with-headers.csv (10 rows, 3 columns: name, age, email)
  - import-test-no-headers.csv (headerless CSV)
  - import-test.json (nested JSON)
  - import-test.xml (XML with nested elements)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade import-from-file.spec.ts with production patterns</name>
  <files>src/Frontend/tests/e2e/import-from-file.spec.ts</files>
  <read_first>
    - src/Frontend/tests/e2e/sanity.spec.ts (lines 1-75 for helpers)
    - src/Frontend/tests/e2e/import-from-file.spec.ts (current tests)
    - src/Frontend/src/components/datasource/import/ImportFromFileButton.tsx (upload trigger)
    - src/Frontend/src/components/datasource/import/ImportPreviewModal.tsx (modal structure)
  </read_first>
  <action>
Upgrade `import-from-file.spec.ts` to use production patterns:

1. **Header**: Add constants (DATASOURCE_API, FRONTEND_URL) and helpers (extractId, extractData, antTabClick, antSelect) matching sanity.spec.ts. Add `APIRequestContext` for API verification. Keep the existing uploadTestFile and waitForPreviewModal helpers (they are good).

2. **Setup**: Add `beforeAll` creating apiContext. Keep existing `beforeEach` with viewport 1440x900 and navigation.

3. **Test: IMPORT-01 — Import button exists on list page**: Keep existing test but use FRONTEND_URL constant instead of hardcoded URL. Verify import button with FileAddOutlined icon is visible alongside Create Datasource button.

4. **Test: IMPORT-02 — CSV with headers shows preview modal**: Upload import-test-with-headers.csv. Verify modal visible with data table (5 preview rows per D-09). Verify ExtractionChecklist shows field names (name, age, email) in `<strong>` tags. Verify row count footer ("10 שורות"). Verify Continue button visible.

5. **Test: IMPORT-03 — Headerless CSV detection**: Upload import-test-no-headers.csv. Verify toggle switch visible. Toggle if needed. Verify editable field name inputs appear. Type custom names. Verify checklist updates.

6. **Test: IMPORT-04 — JSON preview**: Upload import-test.json. Verify `<pre>` block visible with hierarchical JSON content. Verify field names in checklist.

7. **Test: IMPORT-05 — XML preview**: Upload import-test.xml. Verify `<pre>` block with XML tags. Verify checklist shows nested types.

8. **Test: IMPORT-06 — Continue navigates to pre-filled form**: Upload CSV. Click Continue. Verify URL is `/datasources/new`. Verify name input populated with filename-based suggestion. Navigate to File Settings tab (use antTabClick). Verify file type shows CSV. Navigate to Schema tab. Verify schema panel has content.

9. **Cleanup**: `afterAll` disposes apiContext.

Key upgrade points vs existing:
- Replace hardcoded `process.env.FRONTEND_URL || 'http://127.0.0.1:7000'` with constant (already OK in existing)
- Add apiContext even though import tests are mostly UI — needed for potential post-Continue datasource cleanup
- Use antTabClick helper instead of direct `page.locator('[id$="-tab-file"]').click()` for RTL compatibility
- Ensure all test assertions have descriptive messages
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/import-from-file.spec.ts --list 2>&1 | grep -c "test"</automated>
  </verify>
  <acceptance_criteria>
    - File uses FRONTEND_URL constant (not hardcoded URL outside of constant definition)
    - File contains uploadTestFile and waitForPreviewModal helpers
    - File uses antTabClick for tab navigation (not direct id$ click)
    - At least 5 test cases listed
    - grep confirms: `grep -c "antTabClick" src/Frontend/tests/e2e/import-from-file.spec.ts` returns >= 1
    - grep confirms: `grep -c "setInputFiles" src/Frontend/tests/e2e/import-from-file.spec.ts` returns >= 3
  </acceptance_criteria>
  <done>Import from file E2E tests upgraded with production patterns: shared helpers, antTabClick for RTL tabs, API context for verification, consistent assertions. 5-6 tests covering all import flows.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npx playwright test tests/e2e/import-from-file.spec.ts --list` shows 5+ tests
- `grep -c "antTabClick" src/Frontend/tests/e2e/import-from-file.spec.ts` returns >= 1
- `grep -c "FRONTEND_URL" src/Frontend/tests/e2e/import-from-file.spec.ts` returns >= 3
</verification>

<success_criteria>
Import E2E tests use RTL-safe tab navigation, cover CSV/JSON/XML preview flows, verify headerless detection, and confirm Continue button creates pre-filled form.
</success_criteria>

<output>
After completion, create `.planning/phases/31-feature-test-coverage/31-02-SUMMARY.md`
</output>
