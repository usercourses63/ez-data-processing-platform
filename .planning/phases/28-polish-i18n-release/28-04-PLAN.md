---
phase: 28-polish-i18n-release
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - release-package/docs-docusaurus/docs/user-guide/completeness.md
autonomous: true
requirements: [DOC-09]
must_haves:
  truths:
    - "Hebrew user guide has a dedicated completeness chapter with color-coded explanation"
    - "Chapter includes Mermaid state diagram for field status"
    - "Chapter references screenshot image paths"
  artifacts:
    - path: "release-package/docs-docusaurus/docs/user-guide/completeness.md"
      provides: "Hebrew completeness user guide chapter"
      min_lines: 70
      contains: "completeness"
  key_links:
    - from: "completeness.md"
      to: "static/img/screenshots/"
      via: "markdown image references"
      pattern: "screenshots/"
---

<objective>
Write the Hebrew user guide chapter for the "Completeness Checklist" feature with color-coded field explanation, Mermaid state diagram, and screenshot references.

Purpose: Users need Hebrew documentation explaining the real-time form completeness tracking system — progress rings, field borders, missing fields panel, and auto-disable behavior.
Output: One complete Hebrew markdown chapter (~600 words) following the per-chapter template.
</objective>

<execution_context>
@C:\Users\UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-polish-i18n-release/28-CONTEXT.md
@src/Frontend/src/i18n/locales/he.json
@src/Frontend/src/components/datasource/completeness/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Hebrew completeness chapter</name>
  <files>release-package/docs-docusaurus/docs/user-guide/completeness.md</files>
  <read_first>
    release-package/docs-docusaurus/docs/user-guide-he.mdx,
    src/Frontend/src/i18n/locales/he.json,
    src/Frontend/src/components/datasource/completeness/CompletenessPanel.tsx,
    src/Frontend/src/components/datasource/completeness/useCompletenessTracker.ts,
    src/Frontend/src/components/datasource/completeness/completenessConfig.ts,
    src/Frontend/src/components/datasource/completeness/completenessTypes.ts
  </read_first>
  <action>
Create `release-package/docs-docusaurus/docs/user-guide/completeness.md` following the per-chapter template (per D-13, D-14):

```markdown
– – –
title: רשימת שלמות מקור נתונים
sidebar_label: שלמות
sidebar_position: 6
– – –
```

Content structure (all Hebrew, wrapped in `<div dir="rtl">...</div>`):

1. **Overview section** (~100 words):
   - Explain the 3-tier completeness system: required (red), recommended (yellow/amber), optional (gray)
   - The completeness panel appears on the right side of both create and edit forms
   - Shows a progress ring with overall percentage and per-tab breakdown
   - Screenshot: `completeness-sidebar.png`

2. **Mermaid state diagram for field status**:
   ```mermaid
   stateDiagram-v2
       [*] --> ריק: שדה חדש
       ריק --> מלא: הזנת ערך
       מלא --> ריק: מחיקת ערך

       state ריק {
           [*] --> חובה: שדה נדרש
           [*] --> מומלץ: שדה מומלץ
           [*] --> אופציונלי: שדה אופציונלי
       }

       state מלא {
           [*] --> תקין: ערך חוקי
       }
   ```

3. **Color-coded field borders** (~120 words):
   - Explain what each border color means:

   | Color | Hebrew Name | Meaning | Example |
   |-------|-------------|---------|---------|
   | Red border | אדום - חובה | Required field, empty | Name, Schema |
   | Green border | ירוק - מלא | Field has valid value | Filled name |
   | Amber/Yellow | כתום - מומלץ | Recommended but not required | Description |
   | Gray border | אפור - אופציונלי | Optional, empty is OK | Notes |

   - Screenshot: `completeness-field-borders.png`

4. **Completeness Panel sections** (~150 words):
   - **Progress Ring**: Shows overall % (counts required + recommended fields)
   - **Per-Tab Items**: Each tab (Basic Info, Connection, Schema, etc.) shows its own completion status
   - Clicking a tab item navigates directly to that tab
   - **Missing Fields Panel**: Lists specific fields that need attention, grouped by priority
   - Screenshot: `completeness-missing-fields.png`

5. **List Page Indicators** (~80 words):
   - Each datasource in the list shows a mini progress ring
   - Color: green (>80%), amber (50-80%), red (<50%)
   - Datasources below the required threshold cannot be enabled (toggle disabled)
   - Screenshot: `completeness-list-indicator.png`

6. **Auto-Disable Behavior** (~80 words):
   - Datasources that don't meet minimum required fields are automatically disabled
   - The enable/disable toggle in the list is grayed out with a tooltip explaining why
   - Once all required fields are filled, the toggle becomes active
   - This prevents running pipelines with incomplete configurations

7. **Tips section** (~70 words):
   - Start with required fields (red borders) before recommended
   - Use the missing fields panel to quickly find what needs attention
   - The completeness percentage includes both required and recommended fields
   - Completeness recalculates in real-time as you type
   - Import from File automatically fills many fields, boosting completeness quickly

Use Hebrew translation keys from he.json for UI labels.
  </action>
  <verify>
    <automated>test -f release-package/docs-docusaurus/docs/user-guide/completeness.md && grep -c "שלמות" release-package/docs-docusaurus/docs/user-guide/completeness.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at release-package/docs-docusaurus/docs/user-guide/completeness.md
    - Contains `<div dir="rtl">` wrapper
    - Contains `mermaid` code block
    - Contains color-coded field borders table with 4 rows
    - Contains at least 4 screenshot image references
    - File is at least 70 lines long
  </acceptance_criteria>
  <done>Hebrew completeness chapter is complete with color-coded explanation, state diagram, and screenshot references</done>
</task>

</tasks>

<verification>
- File exists and contains Hebrew content
- Mermaid state diagram has valid syntax
- Color table explains all 4 border states
</verification>

<success_criteria>
- Completeness chapter is ~600 words of Hebrew content
- Includes Mermaid state diagram
- Includes color-coded borders table
- Includes 4 screenshot references
- Explains auto-disable behavior
</success_criteria>

<output>
After completion, create `.planning/phases/28-polish-i18n-release/28-04-SUMMARY.md`
</output>
