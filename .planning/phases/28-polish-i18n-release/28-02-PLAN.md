---
phase: 28-polish-i18n-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - release-package/docs-docusaurus/docs/user-guide/import-from-file.md
autonomous: true
requirements: [DOC-09]
must_haves:
  truths:
    - "Hebrew user guide has a dedicated import-from-file chapter with step-by-step workflow"
    - "Chapter includes Mermaid navigation flow diagram"
    - "Chapter references screenshot image paths for future embedding"
  artifacts:
    - path: "release-package/docs-docusaurus/docs/user-guide/import-from-file.md"
      provides: "Hebrew import-from-file user guide chapter"
      min_lines: 80
      contains: "import"
  key_links:
    - from: "import-from-file.md"
      to: "static/img/screenshots/"
      via: "markdown image references"
      pattern: "screenshots/"
---

<objective>
Write the Hebrew user guide chapter for the "Import from File" feature with full step-by-step workflow, Mermaid diagram, and screenshot references.

Purpose: Users need Hebrew documentation explaining how to upload sample files (CSV, JSON, XML, archives) to auto-create datasources with inferred schemas.
Output: One complete Hebrew markdown chapter (~800 words) following the per-chapter template.
</objective>

<execution_context>
@C:\Users\UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-polish-i18n-release/28-CONTEXT.md
@src/Frontend/src/i18n/locales/he.json
@src/Frontend/src/components/datasource/import/ImportFromFileButton.tsx
@src/Frontend/src/components/datasource/import/ImportPreviewModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Hebrew import-from-file chapter</name>
  <files>release-package/docs-docusaurus/docs/user-guide/import-from-file.md</files>
  <read_first>
    release-package/docs-docusaurus/docs/user-guide-he.mdx,
    src/Frontend/src/i18n/locales/he.json,
    src/Frontend/src/components/datasource/import/ImportFromFileButton.tsx,
    src/Frontend/src/components/datasource/import/ImportPreviewModal.tsx,
    src/Frontend/src/components/datasource/import/FileAnalysisEngine.ts,
    src/Frontend/src/components/datasource/import/ArchiveHandler.tsx
  </read_first>
  <action>
Create `release-package/docs-docusaurus/docs/user-guide/import-from-file.md` following the per-chapter template (per D-13, D-14):

```markdown
– – –
title: ייבוא מקור נתונים מקובץ
sidebar_label: ייבוא מקובץ
sidebar_position: 4
– – –
```

Content structure (all Hebrew, wrapped in `<div dir="rtl">...</div>`):

1. **Overview section** (~100 words):
   - Explain the import feature: upload a sample file and auto-create a datasource
   - Supported formats: CSV, JSON, XML, Excel
   - Archive support: ZIP, TAR.GZ, 7Z, RAR (including password-protected)
   - Include overview screenshot reference: `![Overview](../../static/img/screenshots/import-button.png)`

2. **Mermaid navigation flow diagram**:
   ```mermaid
   flowchart TD
       A[רשימת מקורות נתונים] -->|לחיצה על 'מקור חדש מקובץ'| B[בחירת קובץ]
       B -->|קובץ רגיל| C[ניתוח וזיהוי שדות]
       B -->|ארכיון| D[בחירת קובץ מתוך הארכיון]
       D -->|ארכיון מוצפן| E[הזנת סיסמה]
       E --> D
       D --> C
       C --> F[תצוגה מקדימה של הנתונים]
       F --> G[עריכת סכמה]
       G --> H[יצירת מקור נתונים]
       H --> I[מעבר לעריכה]
   ```

3. **Step-by-step workflow** (~400 words):
   - Step 1: Navigate to datasource list page, click the teal "מקור נתונים חדש מקובץ" button
     - Screenshot: `import-button.png`
   - Step 2: Select a file from your computer (or drag and drop)
     - For archive files: the system opens an archive browser showing contained files
     - Screenshot: `import-archive-selector.png`
   - Step 3: For encrypted archives, enter the password when prompted
     - Screenshot: `import-password-input.png`
   - Step 4: Review the data preview table
     - System detects column types (integer, number, boolean, date, string)
     - System applies smart patterns (email, phone, date formats)
     - For CSV without headers: synthetic names generated (field_1, field_2...)
     - Screenshot: `import-preview-csv.png`
   - Step 5: Review and edit the auto-generated JSON Schema in the Monaco editor
     - Schema follows JSON Schema 2020-12 standard
   - Step 6: Click "Continue" to auto-create the datasource
     - System pre-fills: name (from filename), schema, format settings
     - For archives: archive-specific settings (format, password, inner file path) are set
   - Step 7: Complete remaining fields in the edit form
     - Connection settings, scheduling, output destinations
     - Screenshot: `import-edit-archive-settings.png`

4. **Supported formats table**:
   | Format | Extension | Notes (Hebrew) |
   |--------|-----------|----------------|
   | CSV | .csv | With/without headers, auto-detect delimiter |
   | JSON | .json | Objects and arrays |
   | XML | .xml | Flat and nested structures |
   | Excel | .xlsx, .xls | First sheet processed |

5. **Archive formats table**:
   | Format | Extension | Encryption |
   |--------|-----------|------------|
   | ZIP | .zip | Supported |
   | TAR.GZ | .tar.gz, .tgz | No |
   | 7-Zip | .7z | Supported |
   | RAR | .rar | Supported |

6. **Tips section** (~100 words):
   - Use a representative sample file (first 100 rows is enough)
   - For CSV: ensure consistent delimiter across all rows
   - Review auto-detected types carefully — dates may be detected as strings
   - Archive files larger than 100MB may take a few seconds to process
   - The inferred schema can always be edited later in the Schema tab

Use Hebrew translation keys from he.json for button labels and UI text to maintain consistency (e.g., the button text should match what the user sees in the app).
  </action>
  <verify>
    <automated>test -f release-package/docs-docusaurus/docs/user-guide/import-from-file.md && grep -c "ייבוא" release-package/docs-docusaurus/docs/user-guide/import-from-file.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at release-package/docs-docusaurus/docs/user-guide/import-from-file.md
    - Contains `<div dir="rtl">` wrapper
    - Contains `mermaid` code block with Hebrew labels
    - Contains at least 4 screenshot image references (import-button, import-preview-csv, etc.)
    - Contains step-by-step numbered list with at least 5 steps
    - Contains supported formats table
    - File is at least 80 lines long
    - Hebrew word count is substantial (grep for Hebrew characters returns >50 matches)
  </acceptance_criteria>
  <done>Hebrew import-from-file chapter is complete with workflow steps, Mermaid diagram, and screenshot references</done>
</task>

</tasks>

<verification>
- File exists and contains Hebrew content
- Mermaid diagram has valid syntax
- Screenshot references point to correct filenames
</verification>

<success_criteria>
- Import-from-file chapter is ~800 words of Hebrew content
- Includes Mermaid navigation diagram
- Includes 5 screenshot references
- Follows per-chapter template structure
</success_criteria>

<output>
After completion, create `.planning/phases/28-polish-i18n-release/28-02-SUMMARY.md`
</output>
