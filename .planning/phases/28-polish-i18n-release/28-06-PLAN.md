---
phase: 28-polish-i18n-release
plan: 06
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - release-package/docs-docusaurus/docs/admin.md
  - release-package/docs-docusaurus/docs/changelog.md
  - release-package/docs-docusaurus/docs/release-notes.md
  - release-package/docs-docusaurus/docs/index.md
  - release-package/docs-docusaurus/docusaurus.config.js
autonomous: true
requirements: [DOC-10]

must_haves:
  truths:
    - "Changelog has v0.4.0 entry covering all 3 new features"
    - "Release notes have v0.4.0 section with feature summaries"
    - "Admin guide updated with v0.4.0 operational information"
    - "Docusaurus config version bumped to v0.4.0"
  artifacts:
    - path: "release-package/docs-docusaurus/docs/changelog.md"
      provides: "v0.4.0 changelog entry"
      contains: "0.4.0"
    - path: "release-package/docs-docusaurus/docs/release-notes.md"
      provides: "v0.4.0 release notes"
      contains: "0.4.0"
    - path: "release-package/docs-docusaurus/docusaurus.config.js"
      provides: "Updated version string"
      contains: "v0.4.0"
  key_links:
    - from: "index.md"
      to: "release-notes"
      via: "link to v0.4.0 release"
      pattern: "release-notes"
---

<objective>
Update the admin guide, changelog, release notes, landing page, and Docusaurus config for v0.4.0 release.

Purpose: The documentation site still shows v0.2.0/v0.3.0 branding and lacks entries for the 3 new features. Per D-16 (scope includes changelog, component docs, admin guide) and D-12 (total redesign).
Output: Updated admin guide, changelog, release notes, landing page, and config all reflecting v0.4.0.
</objective>

<execution_context>
@C:\Users\UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-polish-i18n-release/28-CONTEXT.md
@release-package/docs-docusaurus/docs/changelog.md
@release-package/docs-docusaurus/docs/release-notes.md
@release-package/docs-docusaurus/docs/admin.md
@release-package/docs-docusaurus/docs/index.md
@release-package/docs-docusaurus/docusaurus.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v0.4.0 changelog and release notes</name>
  <files>
    release-package/docs-docusaurus/docs/changelog.md,
    release-package/docs-docusaurus/docs/release-notes.md
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/changelog.md,
    release-package/docs-docusaurus/docs/release-notes.md
  </read_first>
  <action>
1. Add v0.4.0 entry to changelog.md (ABOVE the v0.3.0 entry, Keep a Changelog format):

```markdown
## [0.4.0] - 2026-03-26

### Added

#### Clone Datasource
- Clone from list page actions menu or edit page header button
- Pre-fills all form fields including schema, connection, archive settings, output destinations
- Cloned name prefixed with "Copy of" (Hebrew: "העתק של")
- Output destinations get new unique IDs; schedule disabled by default
- Archive settings (format, password, inner file path) preserved in clone

#### Import from File
- Upload CSV, JSON, XML, or Excel sample files to auto-create datasources
- Archive support: ZIP, TAR.GZ, 7Z, RAR (including password-protected via SharpCompress backend)
- Client-side file analysis with type detection (integer, number, boolean, date, string)
- Smart constraint suggestions (email format, phone patterns, date formats) via schemaAutoSuggest
- Preview table shows parsed data before applying to form
- Headerless CSV generates synthetic field names (field_1, field_2, ... field_N)
- Auto-create datasource on "Continue" (not just pre-fill)
- Backend CsvToJsonConverter maps headerless CSV columns by schema field position order

#### Completeness Checklist
- Real-time completeness percentage on create and edit forms
- Per-tab completion status with color-coded indicators (red=required, green=filled, amber=recommended, gray=optional)
- Clickable incomplete items navigate directly to the corresponding tab
- Missing fields panel shows which fields need attention
- Mini progress ring in datasource list for at-a-glance status
- Auto-disable toggle for datasources below required completeness threshold
- AJV schema validation for field-level completeness checking

#### Documentation
- Hebrew user guide completely refactored into workflow-centric chapters
- Automated Playwright screenshot pipeline for documentation
- Mermaid diagram support added to Docusaurus
- Per-feature Hebrew chapters: Import from File, Clone, Completeness
```

2. Add v0.4.0 section to release-notes.md (ABOVE v0.3.0):

```markdown
## v0.4.0 - Datasource Productivity Features (March 2026)

**Status:** Production Release
**Type:** Feature Release - Clone, Import from File, Completeness Checklist

### Highlights

This release delivers three productivity features for datasource management:
**Clone Datasource** for quick duplication, **Import from File** for auto-creating
datasources from sample data files, and **Completeness Checklist** for real-time
form guidance.

### Clone Datasource
- Duplicate any datasource from the list page or edit page
- All settings preserved: schema, connection, archive settings, output destinations
- Schedule disabled by default to prevent accidental pipeline execution

### Import from File
- Upload CSV/JSON/XML/Excel files to auto-create datasources
- Archive support (ZIP, TAR.GZ, 7Z, RAR) including encrypted archives
- Smart schema inference with type detection and pattern matching
- Data preview before applying to form

### Completeness Checklist
- Real-time progress tracking with color-coded field borders
- Per-tab completion status in sidebar panel
- Auto-disable for incomplete datasources
- Missing fields navigation for quick form completion

### Migration Notes
- No database migration required (frontend-only features)
- Backend archive API endpoints added (SharpCompress): `/api/v1/archive/analyze`, `/api/v1/archive/extract`
- nginx `client_max_body_size` should be set to at least 100MB for archive uploads
- Frontend image must be rebuilt (new components for clone, import, completeness)

### Known Limitations
- Excel import processes only the first sheet
- Archive files >100MB may experience slower processing
- Completeness percentage includes recommended fields (not just required)
```
  </action>
  <verify>
    <automated>grep -c "0.4.0" release-package/docs-docusaurus/docs/changelog.md && grep -c "0.4.0" release-package/docs-docusaurus/docs/release-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - changelog.md contains "## [0.4.0]" section above "## [0.3.0]"
    - changelog.md mentions Clone, Import, Completeness under v0.4.0
    - release-notes.md contains "## v0.4.0" section above "## v0.3.0"
    - release-notes.md has Migration Notes and Known Limitations sections
  </acceptance_criteria>
  <done>Changelog and release notes have comprehensive v0.4.0 entries</done>
</task>

<task type="auto">
  <name>Task 2: Update admin guide, landing page, and config version</name>
  <files>
    release-package/docs-docusaurus/docs/admin.md,
    release-package/docs-docusaurus/docs/index.md,
    release-package/docs-docusaurus/docusaurus.config.js
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/admin.md,
    release-package/docs-docusaurus/docs/index.md,
    release-package/docs-docusaurus/docusaurus.config.js
  </read_first>
  <action>
1. Update admin.md:
   - Change title version: "Administrator Guide v0.3.0" -> "Administrator Guide v0.4.0"
   - Change Version line: "0.3.0" -> "0.4.0"
   - Add new section after "Device Health Monitoring" (before Backup & Recovery):

   ```markdown
   ## Datasource Productivity Features (v0.4.0)

   ### Archive API Endpoints
   The FileProcessor service exposes archive handling endpoints via SharpCompress:
   - `POST /api/v1/archive/analyze` — Analyze archive contents (returns file list, sizes, encrypted flag)
   - `POST /api/v1/archive/extract` — Extract specific file from archive (with optional password)

   Supported formats: ZIP, TAR.GZ (.tgz), 7-Zip (.7z), RAR (.rar)
   Encrypted archives: ZIP (AES/ZipCrypto), 7Z (AES-256), RAR (AES-128/256)

   ### Archive Upload Size
   Configure nginx `client_max_body_size` for archive uploads:
   ```nginx
   # k8s/infrastructure/nginx or frontend deployment
   client_max_body_size 100m;
   ```

   ### Completeness Auto-Disable
   Datasources that do not meet minimum required field completion are automatically disabled.
   The enable/disable toggle is grayed out in the list page until all required fields are filled.
   This prevents running pipelines with incomplete configurations.

   Required fields include: Name, Data Source Type, Connection settings, Schema.
   ```

   - Update the Table of Contents to include the new section

2. Update index.md (landing page):
   - Update the version reference from v0.2.0/v0.3.0 to v0.4.0
   - Add a "What's New in v0.4.0" section highlighting the 3 features
   - Add links to the new user guide chapters

3. Update docusaurus.config.js:
   - Change title: `'EZ Platform v0.2.0'` -> `'EZ Platform v0.4.0'`
  </action>
  <verify>
    <automated>grep "v0.4.0" release-package/docs-docusaurus/docusaurus.config.js && grep "v0.4.0" release-package/docs-docusaurus/docs/admin.md | head -2</automated>
  </verify>
  <acceptance_criteria>
    - docusaurus.config.js title contains "v0.4.0"
    - admin.md title contains "v0.4.0"
    - admin.md has "Archive API Endpoints" section
    - admin.md has "Completeness Auto-Disable" section
    - index.md references v0.4.0 features
  </acceptance_criteria>
  <done>Admin guide, landing page, and config all updated to v0.4.0</done>
</task>

</tasks>

<verification>
- `grep -r "v0.4.0" release-package/docs-docusaurus/docs/ release-package/docs-docusaurus/docusaurus.config.js | wc -l` returns >= 5
- `npm run build` succeeds
</verification>

<success_criteria>
- Changelog has Keep a Changelog formatted v0.4.0 entry
- Release notes have feature summaries + migration notes
- Admin guide has archive API and completeness sections
- Docusaurus branding is v0.4.0
</success_criteria>

<output>
After completion, create `.planning/phases/28-polish-i18n-release/28-06-SUMMARY.md`
</output>
