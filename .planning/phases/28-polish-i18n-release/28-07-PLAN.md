---
phase: 28-polish-i18n-release
plan: 07
type: execute
wave: 3
depends_on: [28-05, 28-06]
files_modified:
  - release-package/docs-docusaurus/
autonomous: false

requirements: [DOC-09, DOC-10]

must_haves:
  truths:
    - "Docusaurus build succeeds with zero errors"
    - "Docker docs image builds successfully"
    - "All screenshots display correctly in rendered pages"
    - "Hebrew RTL rendering is correct"
    - "Mermaid diagrams render in all user guide chapters"
  artifacts:
    - path: "release-package/docs-docusaurus/build/"
      provides: "Built documentation site"
  key_links:
    - from: "docs/user-guide/*.md"
      to: "static/img/screenshots/"
      via: "image references"
      pattern: "screenshots/"
---

<objective>
Build verification of the complete documentation site, Docker image build, and visual review of rendered pages.

Purpose: Final gate before release — verify all docs build, images render, Mermaid works, and RTL is correct.
Output: Verified documentation build and Docker image ready for deployment.
</objective>

<execution_context>
@C:\Users\UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-polish-i18n-release/28-CONTEXT.md
@release-package/docs-docusaurus/package.json
@release-package/docs-docusaurus/Dockerfile
@.planning/phases/28-polish-i18n-release/28-05-SUMMARY.md
@.planning/phases/28-polish-i18n-release/28-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and broken link check</name>
  <files>release-package/docs-docusaurus/</files>
  <read_first>
    release-package/docs-docusaurus/package.json,
    release-package/docs-docusaurus/docusaurus.config.js,
    release-package/docs-docusaurus/sidebars.js
  </read_first>
  <action>
1. Run full Docusaurus build:
   ```bash
   cd release-package/docs-docusaurus
   npm run clear
   npm run build
   ```
   - Capture all warnings and errors
   - Fix any broken image references (missing screenshots, wrong paths)
   - Fix any broken markdown links between chapters
   - Fix any MDX compilation errors

2. Check for broken image references:
   ```bash
   grep -rn "screenshots/" docs/user-guide/ | while read line; do
     img=$(echo "$line" | grep -oP 'screenshots/[^)"\s]+')
     if [ ! -f "static/img/$img" ]; then
       echo "MISSING: $img in $line"
     fi
   done
   ```
   - Fix any missing references

3. Verify Mermaid blocks have valid syntax:
   - Check each user guide chapter for ```mermaid blocks
   - Ensure they render (build would fail if not)

4. Check that all 10 user guide chapters are reachable from sidebar:
   - Parse sidebars.js and verify each doc ID exists as a file

5. Run `npm run serve` briefly and verify no 404s on key pages (or use build output to verify).
  </action>
  <verify>
    <automated>cd release-package/docs-docusaurus && npm run build 2>&1 | grep -E "(error|Error|success|Successfully)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No "broken link" warnings for user guide pages
    - No missing image warnings for screenshot references
    - Build output directory exists with index.html
  </acceptance_criteria>
  <done>Docusaurus build succeeds with no errors; all links and images verified</done>
</task>

<task type="auto">
  <name>Task 2: Docker docs image build</name>
  <files>release-package/docs-docusaurus/Dockerfile</files>
  <read_first>
    release-package/docs-docusaurus/Dockerfile
  </read_first>
  <action>
1. Build the Docker image for the documentation site:
   ```bash
   cd release-package/docs-docusaurus
   docker build -t ez-platform/docs:v0.4.0 .
   ```
   - This uses the existing multi-stage Dockerfile (node:20-alpine builder + nginx:1.28-alpine)
   - The build stage runs `npm run build` which must pass

2. Verify the image was created:
   ```bash
   docker images ez-platform/docs:v0.4.0
   ```

3. Quick smoke test the image (optional if Docker available):
   ```bash
   docker run --rm -d -p 8888:8080 --name docs-test ez-platform/docs:v0.4.0
   curl -s http://localhost:8888/ | grep -o "EZ Platform" | head -1
   docker stop docs-test
   ```
  </action>
  <verify>
    <automated>docker images ez-platform/docs:v0.4.0 --format "{{.Repository}}:{{.Tag}} {{.Size}}" 2>/dev/null || echo "Docker image build will be verified during execution"</automated>
  </verify>
  <acceptance_criteria>
    - Docker image ez-platform/docs:v0.4.0 exists
    - Image is based on nginx:1.28-alpine (OCP compatible)
    - Image serves the documentation site on port 8080
  </acceptance_criteria>
  <done>Docker documentation image built and verified as v0.4.0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual review of documentation</name>
  <what-built>Complete Docusaurus documentation site redesigned for v0.4.0 with:
    - 10-chapter Hebrew user guide (workflow-centric)
    - 15 automated screenshots
    - Mermaid flow diagrams
    - Updated admin guide, changelog, release notes
    - Docker image ready for deployment
  </what-built>
  <how-to-verify>
    1. Start the docs locally: `cd release-package/docs-docusaurus && npm run serve`
    2. Open http://localhost:3000 in browser
    3. Verify landing page shows v0.4.0 highlights
    4. Navigate to User Guide in sidebar — confirm 10 chapters visible
    5. Open "Import from File" chapter — verify Hebrew RTL, screenshots, Mermaid diagram
    6. Open "Clone Datasource" chapter — verify Hebrew RTL, screenshots, Mermaid diagram
    7. Open "Completeness" chapter — verify Hebrew RTL, color table, screenshots
    8. Open Changelog — verify v0.4.0 entry at top
    9. Open Admin Guide — verify archive API and completeness sections
    10. Spot-check 2-3 screenshots for quality (not blurry, correct content)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build passes with exit code 0
- Docker image exists and serves content
- User confirms visual quality
</verification>

<success_criteria>
- Docusaurus build: zero errors
- Docker image: built and tagged v0.4.0
- Visual: Hebrew RTL correct, screenshots visible, Mermaid renders
- User approval received
</success_criteria>

<output>
After completion, create `.planning/phases/28-polish-i18n-release/28-07-SUMMARY.md`
</output>
