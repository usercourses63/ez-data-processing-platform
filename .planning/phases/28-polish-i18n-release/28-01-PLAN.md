---
phase: 28-polish-i18n-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-package/docs-docusaurus/scripts/capture-screenshots.ts
  - release-package/docs-docusaurus/scripts/tsconfig.json
  - release-package/docs-docusaurus/package.json
  - release-package/docs-docusaurus/static/img/screenshots/
autonomous: true
requirements: [DOC-09, DOC-10]

must_haves:
  truths:
    - "Playwright script captures 15 screenshots of the running frontend at 1280x800"
    - "Screenshots are saved to static/img/screenshots/ with descriptive filenames"
    - "Script is re-runnable and idempotent (overwrites existing screenshots)"
  artifacts:
    - path: "release-package/docs-docusaurus/scripts/capture-screenshots.ts"
      provides: "Automated screenshot capture pipeline"
      min_lines: 100
    - path: "release-package/docs-docusaurus/static/img/screenshots/"
      provides: "15 PNG screenshots for documentation"
  key_links:
    - from: "capture-screenshots.ts"
      to: "http://localhost:7000"
      via: "Playwright browser automation"
      pattern: "page\\.goto"
---

<objective>
Build an automated Playwright screenshot pipeline and capture all 15 screenshots needed for the Hebrew user guide and documentation pages.

Purpose: Documentation redesign requires annotated screenshots of clone, import, and completeness features. An automated pipeline ensures screenshots are reproducible and consistent at 1280x800 resolution.
Output: 15 PNG screenshots in static/img/screenshots/ plus the capture script for future re-runs.
</objective>

<execution_context>
@C:\Users\UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-polish-i18n-release/28-CONTEXT.md
@release-package/docs-docusaurus/package.json
@src/Frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright screenshot capture script</name>
  <files>
    release-package/docs-docusaurus/scripts/capture-screenshots.ts,
    release-package/docs-docusaurus/scripts/tsconfig.json,
    release-package/docs-docusaurus/package.json
  </files>
  <read_first>
    release-package/docs-docusaurus/package.json,
    release-package/docs-docusaurus/scripts/build-and-deploy.sh,
    src/Frontend/playwright.config.ts,
    src/Frontend/tests/e2e/clone-datasource.spec.ts,
    src/Frontend/tests/e2e/import-from-file.spec.ts,
    src/Frontend/tests/e2e/completeness-checklist.spec.ts,
    src/Frontend/src/pages/datasources/DataSourceList.tsx,
    src/Frontend/src/pages/datasources/DataSourceFormEnhanced.tsx
  </read_first>
  <action>
1. Add devDependencies to release-package/docs-docusaurus/package.json:
   - `"playwright": "^1.42.0"` (just the library, not @playwright/test — this is a script, not a test suite)
   - `"tsx": "^4.7.0"` for running TypeScript scripts directly
   - Add script: `"capture-screenshots": "npx tsx scripts/capture-screenshots.ts"`

2. Create release-package/docs-docusaurus/scripts/tsconfig.json:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "ESNext",
       "moduleResolution": "bundler",
       "esModuleInterop": true,
       "strict": true
     }
   }
   ```

3. Create release-package/docs-docusaurus/scripts/capture-screenshots.ts:
   - Import `chromium` from `playwright`
   - Set BASE_URL from env var `SCREENSHOT_URL` or default `http://localhost:7000`
   - Set OUTPUT_DIR to `../static/img/screenshots/`
   - Set viewport to 1280x800
   - Create browser with Hebrew locale (`Accept-Language: he`)
   - Define screenshot list as array of objects: `{ name, url, selector?, waitFor?, actions? }`
   - Implement these 15 screenshots:

   **Clone Datasource (4):**
   - `clone-button-list.png`: Navigate to `/datasources`, wait for table, capture the actions column area showing clone icon. Use `page.locator('.ant-table-row').first()` to find a row, then screenshot the actions cell.
   - `clone-confirm-dialog.png`: Click clone button on first row, wait for `.ant-modal` to appear, screenshot the modal.
   - `clone-form-prefilled.png`: Confirm the clone dialog, wait for navigation to create form, wait for form to load, screenshot full page showing pre-filled fields.
   - `clone-saved-list.png`: Navigate back to `/datasources`, screenshot the list showing both original and cloned datasource.

   **Import from File (5):**
   - `import-button.png`: Navigate to `/datasources`, screenshot the header area showing the teal "New Datasource from File" button. Use `page.locator('[data-testid="import-from-file-btn"]')` or find by button text.
   - `import-archive-selector.png`: Click the import button, upload a test archive file (use `src/Frontend/test-data/test-archive.zip` if available, or create a simple CSV and use that), capture the file type selection area.
   - `import-password-input.png`: If archive flow available, capture password prompt. Otherwise, skip and capture the upload dialog instead.
   - `import-preview-csv.png`: Upload a CSV file, wait for the preview modal to appear with the data table, screenshot the modal.
   - `import-edit-archive-settings.png`: After import creates a datasource, navigate to its edit page, screenshot showing the form with schema populated.

   **Completeness Checklist (4):**
   - `completeness-sidebar.png`: Navigate to `/datasources/create`, wait for the completeness panel to render, screenshot the sidebar area showing progress ring and tab items.
   - `completeness-field-borders.png`: Fill in some fields but leave others empty, screenshot a section showing mixed green/red/gray field borders.
   - `completeness-missing-fields.png`: Scroll to missing fields section in the completeness panel, screenshot that area.
   - `completeness-list-indicator.png`: Navigate to `/datasources`, screenshot showing mini progress rings in the list table.

   **General (2):**
   - `datasource-list-overview.png`: Navigate to `/datasources`, full page screenshot showing all new UI elements (clone, import button, completeness rings).
   - `datasource-edit-overview.png`: Navigate to edit an existing datasource, full page screenshot showing completeness sidebar.

   For each screenshot:
   - `await page.waitForLoadState('networkidle')` before capture
   - Use `page.screenshot({ path: outputPath, fullPage: false })` for element captures
   - Use `page.screenshot({ path: outputPath, fullPage: true })` for full page captures
   - Add 500ms delay after navigation for Ant Design animations to settle
   - Wrap each in try/catch to continue on failure (log which ones failed)

4. At the end, print summary: "Captured X/15 screenshots successfully" with list of failures if any.
  </action>
  <verify>
    <automated>cd release-package/docs-docusaurus && npm install && npx tsx scripts/capture-screenshots.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File release-package/docs-docusaurus/scripts/capture-screenshots.ts exists and is >100 lines
    - File release-package/docs-docusaurus/scripts/tsconfig.json exists
    - package.json has "capture-screenshots" script
    - package.json has playwright and tsx in devDependencies
    - At least 10 of 15 screenshots exist as PNG files in static/img/screenshots/
    - Each screenshot is >10KB (not empty/corrupt)
  </acceptance_criteria>
  <done>Playwright screenshot script captures 15 screenshots from the running frontend and saves them to static/img/screenshots/</done>
</task>

<task type="auto">
  <name>Task 2: Install Mermaid plugin for Docusaurus</name>
  <files>
    release-package/docs-docusaurus/package.json,
    release-package/docs-docusaurus/docusaurus.config.js
  </files>
  <read_first>
    release-package/docs-docusaurus/package.json,
    release-package/docs-docusaurus/docusaurus.config.js
  </read_first>
  <action>
1. Add Mermaid dependencies to release-package/docs-docusaurus/package.json:
   - `"@docusaurus/theme-mermaid": "^3.1.0"` in dependencies

2. Update release-package/docs-docusaurus/docusaurus.config.js:
   - Add `markdown: { mermaid: true }` at the top level of config (after `onBrokenMarkdownLinks`)
   - Add `'@docusaurus/theme-mermaid'` to the `themes` array (alongside the search plugin)

3. Verify the build still works after adding Mermaid.
  </action>
  <verify>
    <automated>cd release-package/docs-docusaurus && npm install && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@docusaurus/theme-mermaid"
    - docusaurus.config.js contains "mermaid: true"
    - docusaurus.config.js themes array contains "@docusaurus/theme-mermaid"
    - `npm run build` succeeds with exit code 0
  </acceptance_criteria>
  <done>Mermaid diagrams render in Docusaurus markdown files; build succeeds</done>
</task>

</tasks>

<verification>
- `ls -la release-package/docs-docusaurus/static/img/screenshots/*.png | wc -l` returns >= 10
- `npm run build` in docs-docusaurus succeeds
- Mermaid syntax in .md files renders as diagrams
</verification>

<success_criteria>
- 15 screenshots captured and saved as PNG files
- Mermaid plugin installed and configured
- Docusaurus build passes with new dependencies
- Screenshot capture script is re-runnable
</success_criteria>

<output>
After completion, create `.planning/phases/28-polish-i18n-release/28-01-SUMMARY.md`
</output>
