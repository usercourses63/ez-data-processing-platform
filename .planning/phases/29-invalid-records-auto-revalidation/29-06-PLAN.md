---
phase: 29-invalid-records-auto-revalidation
plan: 06
type: execute
wave: 4
depends_on: ["29-03", "29-04", "29-05"]
files_modified:
  - src/Frontend/tests/e2e/revalidation.spec.ts
autonomous: true
requirements:
  - REVAL-10

must_haves:
  truths:
    - "E2E test creates a datasource with strict schema, imports data that creates violations, relaxes schema, and verifies auto-resolve"
    - "Test covers Revalidate All button click and confirmation popover"
    - "Test covers YAML download button functionality"
    - "Test covers schema editor link fix in correction dialog"
  artifacts:
    - path: "src/Frontend/tests/e2e/revalidation.spec.ts"
      provides: "Playwright E2E test for auto-revalidation feature"
      contains: "revalidation"
  key_links:
    - from: "src/Frontend/tests/e2e/revalidation.spec.ts"
      to: "http://localhost:7000"
      via: "Playwright browser automation"
      pattern: "page.goto"
---

<objective>
Create Playwright E2E test covering the auto-revalidation workflow: strict schema -> violations -> relax schema -> auto-resolve.

Purpose: Per success criteria #10, an E2E test must verify the full revalidation flow. This test validates the feature works end-to-end through the UI.

Output: Playwright test file with full revalidation workflow test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-invalid-records-auto-revalidation/29-CONTEXT.md
@.planning/phases/29-invalid-records-auto-revalidation/29-04-SUMMARY.md
@.planning/phases/29-invalid-records-auto-revalidation/29-05-SUMMARY.md

@src/Frontend/playwright.config.ts
@src/Frontend/tests/e2e/sanity.spec.ts

<interfaces>
Playwright config baseURL: http://localhost:7000
API URLs:
- POST /api/v1/invalid-records/revalidate-all
- GET /api/v1/invalid-records/revalidate-count
- GET /api/v1/invalid-records/statistics
- PUT /api/v1/datasources/{id} (schema update)

Frontend pages:
- /datasources — datasource list
- /datasources/{id}/edit — datasource edit with schema tab
- /invalid-records — invalid records management
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create revalidation E2E test</name>
  <files>
    src/Frontend/tests/e2e/revalidation.spec.ts
  </files>
  <read_first>
    src/Frontend/tests/e2e/sanity.spec.ts,
    src/Frontend/playwright.config.ts,
    src/Frontend/tests/e2e/datasource.spec.ts
  </read_first>
  <action>
    Create `src/Frontend/tests/e2e/revalidation.spec.ts` with the following tests:

    1. **Test: Revalidate All button on invalid records page**
       - Navigate to /invalid-records
       - Verify the "אמת מחדש הכל" button is visible in the page header
       - Click the button, verify confirmation popover appears
       - Click cancel to dismiss
       - Verify the button still exists (not removed after cancel)

    2. **Test: YAML download button on schema tab**
       - Navigate to an existing datasource edit page (use first datasource from list or API)
       - Click on the Schema tab (use `[data-node-key="schema"]` selector)
       - Verify "הורד YAML" button is visible
       - Click the button and verify a download was triggered (listen for download event)
       - Verify downloaded file has .yaml extension

    3. **Test: Schema editor link in correction dialog (per D-21/D-22)**
       - Navigate to /invalid-records
       - If records exist, click on one to open the EditRecordModal
       - Verify the schema editor link is present
       - Click it and verify navigation to /datasources/{id}/edit with schema tab active
       - If no records exist, skip gracefully with test.skip()

    4. **Test: Revalidate All button on Schema tab (per D-08 location 2)**
       - Navigate to a datasource edit page that has invalid records (or mock via API setup)
       - Click Schema tab
       - If "אמת מחדש הכל" button is visible (depends on invalid record count), click it
       - Verify confirmation popover shows record count
       - This test may need to be conditional on having invalid records

    5. **Test: Per-datasource TTL field in Basic Info tab**
       - Navigate to datasource create page
       - Verify the TTL field exists (search for InputNumber with placeholder "4")
       - Enter a value (e.g., 7)
       - Verify the value is accepted

    NOTE: These tests should work against a running frontend (localhost:7000) with port-forwarded backend services. Use `test.describe('Revalidation Feature', () => { ... })` grouping. Add `test.beforeEach` to navigate to the base page. Use Hebrew selectors since the app defaults to Hebrew.

    Use existing test patterns from sanity.spec.ts for page navigation and element selection. Be robust with waits (use `waitForSelector` or `expect(locator).toBeVisible()`).

    Mark tests that depend on existing data with `.skip` condition if data doesn't exist.
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/revalidation.spec.ts --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "revalidat" src/Frontend/tests/e2e/revalidation.spec.ts returns at least 3
    - grep -c "test\\(" src/Frontend/tests/e2e/revalidation.spec.ts returns at least 4
    - grep -c "YAML\|yaml" src/Frontend/tests/e2e/revalidation.spec.ts returns at least 1
    - grep -c "schema.*tab\|Schema.*tab" src/Frontend/tests/e2e/revalidation.spec.ts returns at least 1
    - npx playwright test --list shows the test file with test names
  </acceptance_criteria>
  <done>Playwright test file exists with 4-5 tests covering: Revalidate All button (both locations), YAML download, schema editor link fix, TTL field. Tests are listable by Playwright.</done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/e2e/revalidation.spec.ts --list` shows all tests
2. Test file covers all 5 test scenarios
3. Test uses Hebrew selectors matching actual UI text
</verification>

<success_criteria>
- E2E test file exists at src/Frontend/tests/e2e/revalidation.spec.ts
- Covers: Revalidate All button (2 locations), YAML download, schema link fix, TTL field
- Tests are structured to handle missing data gracefully (skip conditions)
- Playwright can list all tests without errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-invalid-records-auto-revalidation/29-06-SUMMARY.md`
</output>
