---
phase: 29-invalid-records-auto-revalidation
plan: 07
type: execute
wave: 4
depends_on: ["29-04", "29-05"]
files_modified:
  - release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md
  - release-package/docs-docusaurus/docs/user-guide/create-datasource.md
  - release-package/docs-docusaurus/docs/user-guide/troubleshooting.md
  - release-package/docs-docusaurus/sidebars.js
  - docs/user-guide/USER-GUIDE-HE.md
autonomous: true
requirements:
  - REVAL-11
  - REVAL-12
  - REVAL-23
  - REVAL-24
  - REVAL-25
  - REVAL-26

must_haves:
  truths:
    - "Hebrew user guide chapter for auto-revalidation exists with Mermaid flow diagram"
    - "Create-datasource chapter updated with TTL field documentation"
    - "Troubleshooting chapter updated with revalidation FAQ"
    - "Docusaurus sidebar includes the new chapter"
  artifacts:
    - path: "release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md"
      provides: "Hebrew user guide chapter for auto-revalidation"
      contains: "אימות מחדש אוטומטי"
    - path: "release-package/docs-docusaurus/sidebars.js"
      provides: "Updated sidebar with auto-revalidation chapter"
      contains: "auto-revalidation"
  key_links:
    - from: "release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md"
      to: "Mermaid diagram"
      via: "```mermaid code block"
      pattern: "mermaid"
---

<objective>
Create Hebrew user guide chapter for auto-revalidation and update Docusaurus documentation (changelog, release notes, admin guide).

Purpose: Per D-23/D-24/D-25/D-26, comprehensive documentation is required. The Hebrew user guide follows the same per-chapter template used for clone/import/completeness chapters in Phase 28.

Output: New auto-revalidation chapter, updated existing chapters (create-datasource, troubleshooting), updated sidebar and changelog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-invalid-records-auto-revalidation/29-CONTEXT.md
@.planning/phases/28-polish-i18n-release/28-05-SUMMARY.md

@release-package/docs-docusaurus/docs/user-guide/index.md
@release-package/docs-docusaurus/docs/user-guide/create-datasource.md
@release-package/docs-docusaurus/docs/user-guide/troubleshooting.md
@release-package/docs-docusaurus/sidebars.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hebrew auto-revalidation user guide chapter</name>
  <files>
    release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/user-guide/index.md,
    release-package/docs-docusaurus/docs/user-guide/clone-datasource.md,
    release-package/docs-docusaurus/docs/user-guide/import-from-file.md
  </read_first>
  <action>
    Create `release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md` following the same RTL div wrapper template used in Phase 28 chapters (per D-23).

    Content structure:
    1. **Frontmatter**: title, sidebar_position (after existing chapters)
    2. **RTL wrapper**: `<div dir="rtl" style={{textAlign: 'right'}}>`
    3. **Introduction**: What auto-revalidation is and why it matters
    4. **Mermaid flow diagram** showing:
       ```
       User updates schema → SchemaUpdatedEvent → InvalidRecordsService →
       Query affected records → Batch publish ValidationRequestEvents →
       ValidationService revalidates → Pass: delete from invalid + route to output,
       Fail: update error messages
       ```
    5. **Step-by-step: Automatic revalidation**
       - Explain: "When you save a modified schema, revalidation starts automatically"
       - No confirmation dialog needed (per D-01)
       - Rate limiting for large datasets (per D-02)
    6. **Step-by-step: Manual "Revalidate All" button**
       - Location 1: Invalid records page header
       - Location 2: Schema tab in datasource edit
       - Confirmation popover with record count (per D-09)
       - When to use: edge cases after transient failures (per D-10)
    7. **Schema versioning**
       - How SchemaVersion auto-increments (per D-12)
       - ValidatedWithSchemaVersion tracking
       - Schema version tags on records (per D-13)
    8. **TTL for invalid records** (per D-24)
       - Global default: 4 days
       - Per-datasource override in basic info tab
       - Daily purge job behavior
    9. **YAML schema download** (per D-18/D-19)
       - Button location in schema tab
       - Use case: export schema for external tools
    10. **Notifications**
       - Toast notification: "X of Y records resolved" (per D-05)
       - Stats dashboard auto-refresh (per D-06)
    11. **Troubleshooting FAQ**:
       - "Why didn't my records get revalidated?" (check if ignored, check if schema actually changed)
       - "Records are still failing after schema change" (schema may not address those specific errors)

    All text in Hebrew. Use Mermaid for the flow diagram. Reference screenshot placeholders (can be filled later).
  </action>
  <verify>
    <automated>grep -c "mermaid" release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md && grep -c "אימות מחדש" release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at release-package/docs-docusaurus/docs/user-guide/auto-revalidation.md
    - grep -c "mermaid" returns at least 1 (Mermaid diagram present)
    - grep -c "אימות מחדש" returns at least 5 (Hebrew content about revalidation)
    - grep -c "rtl" returns at least 1 (RTL wrapper present)
    - grep -c "SchemaVersion\|גרסת סכמה" returns at least 1 (schema versioning documented)
    - grep -c "TTL\|שמירת רשומות" returns at least 1 (TTL documented)
  </acceptance_criteria>
  <done>Hebrew auto-revalidation chapter with Mermaid diagram, step-by-step instructions, schema versioning, TTL docs, YAML download docs, and troubleshooting FAQ.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing docs chapters and sidebar</name>
  <files>
    release-package/docs-docusaurus/docs/user-guide/create-datasource.md,
    release-package/docs-docusaurus/docs/user-guide/troubleshooting.md,
    release-package/docs-docusaurus/sidebars.js,
    docs/user-guide/USER-GUIDE-HE.md
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/user-guide/create-datasource.md,
    release-package/docs-docusaurus/docs/user-guide/troubleshooting.md,
    release-package/docs-docusaurus/sidebars.js,
    docs/user-guide/USER-GUIDE-HE.md
  </read_first>
  <action>
    1. Update `create-datasource.md` (per D-24):
       - Add a section about the "Invalid Records TTL" field in the basic info tab
       - Explain: optional field, default 4 days, per-datasource override
       - Place near the existing retention/configuration fields section

    2. Update `troubleshooting.md` (per D-25):
       - Add revalidation FAQ items:
         - "Records not revalidated after schema change" — check if records are marked as ignored
         - "Revalidate All button not visible on Schema tab" — only appears when invalid records exist
         - "Records still failing after revalidation" — the new schema may not fix those specific errors
         - "When are expired records deleted?" — daily at 3 AM UTC, based on TTL setting

    3. Update `sidebars.js`:
       - Add `'user-guide/auto-revalidation'` to the user guide section
       - Place it after the existing chapters (schema-validation or monitoring)

    4. Update `docs/user-guide/USER-GUIDE-HE.md`:
       - Add a section about auto-revalidation
       - Brief summary pointing to the Docusaurus chapter for full details
       - This is the legacy monolithic guide that may still be referenced

    5. If changelog/release-notes files exist in docs, add entry for auto-revalidation feature (per D-26):
       - Check for `release-package/docs-docusaurus/docs/releases/` or similar
       - Add v0.5.0 or next version entry with auto-revalidation feature description
  </action>
  <verify>
    <automated>grep -c "auto-revalidation" release-package/docs-docusaurus/sidebars.js && grep -c "TTL\|אימות מחדש" release-package/docs-docusaurus/docs/user-guide/create-datasource.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "auto-revalidation" release-package/docs-docusaurus/sidebars.js returns at least 1
    - grep -c "TTL\|InvalidRecordsTtlDays\|שמירת רשומות" release-package/docs-docusaurus/docs/user-guide/create-datasource.md returns at least 1
    - grep -c "revalidat\|אימות מחדש" release-package/docs-docusaurus/docs/user-guide/troubleshooting.md returns at least 1
  </acceptance_criteria>
  <done>Sidebar includes auto-revalidation chapter. Create-datasource docs include TTL field. Troubleshooting has revalidation FAQ. Legacy guide updated.</done>
</task>

</tasks>

<verification>
1. Auto-revalidation chapter exists with Hebrew content and Mermaid diagram
2. Sidebar includes the new chapter
3. Create-datasource chapter documents TTL field
4. Troubleshooting has revalidation FAQ
</verification>

<success_criteria>
- Hebrew user guide chapter for auto-revalidation with Mermaid flow diagram, step-by-step instructions
- TTL behavior documented in create-datasource chapter
- Revalidate All button documented in auto-revalidation chapter
- Troubleshooting FAQ added
- Docusaurus sidebar updated
- Changelog/release notes entry for revalidation feature
</success_criteria>

<output>
After completion, create `.planning/phases/29-invalid-records-auto-revalidation/29-07-SUMMARY.md`
</output>
