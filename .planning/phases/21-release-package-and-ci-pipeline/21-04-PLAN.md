---
phase: 21-release-package-and-ci-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 21-03
files_modified:
  - release-package/CHANGELOG.md
  - .github/workflows/ci.yml
  - release-package/README.md
autonomous: true
requirements:
  - REL-01
  - REL-02

must_haves:
  truths:
    - "release-package/CHANGELOG.md has a [v0.2.0] entry at the top with all v0.2 milestone features"
    - "ci.yml has a release job that triggers only on v* tags, creates a GitHub release, attaches the deployment zip artifact"
    - "release-package/README.md documents the offline Helm deployment workflow for air-gapped environments"
    - "GitHub release body includes the v0.2.0 CHANGELOG excerpt"
  artifacts:
    - path: "release-package/CHANGELOG.md"
      provides: "v0.2.0 release notes"
      contains: "[v0.2.0]"
    - path: ".github/workflows/ci.yml"
      provides: "release job creating GitHub release on v* tags"
      contains: "gh release create"
    - path: "release-package/README.md"
      provides: "Offline Helm deployment guide for air-gapped OCP"
      contains: "offline"
  key_links:
    - from: "ci.yml (release job)"
      to: "release-package/CHANGELOG.md"
      via: "reads CHANGELOG for release body"
    - from: "ci.yml (release job)"
      to: "deployment zip artifact"
      via: "actions/download-artifact + gh release upload"
---

<objective>
Write the v0.2.0 CHANGELOG entry, add a GitHub release CI job (tag-triggered), and update the release-package README with the offline Helm deployment guide.

Purpose: Completes the v0.2 milestone release artifacts — a tagged GitHub release with CHANGELOG and the offline deployment documentation. Fulfills REL-01 (tagged release with CHANGELOG) and REL-02 (offline Helm package documentation).

Output: Updated `release-package/CHANGELOG.md`, new `release` job in `ci.yml`, updated `release-package/README.md`.
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-release-package-and-ci-pipeline/21-CONTEXT.md

<interfaces>
<!-- Key references for the executor -->

From 21-CONTEXT.md (locked GitHub release config):
  - Triggered only on v* tags (existing ci.yml pattern)
  - Creates GitHub release with: tag, CHANGELOG excerpt, deployment artifact attached
  - CHANGELOG already exists at release-package/CHANGELOG.md

From release-package/CHANGELOG.md (current content — file ends at [v0.1.1-rc2]):
  Must prepend [v0.2.0] entry above [v0.1.1-rc2].

From ROADMAP.md v0.2 phase list (features to document in CHANGELOG):
  Phase 11: Hebrew/RTL fixes, NAS dropdown fix
  Phase 12: NAS Lifecycle (auto-mount, DataSource linking, PVC validation, delete cascade)
  Phase 13: AdminServer + DemoDataGenerator file-simulator integration, NAS auto-test in forms
  Phase 14: OTEL verification (logs/traces/metrics for all 9 microservices)
  Phase 15: Device health monitoring (Quartz.NET, heartbeat, latency tracking)
  Phase 15.1: Protocol file operations tests (FTP, SFTP, NFS, S3, Kafka, HTTP, WebDAV)
  Phase 16: SignalR real-time monitoring updates
  Phase 17: File-simulator integration in DemoDataGenerator
  Phase 18: E2E validation (full pipeline flow, error handling, Playwright UI tests)
  Phase 19: Docusaurus documentation portal update
  Phase 20: CI/CD pipeline, version injection, Helm-based local production mode, OCP resources
  Phase 21: CI deployment pipeline (image build, package assembly, sanity gate)

From .github/workflows/ci.yml (existing jobs — release job comes after sanity-deploy):
  - Must only trigger on startsWith(github.ref, 'refs/tags/v')
  - Uses gh CLI to create release (available on GitHub Actions)
  - Downloads the deployment zip from package-release job output
  - Attaches zip as release asset

From 21-CONTEXT.md (deferred — do NOT include in README):
  - Full OCP/OpenShift deployment testing in CI
  - Container registry push
  - Multi-platform image builds
  - Helm chart publishing

From CLAUDE.md (air-gapped deployment context):
  - Platform: Windows (PowerShell) + Minikube for dev
  - OCP deployment is documented but not CI-automated
  - install.ps1 / install.sh exist in release package
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write v0.2.0 CHANGELOG entry and update release-package README</name>
  <files>release-package/CHANGELOG.md, release-package/README.md</files>

  <read_first>
    - release-package/CHANGELOG.md (full file — prepend v0.2.0 entry, preserve existing [v0.1.1-rc2] entry)
    - release-package/README.md (full file — append or rewrite offline Helm deployment section)
  </read_first>

  <action>
Prepend the following [v0.2.0] entry to `release-package/CHANGELOG.md` (insert BEFORE the `## [v0.1.1-rc2]` line):

```markdown
## [v0.2.0] - 2026-03-18

### Added
- **NAS Lifecycle Management**: Auto-mount PV/PVC volumes to service deployments based on device role (Input → FileDiscovery + FileProcessor, Output → OutputService, Both → all three). DataSource-NasDevice entity linking with mount path auto-population and PVC binding validation.
- **Device Health Monitoring**: Quartz.NET health check job (every 30s) tracking connectivity, latency, and availability for all NAS devices and AdminServers. Hebrew-aware UI with status cards and React Query polling.
- **SignalR Real-Time Updates**: MonitoringBroadcaster broadcasts health status changes to System Monitoring page in real-time. Connection recovery and polling fallback built in.
- **Protocol File Operations Testing**: Full matrix test coverage for FTP, SFTP, HTTP/WebDAV, S3, Kafka, and NFS protocols against file-simulator. Scenario A (static devices) and Scenario B (dynamic creation) both pass.
- **File-Simulator Integration**: DemoDataGenerator queries file-simulator API and seeds AdminServer + NasDevice entities. `--upload-files` flag uploads test files via multi-protocol endpoints.
- **E2E Validation Suite**: Playwright-based full pipeline flow tests (generate → ingest → process → output → compare), error/negative tests, and UI tests for all features.
- **Docusaurus Documentation Portal**: Updated documentation for all v0.2.0 features, built as standalone container, accessible via frontend Help link.
- **CI/CD Pipeline**: GitHub Actions pipeline builds all 10 application images from source, assembles versioned offline deployment packages (`deployment-{VERSION}-{YYYYMMDD}-{HHMMSS}/`), deploys via Helm on self-hosted runner, seeds with DemoDataGenerator, and validates with `@Sanity` test suite.
- **Offline Deployment Package**: Self-contained deployment folder with all 21 image tars (10 application + 11 infrastructure), Helm chart, values-local.yaml, and install scripts for air-gapped environments.
- **OCP Resource Configuration**: Proper resource requests/limits defined for all pods; security contexts (runAsNonRoot, allowPrivilegeEscalation: false, capabilities drop ALL) for OCP compliance.

### Changed
- Frontend footer displays version number injected by CI/CD build pipeline
- Local production deployment uses single-replica configuration (values-local.yaml) reducing Hazelcast memory from 8Gi to 1Gi
- Docusaurus nginx pinned to 1.28-alpine for OCP compliance
- Helm-first deployment workflow in README replaces manual kubectl apply instructions

### Fixed
- Hebrew/RTL layout regressions in Ant Design (fixed columns, Badge, Collapse icons)
- Unwanted English translations in Hebrew mode eliminated
- NAS device dropdown replaced NFS protocol selector in DataSource Connection and Output tabs
- NAS delete prevented when referenced by DataSources; cascade cleanup on deprovision

---

```

Full updated file will be:
1. The new `## [v0.2.0] - 2026-03-18` section (above)
2. A `---` separator line
3. The existing `## [v0.1.1-rc2] - 2025-01-08` content unchanged

---

For `release-package/README.md`, read the existing file and append the following section at the end (or replace a stale "Offline Deployment" section if one exists — search for `# Offline` or `## Offline` headers):

```markdown

---

## Offline / Air-Gapped Deployment

The CI pipeline produces a self-contained deployment folder on every successful build. This folder can be physically transferred to an air-gapped network and deployed without internet access.

### Package Structure

```
deployment-{VERSION}-{YYYYMMDD}-{HHMMSS}/
├── images/
│   ├── services/           # 10 application image .tar files (built from source)
│   └── infrastructure/     # 11 infrastructure image .tar files
├── helm/
│   └── ez-platform/        # Full Helm chart
├── values-local.yaml       # Single-replica deployment config
├── install.ps1             # PowerShell installer (Windows)
├── install.sh              # Bash installer (Linux/macOS)
├── IMAGE-MANIFEST.txt      # All 21 images with versions
└── DEPLOYMENT-GUIDE.md     # Install instructions
```

### Offline Install Steps

**Prerequisites:** kubectl, helm 3.8+, minikube (or any Kubernetes cluster)

**Windows (PowerShell):**
```powershell
# 1. Load all images into your cluster
foreach ($tar in Get-ChildItem images\services\*.tar, images\infrastructure\*.tar) {
    minikube image load $tar.FullName
}

# 2. Deploy
.\install.ps1 --local
```

**Linux/macOS (Bash):**
```bash
# 1. Load all images into your cluster
for tar in images/services/*.tar images/infrastructure/*.tar; do
    minikube image load "$tar"
done

# 2. Deploy
bash install.sh ez-platform 15m
```

### OCP / OpenShift Air-Gapped

For OpenShift Container Platform (OCP) air-gapped environments:
1. Mirror images using `oc image mirror` or `skopeo copy` from the .tar files
2. Update `values-local.yaml` with your internal registry URLs
3. Apply OCP-specific resources: `kubectl apply -f helm/ez-platform/templates/ocp-routes.yaml`

See `DEPLOYMENT-GUIDE.md` in the package for full OCP instructions.

### Obtaining the Deployment Package

CI artifacts are available under **Actions → package-release** in GitHub for 90 days after each build. Tagged releases attach the package as a release asset on the GitHub Releases page.
```
  </action>

  <verify>
    <automated>grep -q "\[v0.2.0\]" release-package/CHANGELOG.md && echo "PASS: v0.2.0 entry present" || echo "FAIL"
grep -q "NAS Lifecycle" release-package/CHANGELOG.md && echo "PASS: NAS content" || echo "FAIL"
grep -q "\[v0.1.1-rc2\]" release-package/CHANGELOG.md && echo "PASS: old entry preserved" || echo "FAIL"
grep -qi "offline" release-package/README.md && echo "PASS: offline section in README" || echo "FAIL"
grep -q "deployment-" release-package/README.md && echo "PASS: package structure documented" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - `release-package/CHANGELOG.md` starts with `## [v0.2.0] - 2026-03-18`
    - v0.2.0 entry covers: NAS lifecycle, device health monitoring, SignalR, protocol tests, file-simulator integration, E2E validation, Docusaurus, CI/CD pipeline, offline package, OCP resources
    - v0.2.0 entry has Added / Changed / Fixed sections
    - Existing `[v0.1.1-rc2]` entry is preserved verbatim below the new entry
    - `release-package/README.md` has an "Offline / Air-Gapped Deployment" section
    - README offline section shows: package folder structure, Windows + Linux install commands, OCP note, artifact location
  </acceptance_criteria>
  <done>CHANGELOG v0.2.0 entry written; README offline deployment section added; existing content preserved</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub release job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>

  <read_first>
    - .github/workflows/ci.yml (read full file; append release job as final job; preserve all prior jobs)
    - release-package/CHANGELOG.md (understand structure for excerpt extraction in CI step)
  </read_first>

  <action>
Append the `release` job to `.github/workflows/ci.yml` as the final job (after `sanity-deploy`). This job triggers ONLY on v* tags.

```yaml

  release:
    name: Create GitHub Release
    needs: package-release
    # Only runs on version tags, never on branch pushes
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write   # Required for gh release create
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Release version: $VERSION"

      - name: Extract CHANGELOG excerpt for release body
        id: changelog
        run: |
          # Extract the [VERSION] section from CHANGELOG.md
          VERSION="${{ steps.version.outputs.VERSION }}"
          # Remove leading 'v' for CHANGELOG lookup (CHANGELOG uses [v0.2.0] format)
          SECTION_HEADER="## \[${VERSION}\]"
          # Extract from the matching header to the next ## header (or EOF)
          BODY=$(awk "/^${SECTION_HEADER}$/,/^## \[/" release-package/CHANGELOG.md \
            | head -n -1 \
            | tail -n +2)
          if [ -z "$BODY" ]; then
            BODY="See release-package/CHANGELOG.md for full release notes."
          fi
          # Write to file (avoids quoting issues with multiline in GITHUB_OUTPUT)
          echo "$BODY" > /tmp/release-body.md
          echo "Release body extracted (${#BODY} chars)"

      - name: Download deployment package artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.package-release.outputs.pkg_name }}
          path: release-assets/

      - name: Create GitHub release
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          ZIP_FILE=$(ls release-assets/*.zip | head -1)
          echo "Creating release $VERSION with asset: $ZIP_FILE"

          gh release create "$VERSION" \
            --title "EZ Platform $VERSION" \
            --notes-file /tmp/release-body.md \
            "$ZIP_FILE"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release summary
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          echo "## GitHub Release Created" >> $GITHUB_STEP_SUMMARY
          echo "- Tag: $VERSION" >> $GITHUB_STEP_SUMMARY
          echo "- Release: https://github.com/${{ github.repository }}/releases/tag/$VERSION" >> $GITHUB_STEP_SUMMARY
```

Notes on the release job:
- `needs: package-release` (not `sanity-deploy`) — release can proceed even if self-hosted runner is unavailable. sanity-deploy has its own `if:` gate (`vars.EZ_K8S_RUNNER == 'true'`). The release must not be blocked by optional sanity runs.
- `permissions: contents: write` is required for `gh release create` on GITHUB_TOKEN.
- CHANGELOG header format is `## [v0.2.0] - YYYY-MM-DD` — the awk command handles the bracket-wrapped version correctly.
- The `if: startsWith(github.ref, 'refs/tags/v')` means this job never runs on branch pushes.
  </action>

  <verify>
    <automated>grep -q "release:" .github/workflows/ci.yml && echo "PASS: release job present" || echo "FAIL"
grep -q "startsWith(github.ref, 'refs/tags/v')" .github/workflows/ci.yml && grep -A3 "release:" .github/workflows/ci.yml | grep -q "if:" && echo "PASS: tag-only gate" || echo "FAIL"
grep -q "gh release create" .github/workflows/ci.yml && echo "PASS: gh release create present" || echo "FAIL"
grep -q "GH_TOKEN" .github/workflows/ci.yml && echo "PASS: GH_TOKEN present" || echo "FAIL"
grep -q "contents: write" .github/workflows/ci.yml && echo "PASS: write permissions" || echo "FAIL"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "VALID YAML" || echo "INVALID YAML"</automated>
  </verify>

  <acceptance_criteria>
    - `ci.yml` has a `release` job as the final job in the `jobs:` block
    - `if: startsWith(github.ref, 'refs/tags/v')` — never runs on non-tag pushes
    - `needs: package-release` — downloads the assembled deployment zip
    - `permissions: contents: write` declared for the job
    - Steps: extract version, extract CHANGELOG excerpt, download artifact, `gh release create` with notes + asset, summary
    - `gh release create` attaches the deployment zip as a release asset
    - Release notes body comes from `release-package/CHANGELOG.md` [VERSION] section extraction
    - All prior jobs (build, test, performance, docker, helm-validate, docker-build, package-release, sanity-deploy) are unchanged
    - `ci.yml` is valid YAML
  </acceptance_criteria>
  <done>release job added to ci.yml; tag-only trigger; CHANGELOG excerpt extraction; deployment zip attached as release asset</done>
</task>

</tasks>

<verification>
```bash
# Confirm v0.2.0 CHANGELOG entry exists at top
head -5 release-package/CHANGELOG.md

# Confirm both versions in CHANGELOG
grep "^\#\# \[v" release-package/CHANGELOG.md

# Confirm offline section in README
grep -i "offline\|air-gapped" release-package/README.md

# Confirm release job in ci.yml
grep -A5 "^  release:" .github/workflows/ci.yml

# Confirm tag-only condition
grep "refs/tags/v" .github/workflows/ci.yml | grep "release\|if"

# Confirm gh release create
grep "gh release create" .github/workflows/ci.yml

# Validate full ci.yml YAML syntax
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('VALID')"

# Count total jobs in ci.yml (should be 8: build, test, performance, docker, helm-validate, docker-build, package-release, sanity-deploy, release = 9)
grep -E "^  [a-z-]+:" .github/workflows/ci.yml | grep -v "^  #" | wc -l
```
</verification>

<success_criteria>
- `release-package/CHANGELOG.md` documents all v0.2.0 milestone features in Keep a Changelog format
- `release-package/README.md` has a clear offline/air-gapped deployment guide with package structure and install commands
- CI `release` job creates a GitHub release on every v* tag with CHANGELOG excerpt in release body
- Deployment zip from `package-release` is attached as a release asset
- `ci.yml` is valid YAML with 9 total jobs (8 existing + release)
- Running `git tag v0.2.0 && git push --tags` will trigger the full pipeline: test → docker-build → package-release → (sanity-deploy if runner available) → release
</success_criteria>

<output>
After completion, create `.planning/phases/21-release-package-and-ci-pipeline/21-04-SUMMARY.md`
</output>
