---
phase: 26-completeness-checklist
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/DemoDataGenerator/Generators/AllGenerators.cs
autonomous: true
gap_closure: true
requirements: [CHECK-04]

must_haves:
  truths:
    - "DemoDataGenerator creates datasources where all required fields are populated (Name, SupplierName, Category, connectionType, fileType, JsonSchema with typed properties)"
    - "Generated datasources pass checkDataSourceCompleteness with isComplete=true"
    - "IsActive=true is only set on datasources that actually have all required fields"
  artifacts:
    - path: "tools/DemoDataGenerator/Generators/AllGenerators.cs"
      provides: "DataSource generator with completeness-consistent data"
      contains: "ScheduleFrequency"
  key_links:
    - from: "AllGenerators.cs"
      to: "completenessConfig.ts"
      via: "Field names match completeness required field list"
      pattern: "fileConfig|connectionConfig|JsonSchema"
---

<objective>
Close GAP-06: Ensure DemoDataGenerator creates datasources that are consistent with the completeness engine. All generated datasources must have required fields populated (Name, SupplierName, Category, connection type + server/device, file type, and a JSON Schema with typed properties) so they pass completeness checks.

Purpose: Without this fix, demo data shows inconsistent status -- datasources appear as IsActive=true but the completeness indicator would show them as incomplete, confusing users.

Output: Updated AllGenerators.cs with completeness-consistent data generation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-completeness-checklist/26-CONTEXT.md
@.planning/phases/26-completeness-checklist/26-VERIFICATION.md
@.planning/phases/26-completeness-checklist/26-01-SUMMARY.md

<interfaces>
<!-- Completeness required fields that demo data must satisfy -->

From src/Frontend/src/components/datasource/completeness/completenessConfig.ts:
```
Required tabs and their required fields:
- basic: name (required), supplierName (required), category (required)
- connection: connectionType (required), inputServerId (required when FTP/SFTP/HTTP/Kafka/S3), nasDeviceId (required when NAS), filePath (required when FTP/SFTP/S3/Local), httpEndpointPath (required when HTTP), kafkaTopic (required when Kafka)
- file: fileType (required)
- schema: nonFormCheck -- JsonSchema must have properties, each with type defined, passes AJV compilation, and schema-filetype alignment

Recommended tabs (not blocking but tracked):
- schedule: scheduleFrequency (required within tab)
- output: at least 1 destination
- metrics: N/A in create mode
```

From completenessUtils.ts -- field mapping from API to form values:
```typescript
formValues = {
  name: ds.Name,
  supplierName: ds.SupplierName,
  category: ds.Category,
  connectionType: connectionConfig.type,
  filePath: connectionConfig.path || ds.FilePath,
  inputServerId: connectionConfig.inputServerId,
  nasDeviceId: connectionConfig.nasDeviceId,
  kafkaTopic: connectionConfig.topic,
  fileType: fileConfig.type,
  scheduleFrequency: ds.ScheduleFrequency || ds.scheduleFrequency,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DemoDataGenerator to produce completeness-consistent datasources</name>
  <files>tools/DemoDataGenerator/Generators/AllGenerators.cs</files>
  <read_first>
    tools/DemoDataGenerator/Generators/AllGenerators.cs
    src/Frontend/src/components/datasource/completeness/completenessConfig.ts
    src/Frontend/src/components/datasource/completeness/completenessUtils.ts
  </read_first>
  <action>
Read the full `AllGenerators.cs` file. The `DataSourceGenerator` class creates datasources and a separate `SchemaGenerator` class populates `JsonSchema` and creates `DataProcessingSchema` records.

**Issue 1: ScheduleFrequency not set on entity**

The `configurationSettings` object has `schedule.frequency = "Custom"` but this is nested in ConfigurationSettings JSON. The `completenessUtils.ts` reads `ds.ScheduleFrequency` as a top-level field. Check if the entity `DataProcessingDataSource` has a `ScheduleFrequency` property. If it does, set it:

```csharp
ScheduleFrequency = "Custom",
```

Add this to the `DataProcessingDataSource` object literal alongside `CronExpression`, `IsActive`, etc. This ensures the completeness utility can read it from the top-level API field.

**Issue 2: JsonSchema initially empty**

Currently line ~209 shows `JsonSchema = new BsonDocument()` -- an empty document. The `SchemaGenerator.GenerateForDataSourcesAsync` runs later and populates it. Verify that after `SchemaGenerator` runs, the `ds.JsonSchema` is non-empty with typed properties.

Read the `SchemaGenerator` class (search for `class SchemaGenerator` in AllGenerators.cs). Confirm it calls `ds.JsonSchema = BsonDocument.Parse(jsonSchemaContent)` where `jsonSchemaContent` is a valid JSON Schema with typed properties. The current code at line ~359 does this. This is fine -- the schema IS populated, just after initial save.

However, check if `GenerateComplexJsonSchema` produces schemas that pass ALL completeness checks:
1. Has `properties` with at least 1 property
2. Each property has a `type` defined (not undefined/missing)
3. No nested objects/arrays if the datasource fileType is CSV or Excel

Read `GenerateComplexJsonSchema` method. If it generates nested objects for CSV datasources, add a guard: when `fileConfig.type` is "CSV" or "EXCEL", ensure schema only has flat primitive properties.

**Issue 3: Ensure all generated datasources have complete connection fields**

Review the connection config generation. Verify:
- `connectionConfig.type` is always set (line ~156 area)
- `connectionConfig.inputServerId` is set when type is FTP/SFTP/HTTP/Kafka/S3
- `connectionConfig.nasDeviceId` is set when type is NAS
- `connectionConfig.path` is set when type requires filePath
- `connectionConfig.topic` is set when type is Kafka

If any of these are conditionally null but the completeness engine expects them, fix the generator to always provide them.

**Issue 4: FileConfig type alignment**

The completeness engine expects `fileConfig.type` in UPPERCASE (e.g., "CSV", "JSON", "XML"). Verify line ~178: `type = effectiveFilePattern.TrimStart('*', '.').ToUpper()`. This should produce "CSV", "JSON", "XML", "XLSX" etc. Confirm this matches what the completeness engine expects. The engine checks `fileType` from `fileConfig.type`, so uppercase is correct.

**Issue 5: Output destinations (recommended, not blocking)**

Currently `GenerateOutputConfiguration` is called. Verify it generates at least 1 destination. If it does, recommended completeness will be satisfied. Not blocking, but nice to have.

After making changes, verify that the .NET project compiles:
```bash
cd tools/DemoDataGenerator && dotnet build 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>cd C:/Users/UserC/source/repos/EZ/tools/DemoDataGenerator && dotnet build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AllGenerators.cs DataSource entity creation includes `ScheduleFrequency = "Custom"` as a top-level property
    - SchemaGenerator produces schemas where ALL properties have `type` defined (no untyped properties)
    - CSV/Excel datasources do NOT get schemas with nested objects or arrays
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>DemoDataGenerator produces datasources that satisfy completenessUtils.checkDataSourceCompleteness with isComplete=true for all generated items</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds in tools/DemoDataGenerator
2. `grep "ScheduleFrequency" tools/DemoDataGenerator/Generators/AllGenerators.cs` shows it set on the entity
3. Schema generation for CSV datasources produces only flat properties (no type: "object" or type: "array")
</verification>

<success_criteria>
- GAP-06: DemoDataGenerator output is consistent with completeness engine requirements
- All generated datasources have: Name, SupplierName, Category, connectionType + relevant fields, fileType, ScheduleFrequency as top-level field, and valid typed JsonSchema
- .NET project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-completeness-checklist/26-06-SUMMARY.md`
</output>
