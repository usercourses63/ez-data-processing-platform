---
phase: 26-completeness-checklist
plan: 04
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified:
  - src/Frontend/tests/e2e/completeness-checklist.spec.ts
  - src/Frontend/tests/webapp-testing/completeness_comprehensive_test.py
autonomous: true
requirements: [CHECK-01, CHECK-02, CHECK-03, CHECK-04]

must_haves:
  truths:
    - "Playwright E2E spec covers sidebar display, click-to-navigate, color indicators, and enable/disable enforcement"
    - "webapp-testing comprehensive script verifies edge cases and detailed UI behavior"
    - "Both test suites pass with --headed flag"
  artifacts:
    - path: "src/Frontend/tests/e2e/completeness-checklist.spec.ts"
      provides: "Playwright E2E tests for completeness checklist feature"
      min_lines: 80
    - path: "src/Frontend/tests/webapp-testing/completeness_comprehensive_test.py"
      provides: "Python Playwright comprehensive UI test for completeness feature"
      min_lines: 100
  key_links:
    - from: "completeness-checklist.spec.ts"
      to: "http://localhost:7000"
      via: "Playwright tests navigate to datasource forms and list page"
      pattern: "page.goto.*datasource"
    - from: "completeness_comprehensive_test.py"
      to: "http://localhost:7000"
      via: "Python Playwright tests verify detailed UI behavior"
      pattern: "page.goto.*datasource"
---

<objective>
Create automated Playwright E2E tests and webapp-testing comprehensive tests for the completeness checklist feature. Both must run with --headed flag per project testing policy.

Purpose: Every frontend UI phase MUST include both Playwright E2E and webapp-testing comprehensive tests per CLAUDE.md testing requirements. This plan ensures the completeness checklist feature has full automated test coverage before the human checkpoint in Plan 03.

Output: 1 Playwright spec file, 1 Python comprehensive test script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-completeness-checklist/26-CONTEXT.md
@.planning/phases/26-completeness-checklist/26-02-SUMMARY.md
@.planning/phases/26-completeness-checklist/26-03-SUMMARY.md

@src/Frontend/playwright.config.ts
@src/Frontend/tests/e2e/sanity.spec.ts
@src/Frontend/tests/webapp-testing/

@src/Frontend/src/components/datasource/completeness/types.ts
@src/Frontend/src/components/datasource/completeness/completenessConfig.ts
@src/Frontend/src/pages/datasources/DataSourceFormEnhanced.tsx
@src/Frontend/src/pages/datasources/DataSourceEditEnhanced.tsx
@src/Frontend/src/pages/datasources/DataSourceList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright E2E spec for completeness checklist</name>
  <files>
    src/Frontend/tests/e2e/completeness-checklist.spec.ts
  </files>
  <read_first>
    src/Frontend/playwright.config.ts
    src/Frontend/tests/e2e/sanity.spec.ts
    src/Frontend/src/components/datasource/completeness/completenessConfig.ts
    src/Frontend/src/pages/datasources/DataSourceFormEnhanced.tsx
    src/Frontend/src/pages/datasources/DataSourceList.tsx
  </read_first>
  <action>
    Create `src/Frontend/tests/e2e/completeness-checklist.spec.ts` with the following test scenarios. Use the same patterns as `sanity.spec.ts` for page navigation, selectors, and assertions. Base URL is `http://localhost:7000` from playwright.config.ts.

    **Test structure:**

    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Completeness Checklist', () => {

      test.describe('Create Form Sidebar (CHECK-01)', () => {
        test('displays sidebar with progress ring at 0% on new datasource form', async ({ page }) => {
          await page.goto('/datasources/new');
          // Verify sidebar exists with CompletenessPanel
          // Look for Progress circle element (antd renders as SVG with role or class)
          // Verify 0% displayed
          // Verify 9 tab items in sidebar
        });

        test('progress ring updates as required fields are filled', async ({ page }) => {
          await page.goto('/datasources/new');
          // Fill name field
          // Fill supplierName field
          // Fill category field (select from dropdown)
          // Verify progress percentage increased from 0%
        });
      });

      test.describe('Click-to-Navigate (CHECK-03)', () => {
        test('clicking sidebar tab navigates to corresponding form tab', async ({ page }) => {
          await page.goto('/datasources/new');
          // Click "Connection" item in sidebar
          // Verify Connection tab content is visible
          // Click "Schema" item in sidebar
          // Verify Schema tab content is visible
        });

        test('active tab is highlighted in sidebar', async ({ page }) => {
          await page.goto('/datasources/new');
          // Verify Basic Info tab item has active styling (background color)
          // Click Connection in sidebar
          // Verify Connection tab item now has active styling
          // Verify Basic Info no longer has active styling
        });
      });

      test.describe('Per-Field Color Indicators (CHECK-04, D-13)', () => {
        test('required fields show red borders when empty and green when filled', async ({ page }) => {
          await page.goto('/datasources/new');
          // Find Form.Item elements in basic tab
          // Verify they have red left border (border-inline-start-color)
          // Fill the name field
          // Verify that specific Form.Item border changes to green
        });

        test('recommended and optional tab fields show gray borders', async ({ page }) => {
          await page.goto('/datasources/new');
          // Navigate to Schedule tab (recommended)
          // Verify Form.Items have gray borders
          // Navigate to Validation tab (optional)
          // Verify Form.Items have gray borders
        });
      });

      test.describe('Edit Form Sidebar (CHECK-02)', () => {
        test('edit form shows progress reflecting existing data', async ({ page }) => {
          // Navigate to list, find a datasource, click edit
          await page.goto('/datasources');
          // Click first edit button
          // Verify sidebar exists with progress > 0% (existing data)
          // Verify completed tabs show green check icons
        });

        test('metrics tab is available in edit mode', async ({ page }) => {
          await page.goto('/datasources');
          // Click edit on first datasource
          // Verify Metrics tab in sidebar is NOT grayed out
          // Click Metrics tab -- should navigate
        });
      });

      test.describe('Enable/Disable Enforcement (D-16 through D-21)', () => {
        test('status column shows read-only badge', async ({ page }) => {
          await page.goto('/datasources');
          // Verify status column contains Tag elements, NOT Select/dropdown
          // Verify no onChange interaction on status badges
        });

        test('power icon button exists in actions area', async ({ page }) => {
          await page.goto('/datasources');
          // Verify PoweroffOutlined icon button exists in actions column
        });

        test('enabling incomplete datasource shows warning modal', async ({ page }) => {
          await page.goto('/datasources');
          // Find a disabled/incomplete datasource row
          // Click the power icon button
          // Verify modal.warning appears with missing fields text
          // Close the modal
        });

        test('enabling complete datasource succeeds', async ({ page }) => {
          await page.goto('/datasources');
          // Find a complete but disabled datasource (or create one first)
          // Click the power icon
          // Verify status changes to enabled (Tag changes to success color)
        });
      });

      test.describe('Auto-Disable on Save (D-16)', () => {
        test('saving incomplete datasource auto-sets to disabled', async ({ page }) => {
          await page.goto('/datasources/new');
          // Fill only basic info (name, supplier, category)
          // Do NOT fill connection, file, schema
          // Submit the form
          // Verify the created datasource has IsActive = false (check list page badge)
        });
      });
    });
    ```

    **Important implementation notes:**
    - Use `page.waitForSelector` or `page.locator` with appropriate timeouts for antd lazy-loaded tabs
    - For RTL sidebar detection, check computed style of the flex container
    - For border color checks, use `page.evaluate` to read `getComputedStyle(el).borderInlineStartColor`
    - For antd Progress circle, look for `.ant-progress-circle` or SVG elements
    - For antd Tag, look for `.ant-tag` elements
    - For sidebar tab items, look for clickable elements within the CompletenessPanel Card
    - Viewport width should be 1400px to avoid RTL overflow issues (per MEMORY.md)
    - Use `test.setTimeout(30000)` for tests that involve form submission and navigation
  </action>
  <verify>
    <automated>cd C:/Users/UserC/source/repos/EZ/src/Frontend && npx playwright test tests/e2e/completeness-checklist.spec.ts --headed 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Frontend/tests/e2e/completeness-checklist.spec.ts` exists
    - Test file has at least 8 test cases covering CHECK-01, CHECK-02, CHECK-03, CHECK-04
    - Tests cover: sidebar display, progress ring, click-to-navigate, per-field borders, enable/disable enforcement, auto-disable on save
    - Tests use Playwright best practices (locators, assertions, proper waits)
    - All tests pass when run with `npx playwright test --headed`
  </acceptance_criteria>
  <done>Playwright E2E spec covers all completeness checklist requirements with headed browser verification.</done>
</task>

<task type="auto">
  <name>Task 2: Create webapp-testing comprehensive Python test script</name>
  <files>
    src/Frontend/tests/webapp-testing/completeness_comprehensive_test.py
  </files>
  <read_first>
    src/Frontend/tests/webapp-testing/
    src/Frontend/src/components/datasource/completeness/completenessConfig.ts
    src/Frontend/src/components/datasource/completeness/types.ts
    src/Frontend/src/pages/datasources/DataSourceList.tsx
  </read_first>
  <action>
    Create `src/Frontend/tests/webapp-testing/completeness_comprehensive_test.py` -- a Python Playwright comprehensive test using the webapp-testing pattern (reconnaissance-first approach).

    **Follow the webapp-testing skill pattern:**
    1. Reconnaissance phase: discover DOM structure of sidebar, form tabs, status column
    2. Verification phase: test each behavior

    **Test cases to cover (edge cases and detailed UI):**

    ```python
    from playwright.sync_api import sync_playwright
    import time

    BASE_URL = "http://localhost:7000"

    def test_sidebar_presence_and_structure(page):
        """Verify sidebar Card, Progress circle, 9 tab items"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Find sidebar (Card with Progress circle)
        # Count tab items -- must be exactly 9
        # Verify tab order matches: basic, connection, file, schema, schedule, output, metrics, validation, notifications

    def test_progress_ring_gradient_colors(page):
        """Verify progress ring uses red-to-green gradient"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Check SVG stroke colors at 0% (should be red)
        # Fill fields to 50% -- check gradient
        # Fill all required -- check green

    def test_per_field_border_colors_basic_tab(page):
        """D-13: Each Form.Item gets individual border color"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Recon: find all .ant-form-item elements in basic tab
        # Verify name field has red border (required, empty)
        # Verify description field has gray border (optional)
        # Fill name field
        # Verify name field border changes to green
        # Verify supplierName still has red border

    def test_per_field_border_colors_connection_tab(page):
        """D-13: Connection tab conditional fields"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Navigate to connection tab
        # Select connection type = SFTP
        # Verify connectionPath gets red border (required for SFTP)
        # Verify inputServerId gets red border (required for SFTP)
        # Change to Kafka
        # Verify kafkaBrokers and kafkaTopic get red borders
        # Verify connectionPath no longer shown or no longer required

    def test_sidebar_click_navigation_all_tabs(page):
        """Click each of 9 tabs in sidebar, verify correct tab content shows"""
        page.goto(f"{BASE_URL}/datasources/new")
        tabs = ['basic', 'connection', 'file', 'schema', 'schedule', 'output', 'metrics', 'validation', 'notifications']
        for tab in tabs:
            # Click tab in sidebar
            # Verify tab content is visible (tab-specific element)
            # Verify sidebar item has active highlight

    def test_metrics_tab_grayed_in_create_mode(page):
        """Metrics tab shows as unavailable in create form"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Find metrics tab item in sidebar
        # Verify it appears grayed out / has tooltip "Available after saving"
        # Click it -- should either not navigate or show empty state

    def test_metrics_tab_available_in_edit_mode(page):
        """Metrics tab is active in edit form"""
        # Navigate to edit form for existing datasource
        # Find metrics tab in sidebar
        # Verify it is NOT grayed out
        # Click it -- should navigate to metrics tab

    def test_recommended_tabs_gray_borders(page):
        """D-15: Recommended tabs show gray borders, not yellow"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Navigate to Schedule tab
        # Verify all Form.Items have gray left border (not yellow)
        # Navigate to Output tab
        # Same check

    def test_status_column_readonly(page):
        """D-20: Status column has no interactive elements"""
        page.goto(f"{BASE_URL}/datasources")
        # Find status column cells
        # Verify they contain Tag elements
        # Verify NO Select/dropdown/input elements exist in status cells
        # Try clicking status -- verify no dropdown opens

    def test_enable_incomplete_shows_modal(page):
        """D-17/D-18: Enable blocked with missing fields message"""
        page.goto(f"{BASE_URL}/datasources")
        # Find a disabled datasource row
        # Click the PoweroffOutlined button
        # Wait for modal to appear
        # Verify modal title contains "enable" or "blocked" text
        # Verify modal content lists specific missing field names
        # Close modal

    def test_enable_complete_succeeds(page):
        """D-21: Complete datasource can be enabled"""
        page.goto(f"{BASE_URL}/datasources")
        # Find a complete but disabled datasource
        # Click power icon
        # Verify no modal appears
        # Verify status badge changes to active/success

    def test_disable_always_allowed(page):
        """Disabling works without completeness check"""
        page.goto(f"{BASE_URL}/datasources")
        # Find an active datasource
        # Click power icon to disable
        # Verify status changes to inactive (no modal)

    def test_auto_disable_on_save_incomplete(page):
        """D-16: Saving incomplete datasource auto-disables"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Fill only name, supplier, category
        # Submit form
        # Navigate to list
        # Find the newly created datasource
        # Verify its status badge shows disabled

    def test_sidebar_sticky_scroll(page):
        """Sidebar stays visible when form content scrolls"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Set viewport to trigger scroll
        # Scroll form content down
        # Verify sidebar is still visible (position: sticky)

    def test_rtl_sidebar_position(page):
        """Sidebar appears on correct side in RTL mode"""
        page.goto(f"{BASE_URL}/datasources/new")
        # Switch to Hebrew (RTL)
        # Verify sidebar is visually on the left side of the page
        # Verify field borders use border-inline-start (right side visually in RTL)

    def test_cancel_enable_modal(page):
        """Modal can be dismissed without enabling"""
        page.goto(f"{BASE_URL}/datasources")
        # Find incomplete disabled datasource
        # Click power icon
        # Wait for modal
        # Click OK/close button
        # Verify datasource is still disabled

    def run_all_tests():
        with sync_playwright() as p:
            browser = p.chromium.launch(headless=False)
            context = browser.new_context(viewport={"width": 1400, "height": 900})
            page = context.new_page()

            tests = [
                test_sidebar_presence_and_structure,
                test_progress_ring_gradient_colors,
                test_per_field_border_colors_basic_tab,
                test_per_field_border_colors_connection_tab,
                test_sidebar_click_navigation_all_tabs,
                test_metrics_tab_grayed_in_create_mode,
                test_metrics_tab_available_in_edit_mode,
                test_recommended_tabs_gray_borders,
                test_status_column_readonly,
                test_enable_incomplete_shows_modal,
                test_enable_complete_succeeds,
                test_disable_always_allowed,
                test_auto_disable_on_save_incomplete,
                test_sidebar_sticky_scroll,
                test_rtl_sidebar_position,
                test_cancel_enable_modal,
            ]

            passed = 0
            failed = 0
            for test_fn in tests:
                try:
                    print(f"  Running {test_fn.__name__}...")
                    test_fn(page)
                    print(f"  PASSED: {test_fn.__name__}")
                    passed += 1
                except Exception as e:
                    print(f"  FAILED: {test_fn.__name__}: {e}")
                    failed += 1

            print(f"\nResults: {passed} passed, {failed} failed out of {len(tests)}")
            browser.close()
            return failed == 0

    if __name__ == "__main__":
        success = run_all_tests()
        exit(0 if success else 1)
    ```

    **Implementation notes:**
    - Use `headless=False` (headed mode) per project testing policy
    - Viewport 1400x900 per MEMORY.md to avoid RTL overflow
    - Each test function receives a `page` object and runs assertions
    - Use `page.evaluate()` for computed style checks (border colors)
    - Use `page.wait_for_selector()` with timeouts for lazy-loaded content
    - Print clear PASSED/FAILED for each test with descriptive names
    - Reconnaissance pattern: before assertions, log discovered DOM structure
  </action>
  <verify>
    <automated>cd C:/Users/UserC/source/repos/EZ/src/Frontend && python tests/webapp-testing/completeness_comprehensive_test.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Frontend/tests/webapp-testing/completeness_comprehensive_test.py` exists
    - Script has at least 15 test functions covering all completeness checklist scenarios
    - Tests cover edge cases: cancel modal, RTL positioning, sticky scroll, conditional connection fields
    - Script runs with headed browser (headless=False)
    - Script prints clear PASSED/FAILED results for each test
    - All tests pass
  </acceptance_criteria>
  <done>Comprehensive webapp-testing script verifies all completeness checklist UI behaviors including edge cases, per-field borders, modal interactions, RTL layout, and auto-disable enforcement.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/completeness-checklist.spec.ts --headed` passes all tests
- `python tests/webapp-testing/completeness_comprehensive_test.py` passes all tests
- Both test suites cover CHECK-01, CHECK-02, CHECK-03, CHECK-04
- Tests verify per-field borders (D-13), auto-disable (D-16), enable enforcement (D-17/D-18/D-21)
</verification>

<success_criteria>
- Playwright E2E spec has 8+ test cases and all pass with --headed
- webapp-testing script has 15+ test functions and all pass with headed browser
- Both test suites verify the critical paths: sidebar, navigation, borders, enforcement
- Testing policy satisfied: both /gsd:add-tests and webapp-testing tools exercised
</success_criteria>

<output>
After completion, create `.planning/phases/26-completeness-checklist/26-04-SUMMARY.md`
</output>
