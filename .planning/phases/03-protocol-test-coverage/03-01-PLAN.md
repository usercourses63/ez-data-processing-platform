---
phase: 03-protocol-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/IntegrationTests/Fixtures/FileSimulatorFixture.cs
  - tests/IntegrationTests/Fixtures/TestConfiguration.cs
  - tests/IntegrationTests/Connectors/FtpConnectorTests.cs
  - tests/IntegrationTests/Connectors/SftpConnectorTests.cs
  - tests/IntegrationTests/Connectors/HttpApiConnectorTests.cs
autonomous: true

must_haves:
  truths:
    - "FTP connector connects to file-simulator FTP server and reads/writes files"
    - "SFTP connector connects to file-simulator SFTP server and reads/writes files"
    - "HTTP connector connects to file-simulator HTTP endpoint and reads files"
    - "Tests skip gracefully when file-simulator is unavailable"
  artifacts:
    - path: "tests/IntegrationTests/Fixtures/FileSimulatorFixture.cs"
      provides: "Shared fixture managing file-simulator connectivity"
      contains: "class FileSimulatorFixture"
    - path: "tests/IntegrationTests/Connectors/FtpConnectorTests.cs"
      provides: "FTP connector integration tests"
      contains: "FtpConnector_TestConnection"
    - path: "tests/IntegrationTests/Connectors/SftpConnectorTests.cs"
      provides: "SFTP connector integration tests"
      contains: "SftpConnector_TestConnection"
    - path: "tests/IntegrationTests/Connectors/HttpApiConnectorTests.cs"
      provides: "HTTP connector integration tests"
      contains: "HttpApiConnector_TestConnection"
  key_links:
    - from: "tests/IntegrationTests/Connectors/*Tests.cs"
      to: "FileSimulatorFixture"
      via: "IClassFixture<FileSimulatorFixture>"
      pattern: "IClassFixture<FileSimulatorFixture>"
    - from: "FileSimulatorFixture"
      to: "file-simulator"
      via: "TCP connectivity test"
      pattern: "TcpClient.*ConnectAsync"
---

<objective>
Create the test infrastructure foundation and implement FTP, SFTP, and HTTP protocol tests against file-simulator.

Purpose: Establish the FileSimulatorFixture pattern and verify the most common file transfer protocols work correctly. This provides the foundation all other protocol tests will follow.

Output: FileSimulatorFixture shared test fixture, FtpConnectorTests, SftpConnectorTests, HttpApiConnectorTests - all verifying read/write/list operations against file-simulator NodePort services.
</objective>

<execution_context>
@C:\Users\UserC\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserC\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-protocol-test-coverage/03-RESEARCH.md

# Existing test patterns to follow
@tests/IntegrationTests/Fixtures/KafkaFixture.cs
@tests/IntegrationTests/ServiceIntegration/KafkaFlowTests.cs
@tests/IntegrationTests/DataProcessing.IntegrationTests.csproj

# Connectors under test
@src/Services/Shared/Connectors/FtpConnector.cs
@src/Services/Shared/Connectors/SftpConnector.cs
@src/Services/Shared/Connectors/HttpApiConnector.cs

# File-simulator configuration
@k8s/configmaps/file-simulator-config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FileSimulatorFixture and update test configuration</name>
  <files>
    tests/IntegrationTests/Fixtures/FileSimulatorFixture.cs
    tests/IntegrationTests/Fixtures/TestConfiguration.cs
  </files>
  <action>
Create FileSimulatorFixture following the KafkaFixture pattern:

1. Create `tests/IntegrationTests/Fixtures/FileSimulatorFixture.cs`:
   - Implement IAsyncLifetime for async initialization
   - Properties: FileSimulatorIp (string), IsAvailable (bool)
   - Protocol endpoint properties: FtpPort (30021), SftpPort (30022), S3Port (30900), HttpPort (30088), WebDavPort (30089)
   - Static Credentials class with tuples for each protocol:
     - Ftp = ("ftpuser", "ftppass123")
     - Sftp = ("sftpuser", "sftppass123")
     - S3 = ("minioadmin", "minioadmin123")
     - Http = ("httpuser", "httppass123")
   - InitializeAsync: Read FILE_SIMULATOR_IP from environment, test TCP connectivity to FtpPort
   - DisposeAsync: Empty implementation (no cleanup needed)
   - Private TestTcpConnectivityAsync method: 3 second timeout, returns bool

2. Update `tests/IntegrationTests/Fixtures/TestConfiguration.cs`:
   - Add static property FileSimulatorIp returning Environment.GetEnvironmentVariable("FILE_SIMULATOR_IP") ?? "172.25.201.3"
   - Add static nested class FileSimulator with port constants

Use xunit.skip.if pattern: tests calling Skip.If(!IsAvailable) for graceful skip when file-simulator unavailable.
  </action>
  <verify>
dotnet build tests/IntegrationTests/DataProcessing.IntegrationTests.csproj
No build errors for new fixtures.
  </verify>
  <done>FileSimulatorFixture class exists, implements IAsyncLifetime, has protocol endpoint properties and credentials, TestConfiguration extended with file-simulator settings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FTP, SFTP, and HTTP connector tests</name>
  <files>
    tests/IntegrationTests/Connectors/FtpConnectorTests.cs
    tests/IntegrationTests/Connectors/SftpConnectorTests.cs
    tests/IntegrationTests/Connectors/HttpApiConnectorTests.cs
  </files>
  <action>
Create connector test classes following the research patterns (see 03-RESEARCH.md Pattern 3):

**For each test class (FtpConnectorTests, SftpConnectorTests, HttpApiConnectorTests):**

1. Class structure:
   - [Collection("Integration")] attribute
   - Implement IClassFixture&lt;FileSimulatorFixture&gt;
   - Constructor taking FileSimulatorFixture, ITestOutputHelper
   - Private readonly fields: _fixture, _output, _logger (use NullLogger or TestOutputLogger)
   - Private TestRunId = Guid.NewGuid().ToString("N")[..8]

2. Helper methods:
   - SkipIfFileSimulatorUnavailable(): Skip.If(!_fixture.IsAvailable, "File-simulator not available")
   - GenerateTestFileName(string ext = ".csv"): returns $"test-{TestRunId}-{DateTime.UtcNow:yyyyMMddHHmmss}{ext}"
   - GenerateTestContent(int rows = 10): returns byte[] of CSV with id,name,amount,date columns
   - CreateAdminServer(): returns AdminServer with host, port, serverType from fixture
   - CreateCredentials(): returns ServerCredentials from fixture

3. Test methods with traits [Trait("Category", "Protocol")] and [Trait("Protocol", "{FTP|SFTP|HTTP}")]:
   - TestConnection_ReturnsSuccess()
   - WriteAndReadFile_RoundTripsSuccessfully() (skip for HTTP - read-only)
   - ListFiles_ReturnsMatchingFiles()

**FTP-specific (FtpConnectorTests.cs):**
   - Port 30021, credentials from Credentials.Ftp
   - All 3 test methods

**SFTP-specific (SftpConnectorTests.cs):**
   - Port 30022, credentials from Credentials.Sftp
   - All 3 test methods

**HTTP-specific (HttpApiConnectorTests.cs):**
   - Port 30088, credentials from Credentials.Http
   - TestConnection and ListFiles tests only (HTTP is read-only)
   - Use auth header in AdminServer: BasicAuth with username/password

Important: Each test must call SkipIfFileSimulatorUnavailable() first.
  </action>
  <verify>
dotnet build tests/IntegrationTests/DataProcessing.IntegrationTests.csproj
No build errors.

# Run tests with filter (will skip if file-simulator unavailable):
dotnet test tests/IntegrationTests --filter "Category=Protocol&amp;(Protocol=FTP|Protocol=SFTP|Protocol=HTTP)" --logger "console;verbosity=detailed"
  </verify>
  <done>FtpConnectorTests, SftpConnectorTests, HttpApiConnectorTests exist with TestConnection, Read/Write (where applicable), and ListFiles tests. Tests skip gracefully when file-simulator unavailable.</done>
</task>

</tasks>

<verification>
Run all new protocol tests:
```
dotnet test tests/IntegrationTests --filter "Category=Protocol" --logger "console;verbosity=detailed"
```

Expected outcomes:
- If FILE_SIMULATOR_IP not set and file-simulator unavailable: All tests skip with message "File-simulator not available"
- If file-simulator reachable: FTP/SFTP/HTTP tests pass

Verify build:
```
dotnet build tests/IntegrationTests/DataProcessing.IntegrationTests.csproj
```
</verification>

<success_criteria>
1. FileSimulatorFixture exists and manages file-simulator connectivity
2. FtpConnectorTests has 3 tests (TestConnection, WriteRead, ListFiles)
3. SftpConnectorTests has 3 tests (TestConnection, WriteRead, ListFiles)
4. HttpApiConnectorTests has 2 tests (TestConnection, ListFiles)
5. All tests skip gracefully when file-simulator unavailable
6. Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-protocol-test-coverage/03-01-SUMMARY.md`
</output>
