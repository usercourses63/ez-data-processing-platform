---
phase: 27-import-from-file
plan: 05
type: execute
wave: 3
depends_on:
  - 27-03
  - 27-04
files_modified:
  - src/Frontend/tests/e2e/import-from-file.spec.ts
  - src/Frontend/tests/webapp-testing/import_comprehensive_test.py
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
  - IMPORT-05
  - IMPORT-06

must_haves:
  truths:
    - "E2E test verifies Upload File button exists on list page and opens upload dialog"
    - "E2E test verifies CSV upload shows preview modal with data table and extraction checklist"
    - "E2E test verifies headerless CSV detection shows toggle and editable field names"
    - "E2E test verifies Continue button navigates to form with pre-filled fields"
    - "Webapp-testing comprehensive script verifies edge cases: malformed files, large files, encoding detection, constraint display"
    - "All tests run in --headed mode"
  artifacts:
    - path: "src/Frontend/tests/e2e/import-from-file.spec.ts"
      provides: "Playwright E2E tests for import flow"
      min_lines: 80
    - path: "src/Frontend/tests/webapp-testing/import_comprehensive_test.py"
      provides: "Python Playwright comprehensive UI tests"
      min_lines: 100
  key_links:
    - from: "src/Frontend/tests/e2e/import-from-file.spec.ts"
      to: "http://localhost:7000"
      via: "Playwright test against running frontend"
      pattern: "page.goto.*localhost:7000"
---

<objective>
Create comprehensive Playwright E2E tests and webapp-testing scripts for the Import from File feature, then deploy updated frontend and backend images to Kubernetes.

Purpose: Per CLAUDE.md testing requirements, every UI phase MUST run both Playwright and webapp-testing tools. This plan covers all IMPORT requirements with automated tests, plus production deployment of the updated images.

Output: E2E test spec, webapp-testing comprehensive script, deployed frontend and backend images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-import-from-file/27-CONTEXT.md
@.planning/phases/27-import-from-file/27-RESEARCH.md
@src/Frontend/playwright.config.ts
@src/Frontend/tests/e2e/sanity.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright E2E tests for import flow</name>
  <files>
    src/Frontend/tests/e2e/import-from-file.spec.ts
    src/Frontend/test-data/import-test-with-headers.csv
    src/Frontend/test-data/import-test-no-headers.csv
    src/Frontend/test-data/import-test.json
    src/Frontend/test-data/import-test.xml
  </files>
  <read_first>
    src/Frontend/playwright.config.ts
    src/Frontend/tests/e2e/sanity.spec.ts
    src/Frontend/src/components/datasource/import/ImportPreviewModal.tsx
    src/Frontend/src/components/datasource/import/ImportFromFileButton.tsx
    src/Frontend/src/pages/datasources/DataSourceList.tsx
  </read_first>
  <action>
1. Create test data files:
   - `import-test-with-headers.csv`: 10 rows with headers: name,age,email,phone,price,active (uses field names that trigger schemaAutoSuggest patterns for IMPORT-04 testing)
   - `import-test-no-headers.csv`: 10 rows same data structure but NO header row
   - `import-test.json`: JSON array of 10 objects with same fields
   - `import-test.xml`: XML with root `<records>` containing 10 `<record>` elements

2. Create `import-from-file.spec.ts` with these test cases:

   **Test: Upload File button exists on list page (IMPORT-01)**
   - Navigate to datasources list page
   - Assert "Upload File" button visible next to "Create Datasource" button
   - Both buttons should be in the same row/space

   **Test: CSV with headers shows preview modal (IMPORT-01, IMPORT-02, IMPORT-05)**
   - Click Upload File button
   - Upload `import-test-with-headers.csv` via Playwright file chooser
   - Wait for preview modal to appear
   - Assert modal title contains import/preview text
   - Assert data table shows 5 rows
   - Assert extraction checklist shows field names with types (name: string, age: integer, etc.)
   - Assert total row count in footer shows 10
   - Assert "Continue" button is visible

   **Test: Headerless CSV detection and editing (IMPORT-03, IMPORT-05)**
   - Upload `import-test-no-headers.csv`
   - Assert "First row is headers" toggle appears set to "No" (auto-detected)
   - Assert editable field name inputs appear (field_1, field_2, etc. as placeholders)
   - Type custom names: "name", "age", "email" in first 3 inputs
   - Assert extraction checklist updates with new field names

   **Test: JSON file preview (IMPORT-01, IMPORT-02)**
   - Upload `import-test.json`
   - Assert preview modal shows data table with correct columns
   - Assert extraction checklist shows inferred types

   **Test: XML file shows tree view (IMPORT-01, IMPORT-05)**
   - Upload `import-test.xml`
   - Assert tree view component is visible (collapsible nodes)
   - Assert data table also shows below the tree

   **Test: Continue navigates to form with pre-filled data (IMPORT-01, IMPORT-02, IMPORT-04, IMPORT-05)**
   - Upload CSV, wait for preview modal
   - Click "Continue"
   - Assert URL is `/datasources/new`
   - Assert form name field contains filename (minus extension)
   - Assert file type selector shows "CSV"
   - Assert encoding field shows detected encoding
   - Assert delimiter field shows detected delimiter
   - Assert schema tab has content (not empty)

   **Test: Smart constraints visible in extraction checklist (IMPORT-04)**
   - Upload CSV with headers (has email, phone, age, price columns)
   - In extraction checklist, verify "email" field shows "email format" or similar constraint
   - Verify "age" field shows range constraint
   - Verify "price" field shows "min: 0" constraint

   Use `page.setInputFiles()` for file upload. Handle Ant Design Upload component by targeting the hidden file input or the dragger zone.

   Run all tests with `--headed` flag per testing policy.
  </action>
  <verify>
    <automated>cd src/Frontend && npx playwright test tests/e2e/import-from-file.spec.ts --headed 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at `src/Frontend/tests/e2e/import-from-file.spec.ts`
    - Test data files exist in `src/Frontend/test-data/`
    - Tests cover IMPORT-01 (upload), IMPORT-02 (schema), IMPORT-03 (headerless), IMPORT-04 (constraints), IMPORT-05 (preview)
    - All tests use `page.setInputFiles()` for file upload
    - Tests run with `--headed` flag
    - At least 7 test cases covering the scenarios listed above
  </acceptance_criteria>
  <done>Playwright E2E tests pass in headed mode for CSV (with/without headers), JSON, XML upload flows, preview modal display, headerless field editing, and form pre-fill</done>
</task>

<task type="auto">
  <name>Task 2: Create webapp-testing comprehensive script and deploy to K8s</name>
  <files>
    src/Frontend/tests/webapp-testing/import_comprehensive_test.py
  </files>
  <read_first>
    src/Frontend/tests/webapp-testing/
    src/Frontend/tests/e2e/import-from-file.spec.ts
  </read_first>
  <action>
1. Create `import_comprehensive_test.py` Python Playwright script covering edge cases NOT in E2E spec:

   **Reconnaissance phase:**
   - Navigate to list page, discover Upload File button DOM structure
   - Document all testable selectors

   **Edge case tests:**
   - Upload empty CSV file -> verify error message appears
   - Upload CSV with only headers (no data rows) -> verify handles gracefully
   - Upload file > 10MB -> verify soft limit warning (if enforced)
   - Upload unsupported format (.txt) -> verify error message
   - Cancel modal -> verify modal closes and no navigation occurs
   - Re-upload different file -> verify preview updates
   - RTL layout: verify Upload File button and modal render correctly in Hebrew mode
   - Headerless CSV: toggle from No to Yes -> verify field names change to first row values
   - Headerless CSV: leave field names blank -> verify fallback to field_1, field_2
   - Extraction checklist: verify all constraint types display (email format, phone pattern, integer range, number min)
   - Preview table: verify exactly 5 rows shown even when file has > 5
   - Modal "Continue" -> form pre-fill -> verify EACH tab has correct values (switch tabs and check)
   - Archive file (if test .zip available): verify file list appears in modal

   **Run with:** `python tests/webapp-testing/import_comprehensive_test.py`

2. Build and deploy updated frontend image:
   ```bash
   cd src/Frontend
   docker build -t frontend:v0.4.0-import .
   minikube image load frontend:v0.4.0-import
   kubectl set image deployment/frontend frontend=frontend:v0.4.0-import -n ez-platform
   kubectl rollout status deployment/frontend -n ez-platform --timeout=120s
   ```

3. Build and deploy updated FileProcessorService (for IMPORT-06 fix):
   ```bash
   cd src/Services/FileProcessorService
   docker build -t fileprocessor:v0.4.0-import .
   minikube image load fileprocessor:v0.4.0-import
   kubectl set image deployment/fileprocessor fileprocessor=fileprocessor:v0.4.0-import -n ez-platform
   kubectl rollout status deployment/fileprocessor -n ez-platform --timeout=120s
   ```

4. Build and deploy updated Shared library (part of FileProcessorService image -- no separate deploy needed, but verify the Shared project is included in FileProcessorService's Docker build context).

5. Start port forwards and verify:
   ```bash
   powershell.exe -ExecutionPolicy Bypass -File "scripts/start-port-forwards.ps1"
   curl http://localhost:7000  # Frontend loads
   curl http://localhost:5008/health  # FileProcessor healthy
   ```
  </action>
  <verify>
    <automated>cd src/Frontend && python tests/webapp-testing/import_comprehensive_test.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `import_comprehensive_test.py` exists with at least 10 test scenarios
    - Script uses reconnaissance-first approach (discover DOM before asserting)
    - Covers edge cases: empty file, oversized file, cancel flow, RTL, toggle headerless
    - Frontend deployed as `frontend:v0.4.0-import` image
    - FileProcessorService deployed as `fileprocessor:v0.4.0-import` image
    - Both pods running and healthy after deploy
  </acceptance_criteria>
  <done>Webapp-testing comprehensive script passes all edge case tests, frontend and backend images deployed to K8s, port forwards working, feature accessible at localhost:7000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Import from File feature</name>
  <files>none</files>
  <action>
    Human verifies the complete Import from File feature end-to-end on the deployed application.

    What was built: Upload File button on list page, preview modal with extraction checklist and data table, headerless CSV editing, XML tree view, archive handling, form pre-fill, and backend CsvToJsonConverter fix.

    Verification steps:
    1. Open http://localhost:7000 and navigate to Data Sources list
    2. Verify "Upload File" button appears next to "Create Datasource"
    3. Click "Upload File" and upload a CSV file with headers
    4. Verify preview modal shows: extraction checklist with types+constraints, 5-row data table, total row count
    5. Click "Continue" and verify form is pre-filled (name, file type, encoding, delimiter, schema)
    6. Go back, upload a CSV without headers -- verify toggle appears, field name inputs editable
    7. Upload a JSON file -- verify preview works
    8. Upload an XML file -- verify tree view appears
    9. Switch to Hebrew -- verify RTL layout and translations
    10. Check completeness sidebar shows imported fields as green/complete
  </action>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>User types "approved" confirming the import feature works correctly end-to-end</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npx playwright test tests/e2e/import-from-file.spec.ts --headed` -- all tests pass
- `python src/Frontend/tests/webapp-testing/import_comprehensive_test.py` -- all tests pass
- `kubectl get pods -n ez-platform` -- frontend and fileprocessor pods running
- `curl http://localhost:7000` -- frontend accessible
- `curl http://localhost:5008/health` -- fileprocessor healthy
</verification>

<success_criteria>
- All Playwright E2E tests pass in headed mode (IMPORT-01 through IMPORT-05)
- Webapp-testing comprehensive script passes all edge cases
- Frontend and FileProcessorService deployed to K8s with new images
- Human verification confirms the complete import flow works end-to-end
- IMPORT-06 backend fix deployed and operational
</success_criteria>

<output>
After completion, create `.planning/phases/27-import-from-file/27-05-SUMMARY.md`
</output>
