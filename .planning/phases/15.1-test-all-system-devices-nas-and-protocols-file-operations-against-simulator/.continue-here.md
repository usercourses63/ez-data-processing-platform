---
phase: 15.1-test-all-system-devices-nas-and-protocols-file-operations-against-simulator
task: 2
total_tasks: 2
plan: 15.1-04
status: in_progress
last_updated: 2026-02-26T14:00:00.000Z
---

<current_state>
Phase 15.1 execution: Plans 01, 02, 03 fully complete. Plan 04 Task 1 complete. Plan 04 Task 2 in progress.

FIXED: Root cause bug — `ServersController.cs` `new BsonDocument(Dictionary<string, object>)` fails when values are `JsonElement` (System.Text.Json deserialization). Added `ConvertToBsonDocument` helper that properly converts JsonElement values to BsonValues. This fixed server creation with TypeSpecificConfig AND SFTP credential resolution.

FIXED: SFTP test path doubling — tests passed absolute `/upload/filename` but server BasePath is already `/upload`, causing double path `/upload/upload`. Changed to relative filenames.

FIXED: SFTP Dynamic credentials — UI registration doesn't store credentials in TypeSpecificConfig. Changed to always create via API with TypeSpecificConfig credentials.

Test results BEFORE fix: 3 passed / 23 failed / 60 skipped (server creation broke all tests)
Test results AFTER TypeSpecificConfig fix: 19 passed / 53 failed / 14 skipped (server creation works, but other issues remain)
SFTP path + credential fixes not yet re-tested.

DataSourceManagementService image: `datasource-management:v0.3.0-fileops2`
</current_state>

<completed_work>
- Plan 15.1-01 through 15.1-03: DONE
- Plan 15.1-04 Task 1: DONE (global setup/teardown + playwright config)
- BsonDocument JsonElement conversion fix in ServersController.cs (Create + Update)
- SFTP test path fixes (relative paths instead of absolute)
- SFTP Dynamic credentials fix (API creation instead of UI-only)
- Service rebuilt and deployed as `datasource-management:v0.3.0-fileops2`
</completed_work>

<remaining_work>
### Remaining test failures (53 failed, categorized):

1. **SFTP (12 Static + 13 Dynamic):** Path/credential fixes applied but not re-tested. Static may still fail with "Permission denied" on Write — the SFTP server may require different write path or permissions.

2. **HTTP/WebDAV (12 failures):** `HttpConnector` — "This instance has already started one or more requests. Properties can only be modified before sending the first request." Root cause: HttpClient is being reused after properties are modified. Need to check `HttpConnector.cs` — likely needs a new HttpClient per request or use HttpClientFactory properly.

3. **S3 (5 failures):** TestConnection fails → cascades to all file ops. Need to check S3 connector and how S3 servers are registered. The S3 server on file-simulator may need AccessKey/SecretKey in TypeSpecificConfig.

4. **Kafka (3 failures):** Both local and simulator Kafka TestConnection fail. May be connectivity issue (Kafka on EZ cluster uses port 9094 externally).

5. **NAS/NFS (5 failures):** Provisioning poll returns status=undefined. NAS provisioning API may not return expected status field. Also folder-type server for NFS bridge may have path issues.

6. **FTP Dynamic (3 failures):** Dynamic FTP TestConnection fails. May be similar TypeSpecificConfig credential issue or FTP connector bug.

### Priority order for next session:
1. Re-run tests to verify SFTP fixes
2. Fix HTTP/WebDAV HttpClient reuse bug
3. Fix S3 credential/connection handling
4. Fix FTP Dynamic credential handling (similar pattern to SFTP)
5. Investigate Kafka connectivity
6. Investigate NAS provisioning status field
7. Generate matrix report and complete phase
</remaining_work>

<decisions_made>
- Root cause was JsonElement→BsonDocument, NOT Direction enum as originally thought
- Added ConvertToBsonValue helper supporting all JsonElement types (string, number, bool, object, array, null)
- Test file paths should be relative to BasePath, not absolute
- Dynamic SFTP always creates via API (not UI) to ensure credentials in TypeSpecificConfig
</decisions_made>

<context>
The execution flow for resume:
1. Re-run SFTP tests only: `PROTOCOL_TESTS=true FILE_SIMULATOR_URL=http://172.17.89.141:30500 npx playwright test --project=protocols --grep "SFTP" --reporter=list`
2. Fix HttpConnector.cs HttpClient reuse bug
3. Fix S3/FTP test credential handling (same pattern as SFTP)
4. Rebuild and redeploy if backend changes needed
5. Re-run full suite
6. Review matrix report
7. Complete phase
</context>

<next_action>
Re-run SFTP-only tests to verify the path + credential fixes. Then tackle HttpConnector HttpClient reuse issue.
</next_action>
