---
phase: 15.1-test-all-system-devices-nas-and-protocols-file-operations-against-simulator
task: 2
total_tasks: 2
plan: 15.1-04
status: in_progress
last_updated: 2026-02-26T11:27:53.078Z
---

<current_state>
Phase 15.1 execution: Plans 01, 02, 03 fully complete. Plan 04 Task 1 complete (global setup/teardown + playwright config). Plan 04 Task 2 is checkpoint:human-verify — we ran the full protocol test suite and found 2 bugs that need fixing before approval.

DataSourceManagementService was rebuilt and deployed with new image `datasource-management:v0.3.0-fileops` containing the FileOperationsController.

Test results: 3 passed / 23 failed / 60 skipped out of 86 total tests.
</current_state>

<completed_work>

- Plan 15.1-01: Backend file operations REST proxy + Playwright test infrastructure — DONE (commits 9ea7c7a, 01ede9e, 1bc8b2f)
- Plan 15.1-02: FTP, SFTP, HTTP/WebDAV protocol test files — DONE (commits 54b6a98, 1a104f9, 527d65c)
- Plan 15.1-03: S3, Kafka, NAS/NFS protocol test files — DONE (commits 8ee77bf, d1f5b11, 7b4aa92)
- Plan 15.1-04 Task 1: Global setup/teardown + playwright config — DONE (commit 5a05be0)
- DataSourceManagementService rebuilt and deployed with FileOperationsController
- Full test suite executed against live clusters — results collected
</completed_work>

<remaining_work>

- Fix Bug 1: `ez-api-client.ts` — `createAdminServer` sends `Direction: "Input"` (string) but backend expects numeric enum (0=Input, 1=Output). This breaks ALL protocol tests at server creation step, cascading 60 skipped tests. Fix: change to `Direction: 0` in the helper.
- Fix Bug 2: `FileOperationsService.cs` — SFTP dynamic server operations fail with "SFTP username is required". The service doesn't resolve credentials from AdminServer's TypeSpecificConfig when calling the SFTP connector. Need to extract Username/Password from the server's stored config.
- Re-run full protocol test suite after fixes
- Review matrix report and approve checkpoint
- Complete Plan 15.1-04 Task 2 (spawn continuation agent with "approved")
- Run phase verification (gsd-verifier)
- Update roadmap
</remaining_work>

<decisions_made>

- Built FileOperationsController as lightweight REST proxy to existing IServerConnector interface — no new business logic
- Used file-based persistence for cross-worker matrix report sharing (report-generator.ts writes JSON fragments to temp dir, global-teardown aggregates)
- Deployed as `datasource-management:v0.3.0-fileops` image tag to avoid minikube image caching issues
- Used minikube's Docker daemon directly (Docker Desktop not running on this Hyper-V setup)
</decisions_made>

<blockers>

- Bug 1 (Direction enum): `src/Frontend/tests/e2e/helpers/ez-api-client.ts` sends string enum, backend expects numeric. Known pattern from MEMORY.md (Backend JSON Serialization section).
- Bug 2 (SFTP credentials): `src/Services/DataSourceManagementService/Services/FileOperationsService.cs` doesn't extract SFTP credentials from AdminServer.TypeSpecificConfig before calling connector.
</blockers>

<context>
The phase is about validating all 8 protocol types against the file-simulator. The test infrastructure (backend controller, helpers, fixtures, global setup/teardown, report generator) all work correctly. The 2 bugs are in the integration layer — how the test helpers communicate with the backend API. Both are targeted, small fixes.

The execution flow for resume:
1. Fix the 2 bugs (ez-api-client.ts + FileOperationsService.cs)
2. Rebuild and redeploy DataSourceManagementService
3. Re-run: `cd src/Frontend && PROTOCOL_TESTS=true FILE_SIMULATOR_URL=http://172.17.89.141:30500 npx playwright test --project=protocols --reporter=list`
4. Check matrix report at `src/Frontend/test-results/protocol-matrix-report.md`
5. If passing, resume Plan 15.1-04 checkpoint continuation with "approved"
6. Phase verification + roadmap update
</context>

<next_action>
Start with: Fix Direction enum in `src/Frontend/tests/e2e/helpers/ez-api-client.ts` — change `Direction: "Input"` to `Direction: 0` (and "Output" to 1, "Both" to 3). Then fix SFTP credential resolution in `src/Services/DataSourceManagementService/Services/FileOperationsService.cs`. Rebuild service image, redeploy, re-run tests.
</next_action>
