---
phase: 15.1-test-all-system-devices-nas-and-protocols-file-operations-against-simulator
task: 2
total_tasks: 2
plan: 15.1-04
status: in_progress
last_updated: 2026-02-26T17:45:00.000Z
---

<current_state>
Phase 15.1 execution: Plans 01-03 complete. Plan 04 Task 2 in progress.

### SESSION 13 PROGRESS:

#### NFSv4 Fix — CROSS-CLUSTER NFS NOW WORKING
- Root cause: NFSv3 requires mountd (port 32765) which is NOT exposed via NodePort on file-simulator
- NFSv3 "access denied" error was misleading — mountd was unreachable, not an auth issue
- **FIX:** Switch to NFSv4 which only needs port 2049 (already exposed via NodePort)
- `erichough/nfs-server` has `fsid=0` → NFSv4 pseudo-root is `/` (maps to `/data` on server)
- ExportPath changed from `/data` to `/`
- MountOptions changed from `['nfsvers=3', 'tcp', 'hard', 'intr', 'port=X', 'nolock']` to `['nfsvers=4', 'port=X', 'nolock']`
- writeFile timeout increased to 60s (first write to new NFSv4 mount takes ~57s for cache warmup)
- beforeAll timeout increased to 240s for NAS (PV/PVC + rollout + mount verification)

#### NAS Test Results:
- **Standalone run: 10/10 PASS** (both static + dynamic, all 4 formats)
- Full suite run: NAS timed out at 120s due to resource contention with 6 parallel workers
  - Fixed by increasing beforeAll timeout to 240s
  - Not re-run yet with the timeout fix

#### Full Protocol Suite Results (50/86 passed, 4 failed, 32 skipped):
- FTP Static: 5/5 PASS
- FTP Dynamic: FAIL (NodePort collision on simulator — simulator infra issue)
- SFTP Static: 5/5 PASS
- SFTP Dynamic: FAIL (needs localhost:7000 frontend port-forward for UI creation)
- HTTP/WebDAV: 18/18 PASS
- S3: 6/6 PASS (including skip for dynamic)
- Kafka: 3/3 PASS (including skip for dynamic)
- NAS Static: FAIL (beforeAll timeout — fixed to 240s, not re-run)
- NAS Dynamic: FAIL (beforeAll timeout — fixed to 240s, not re-run)
</current_state>

<files_modified_this_session>
- `src/Frontend/tests/e2e/protocols/nas-nfs.spec.ts`:
  - Changed ExportPath from `/data` to `/` (NFSv4 pseudo-root)
  - Changed MountOptions to NFSv4: `['nfsvers=4', 'port=X', 'nolock']`
  - Increased beforeAll timeout to 240s for both scenarios
- `src/Frontend/tests/e2e/helpers/ez-api-client.ts`:
  - Increased writeFile POST timeout to 60s
</files_modified_this_session>

<priority_for_next_session>
1. **Run full protocol suite** to confirm NAS passes with 240s timeout:
   ```
   cd src/Frontend
   EZ_API_URL=http://172.17.80.157:31205 FILE_SIMULATOR_URL=http://172.17.89.141:30500 PROTOCOL_TESTS=true npx playwright test tests/e2e/protocols/ --project=protocols --reporter=list
   ```
   NOTE: Must clean stale NAS PVs/PVCs and recreate deployments before each run.

2. **Write 15.1-04-SUMMARY.md** after full suite passes

3. **Known issues (not blockers):**
   - FTP Dynamic: NodePort collision on simulator (simulator-side fix needed)
   - SFTP Dynamic: needs frontend port-forward for UI-based creation
   - These are pre-existing issues, not new regressions
</priority_for_next_session>

<important_context>
- Minikube IP: 172.17.80.157, NodePort: 31205
- File-simulator IP: 172.17.89.141:30500
- NFSv4 requires ExportPath=`/` and nfsvers=4 mount option
- First NFSv4 write to fresh mount takes ~57s (cache warmup)
- After NAS test cleanup, must delete+recreate deployments (stale volume refs)
- Image: datasource-management:v0.3.0-fileops11
</important_context>
