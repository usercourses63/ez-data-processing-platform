---
phase: 09-frontend-development-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/tests/e2e/schema.spec.ts
  - src/Frontend/tests/e2e/rtl-visual.spec.ts
  - src/Frontend/tests/e2e/datasource.spec.ts
  - src/Frontend/playwright.config.ts
autonomous: true

must_haves:
  truths:
    - "E2E tests cover schema management user journey (create, edit, delete schemas)"
    - "RTL layout is verified with automated assertions (document.dir, text-align)"
    - "Visual regression screenshots capture RTL layouts for comparison"
  artifacts:
    - path: "src/Frontend/tests/e2e/schema.spec.ts"
      provides: "Schema management E2E tests"
      min_lines: 100
    - path: "src/Frontend/tests/e2e/rtl-visual.spec.ts"
      provides: "RTL visual regression tests with screenshots"
      min_lines: 80
  key_links:
    - from: "src/Frontend/tests/e2e/schema.spec.ts"
      to: "/datasources/:id/schema"
      via: "page.goto navigation"
      pattern: "schema"
    - from: "src/Frontend/tests/e2e/rtl-visual.spec.ts"
      to: "toHaveScreenshot"
      via: "Playwright visual comparison"
      pattern: "toHaveScreenshot"
---

<objective>
Expand Playwright E2E test coverage to include schema management and RTL visual regression testing.

Purpose: FE-02 (E2E tests cover critical user journeys) and FE-05 (RTL/Hebrew testing verified in E2E tests). Per user decisions, tests use real backend via port-forward and capture visual regression screenshots for RTL verification.

Output: New schema.spec.ts for schema management journey, rtl-visual.spec.ts for RTL layout verification with automated assertions and screenshots.
</objective>

<execution_context>
@c:\Users\UserC\.claude/get-shit-done/workflows/execute-plan.md
@c:\Users\UserC\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-development-workflow/09-RESEARCH.md
@.planning/phases/09-frontend-development-workflow/09-CONTEXT.md

# Existing E2E test patterns
@src/Frontend/tests/e2e/datasource.spec.ts
@src/Frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Schema Management E2E Tests</name>
  <files>src/Frontend/tests/e2e/schema.spec.ts</files>
  <action>
Create comprehensive E2E tests for schema management following existing datasource.spec.ts patterns:

1. Create `src/Frontend/tests/e2e/schema.spec.ts` with test.describe blocks:

**Schema Management Tests:**
```typescript
import { test, expect } from '@playwright/test';

test.describe('Schema Management', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    await page.waitForLoadState('networkidle');
  });

  // Tests to implement:
});
```

**Test cases to implement:**

a) `should navigate to schema editor from data source`:
   - Select existing data source from list
   - Navigate to Schema tab
   - Click "Edit Schema" button (Hebrew: "ערוך Schema")
   - Verify schema editor page loads

b) `should display schema editor with JSON structure`:
   - Navigate to schema editor page
   - Verify Monaco editor or vanilla-jsoneditor is visible
   - Check for schema template library button

c) `should add field to schema`:
   - Open schema editor
   - Click "Add Field" (Hebrew: "הוסף שדה")
   - Fill field name, type, validation rules
   - Verify field appears in schema structure

d) `should use regex helper for pattern validation`:
   - Open schema editor
   - Add string field with pattern validation
   - Click regex helper button
   - Select predefined pattern (email, phone, etc.)
   - Verify pattern applied to field

e) `should save schema changes`:
   - Make schema modification
   - Click save (Hebrew: "שמור")
   - Verify success message or navigation back
   - Re-open schema to verify changes persisted

f) `should apply schema template`:
   - Open schema template library
   - Select template (e.g., "Customer Data", "Transaction")
   - Apply template
   - Verify schema structure matches template

g) `should validate JSON schema syntax`:
   - Enter invalid JSON in editor
   - Verify error indicator or message
   - Fix syntax
   - Verify error cleared

**Hebrew selector patterns (from datasource.spec.ts):**
- Use Hebrew text in selectors: `page.getByText('הגדרת Schema')`
- Dual-language fallback: `{ name: /ערוך|edit/i }`
- Wait for networkidle after navigation
  </action>
  <verify>
    - `wc -l src/Frontend/tests/e2e/schema.spec.ts` shows 100+ lines
    - `grep -c "test\\(" src/Frontend/tests/e2e/schema.spec.ts` shows 7+ test cases
    - `grep -i "Schema" src/Frontend/tests/e2e/schema.spec.ts` shows schema-related assertions
  </verify>
  <done>Schema management E2E tests cover full user journey: navigate, view, edit, save, templates, regex helper</done>
</task>

<task type="auto">
  <name>Task 2: Create RTL Visual Regression Tests</name>
  <files>src/Frontend/tests/e2e/rtl-visual.spec.ts</files>
  <action>
Create RTL visual regression tests with automated layout assertions and screenshots:

1. Create `src/Frontend/tests/e2e/rtl-visual.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('RTL Visual Regression', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    await page.waitForLoadState('networkidle');

    // Verify RTL mode is active (app defaults to Hebrew)
    const dir = await page.evaluate(() => document.documentElement.dir);
    expect(dir).toBe('rtl');
  });

  // Tests to implement:
});
```

**Test cases to implement:**

a) `should have correct RTL layout attributes`:
   - Check `document.documentElement.dir === 'rtl'`
   - Check `document.documentElement.lang === 'he'`
   - Verify body text-align is 'right'
   - Verify table headers align right

b) `should render data source list in RTL`:
   - Navigate to /datasources
   - Verify table renders RTL
   - Take screenshot: `datasource-list-rtl.png`
   - Assert page direction

c) `should render data source form in RTL`:
   - Click "Add data source" button
   - Wait for form to load
   - Take screenshot: `datasource-form-rtl.png`
   - Verify form labels align right
   - Verify input fields align correctly

d) `should keep technical fields LTR`:
   - Navigate to Connection tab
   - Find path/URL input field
   - Verify input has LTR direction (check computed style or class)
   - Verify .ltr-field class applied to technical inputs

e) `should render schema editor correctly`:
   - Navigate to schema editor
   - Verify Monaco/JSON editor is LTR (code always LTR)
   - Verify surrounding UI is RTL
   - Take screenshot: `schema-editor-rtl.png`

f) `should render admin NAS devices tab in RTL`:
   - Navigate to admin page
   - Click NAS Devices tab
   - Verify RTL layout
   - Take screenshot: `nas-devices-rtl.png`

**Screenshot configuration:**
```typescript
await expect(page).toHaveScreenshot('filename.png', {
  fullPage: false,
  maxDiffPixelRatio: 0.02  // 2% tolerance for font rendering
});
```

**Layout assertion pattern (from RESEARCH.md):**
```typescript
// Check computed style for RTL alignment
const thAlign = await page.locator('.ant-table-thead th').first().evaluate(
  el => getComputedStyle(el).textAlign
);
expect(thAlign).toBe('right');
```
  </action>
  <verify>
    - `wc -l src/Frontend/tests/e2e/rtl-visual.spec.ts` shows 80+ lines
    - `grep -c "toHaveScreenshot" src/Frontend/tests/e2e/rtl-visual.spec.ts` shows 4+ screenshot captures
    - `grep -i "rtl" src/Frontend/tests/e2e/rtl-visual.spec.ts` shows RTL assertions
  </verify>
  <done>RTL visual regression tests verify layout with automated assertions and capture screenshots</done>
</task>

<task type="auto">
  <name>Task 3: Update Playwright Config for Screenshots</name>
  <files>
    src/Frontend/playwright.config.ts
    src/Frontend/tests/e2e/datasource.spec.ts
  </files>
  <action>
Update Playwright configuration for screenshot comparison and add RTL assertions to existing tests:

1. Update `src/Frontend/playwright.config.ts`:

Add snapshot configuration if not present:
```typescript
export default defineConfig({
  // ... existing config

  // Add snapshot settings
  expect: {
    toHaveScreenshot: {
      maxDiffPixelRatio: 0.02,  // 2% tolerance
      threshold: 0.2,           // Per-pixel threshold
    },
  },

  // Ensure snapshots directory
  snapshotDir: './tests/e2e/snapshots',

  // ... rest of config
});
```

Ensure projects include viewport for consistent screenshots:
```typescript
projects: [
  {
    name: 'chromium',
    use: {
      ...devices['Desktop Chrome'],
      viewport: { width: 1280, height: 720 },
    },
  },
],
```

2. Add RTL baseline assertion to `src/Frontend/tests/e2e/datasource.spec.ts`:

Add to the beginning of the test file after imports:
```typescript
test.describe('DataSource Management', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    await page.waitForLoadState('networkidle');

    // Verify RTL mode is active (app defaults to Hebrew)
    const dir = await page.evaluate(() => document.documentElement.dir);
    expect(dir).toBe('rtl');
  });

  // ... existing tests
});
```

This ensures existing tests also verify RTL baseline without breaking current functionality.
  </action>
  <verify>
    - `grep -i "toHaveScreenshot" src/Frontend/playwright.config.ts` shows snapshot config
    - `grep -i "snapshotDir" src/Frontend/playwright.config.ts` shows directory configured
    - `grep -i "document.*dir" src/Frontend/tests/e2e/datasource.spec.ts` shows RTL assertion added
  </verify>
  <done>Playwright configured for visual regression, existing tests have RTL baseline verification</done>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] schema.spec.ts exists with 7+ test cases covering schema CRUD
- [ ] rtl-visual.spec.ts exists with layout assertions and screenshots
- [ ] playwright.config.ts has snapshot configuration
- [ ] Existing datasource.spec.ts has RTL baseline assertion
- [ ] Tests run without TypeScript errors: `cd src/Frontend && npx playwright test --list`
- [ ] Screenshot baseline generated on first run (stored in tests/e2e/snapshots/)
</verification>

<success_criteria>
FE-02 and FE-05 complete:
- E2E tests cover critical user journeys (data source CRUD + schema management)
- RTL/Hebrew testing verified with automated layout assertions
- Visual regression screenshots capture RTL layouts
- Tests use real backend (port-forward pattern established)
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-development-workflow/09-02-SUMMARY.md`
</output>
