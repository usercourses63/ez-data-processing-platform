---
phase: 13-adminserver-feature-completion
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/Frontend/src/locales/he/translation.json
  - src/Frontend/src/locales/en/translation.json
autonomous: false

must_haves:
  truths:
    - "Hebrew translation keys exist for NAS auto-test messages"
    - "English translation keys exist for NAS auto-test messages"
    - "DemoDataGenerator with --use-simulator seeds data correctly (manual verification)"
    - "NAS auto-test in UI works with real API call (manual verification)"
  artifacts:
    - path: "src/Frontend/src/locales/he/translation.json"
      provides: "Hebrew translations for NAS auto-test"
      contains: "nasConnectionSuccess"
    - path: "src/Frontend/src/locales/en/translation.json"
      provides: "English translations for NAS auto-test"
      contains: "nasConnectionSuccess"
  key_links:
    - from: "ConnectionTab.tsx"
      to: "translation.json"
      via: "t('datasources.nasConnectionSuccess') lookup"
      pattern: "t\\('datasources\\.nasConnectionSuccess'\\)"
---

<objective>
Add translation keys and verify end-to-end functionality

Purpose: Complete the phase by adding missing translation keys for NAS auto-test messages and verifying that DemoDataGenerator simulator integration and NAS auto-test UI features work correctly together.

Output: Translation keys for NAS auto-test, verified DemoDataGenerator simulator seeding, verified NAS auto-test UI
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-adminserver-feature-completion/13-CONTEXT.md
@.planning/phases/13-adminserver-feature-completion/13-01-SUMMARY.md
@.planning/phases/13-adminserver-feature-completion/13-02-SUMMARY.md
@src/Frontend/src/locales/he/translation.json
@src/Frontend/src/locales/en/translation.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NAS auto-test translation keys</name>
  <files>
    src/Frontend/src/locales/he/translation.json
    src/Frontend/src/locales/en/translation.json
  </files>
  <action>
Read existing translation.json files to find the datasources section.

Add to Hebrew translation.json under "datasources" key:
```json
"nasConnectionSuccess": "החיבור להתקן NAS הצליח",
"nasConnectionFailed": "החיבור להתקן NAS נכשל",
"nasTestError": "שגיאה בבדיקת חיבור NAS",
"nasTestInProgress": "בודק חיבור להתקן NAS..."
```

Add to English translation.json under "datasources" key:
```json
"nasConnectionSuccess": "NAS device connection successful",
"nasConnectionFailed": "NAS device connection failed",
"nasTestError": "Error testing NAS connection",
"nasTestInProgress": "Testing NAS device connection..."
```

Ensure keys are added in alphabetical order within the datasources section to maintain consistency.
  </action>
  <verify>
JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('src/Frontend/src/locales/he/translation.json'))"`
  </verify>
  <done>
Both Hebrew and English translation files have nasConnectionSuccess, nasConnectionFailed, nasTestError, nasTestInProgress keys
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DemoDataGenerator simulator integration</name>
  <what-built>
DemoDataGenerator with --use-simulator flag that queries file-simulator Control API and seeds AdminServer + NasDevice entities.
  </what-built>
  <how-to-verify>
**Prerequisites:**
- File-simulator cluster running: `minikube profile file-simulator && minikube status`
- EZ platform cluster running: `minikube profile minikube && minikube status`
- Port forwarding active for file-simulator Control API (30500)
- Port forwarding active for MongoDB (27017)

**Test Steps:**

1. Run DemoDataGenerator with simulator mode:
   ```powershell
   cd tools/DemoDataGenerator
   dotnet run -- --use-simulator --simulator-url=http://file-simulator.local:30500
   ```

2. Expected output should show:
   - "Mode: SIMULATOR"
   - "Checking file-simulator health..."
   - "Wiping existing AdminServers and NasDevices..."
   - "Discovered {N} servers from file-simulator"
   - "Created AdminServer: file-simulator FTP ..." for each protocol
   - "Created NasDevice: nas-input-1 (Input)" for NAS servers
   - Summary with counts

3. Verify in MongoDB (optional):
   ```powershell
   mongosh --eval "db.AdminServer.find().count()"
   mongosh --eval "db.NasDevice.find().count()"
   ```

**If file-simulator not running**, verify fallback behavior:
   ```powershell
   dotnet run -- --use-simulator
   ```
   Should fail fast with clear error: "File-simulator not reachable"
  </how-to-verify>
  <resume-signal>
Type "approved" if DemoDataGenerator seeds data from simulator correctly, or describe any issues encountered
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify NAS auto-test in UI</name>
  <what-built>
NAS device selection auto-test in ConnectionTab and DestinationEditorModal that triggers connection test when device is selected.
  </what-built>
  <how-to-verify>
**Prerequisites:**
- Frontend running: `cd src/Frontend && npm run dev`
- Backend DataSourceManagementService running with NasDevicesController
- At least one NasDevice in database (seeded by DemoDataGenerator or manually created)
- Port forwarding active for DataSourceManagementService API (5001)

**Test Steps for ConnectionTab:**

1. Navigate to http://localhost:7000/datasources/create (or edit existing)
2. In Connection tab, select "NAS - Network Attached Storage" protocol
3. Select a NAS device from dropdown
4. **Expected:**
   - Loading spinner appears while testing
   - Success toast (green): "החיבור להתקן NAS הצליח" if device is reachable
   - Warning toast (yellow): "החיבור להתקן NAS נכשל" if device not reachable
   - Error toast (red): "שגיאה בבדיקת חיבור NAS" if API call fails

**Test Steps for DestinationEditorModal:**

1. Navigate to DataSource edit page, go to Output tab
2. Click "Add Destination" or edit existing destination
3. Select "NAS" as destination type
4. Select a NAS device from dropdown
5. **Expected:** Same auto-test behavior as ConnectionTab

**Verify RTL:**
6. Switch to Hebrew language if not already
7. Verify messages display correctly in RTL layout
  </how-to-verify>
  <resume-signal>
Type "approved" if NAS auto-test works correctly in both ConnectionTab and DestinationEditorModal, or describe any issues
  </resume-signal>
</task>

</tasks>

<verification>
1. Translation files valid JSON and contain new keys
2. DemoDataGenerator --use-simulator successfully seeds from file-simulator API
3. NAS auto-test in ConnectionTab shows success/warning/error messages
4. NAS auto-test in DestinationEditorModal shows success/warning/error messages
5. Both components show loading state during auto-test
</verification>

<success_criteria>
- Translation keys present in both he/en translation files
- DemoDataGenerator --use-simulator mode works with file-simulator API
- NAS auto-test UI triggers on selection in both forms
- User has approved both verification checkpoints
</success_criteria>

<output>
After completion, create `.planning/phases/13-adminserver-feature-completion/13-03-SUMMARY.md`
</output>
