---
phase: 19-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-package/docs-docusaurus/docs/admin.md
  - release-package/docs-docusaurus/docs/architecture/system-architecture.md
  - release-package/docs-docusaurus/docs/changelog.md
  - release-package/docs-docusaurus/docs/release-notes.md
  - release-package/docs-docusaurus/docs/user-guide-he.mdx
  - release-package/docs-docusaurus/docs/components/system-monitoring.mdx
  - release-package/docs-docusaurus/docs/components/device-health.mdx
  - release-package/docs-docusaurus/docs/components/index.md
  - release-package/docs-docusaurus/sidebars.js
autonomous: true
requirements:
  - DOC-06

must_haves:
  truths:
    - "All v0.2.0 features from phases 14-18 are documented in changelog and release notes"
    - "New component docs exist for System Monitoring and Device Health features"
    - "Admin guide covers SignalR monitoring, device health, and OTEL observability"
    - "Sidebar no longer shows Legacy labels and includes Monitoring category"
    - "Hebrew user guide includes v0.2.0 monitoring and device health features"
  artifacts:
    - path: "release-package/docs-docusaurus/docs/components/system-monitoring.mdx"
      provides: "System Monitoring component documentation"
      contains: "SignalR"
    - path: "release-package/docs-docusaurus/docs/components/device-health.mdx"
      provides: "Device Health component documentation"
      contains: "DeviceHealth"
    - path: "release-package/docs-docusaurus/docs/changelog.md"
      provides: "Complete v0.2.0 changelog"
      contains: "SignalR Real-Time Updates"
    - path: "release-package/docs-docusaurus/docs/release-notes.md"
      provides: "Complete v0.2.0 release notes"
      contains: "Device Health Monitoring"
    - path: "release-package/docs-docusaurus/sidebars.js"
      provides: "Updated sidebar without Legacy labels"
  key_links:
    - from: "release-package/docs-docusaurus/sidebars.js"
      to: "release-package/docs-docusaurus/docs/components/system-monitoring.mdx"
      via: "sidebar category item reference"
      pattern: "components/system-monitoring"
    - from: "release-package/docs-docusaurus/sidebars.js"
      to: "release-package/docs-docusaurus/docs/components/device-health.mdx"
      via: "sidebar category item reference"
      pattern: "components/device-health"
---

<objective>
Update all Docusaurus documentation content to reflect v0.2.0 features from phases 14-18, add new component documentation pages, restructure sidebar navigation, and rewrite changelog/release-notes.

Purpose: Ensure documentation accurately reflects all shipped v0.2.0 features before the container is built and deployed.
Output: Updated doc content files, new component docs, restructured sidebar.
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-documentation-update/19-CONTEXT.md
@.planning/phases/19-documentation-update/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new component documentation pages and update sidebar</name>
  <files>
    release-package/docs-docusaurus/docs/components/system-monitoring.mdx,
    release-package/docs-docusaurus/docs/components/device-health.mdx,
    release-package/docs-docusaurus/docs/components/index.md,
    release-package/docs-docusaurus/sidebars.js
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/components/nas-devices.mdx,
    release-package/docs-docusaurus/docs/components/index.md,
    release-package/docs-docusaurus/sidebars.js,
    src/Frontend/src/pages/SystemMonitoringPage.tsx,
    src/Frontend/src/components/monitoring/,
    .planning/phases/19-documentation-update/19-CONTEXT.md,
    .planning/STATE.md
  </read_first>
  <action>
**Create `system-monitoring.mdx`** in MDX format matching nas-devices.mdx style:
- Frontmatter: `sidebar_position: 5`, `title: System Monitoring`, `description: Real-time system monitoring with SignalR integration`, `last_verified: 2026-03-17`
- Import `Tabs` and `TabItem` from `@theme/Tabs`
- Sections: Features, Data Model (TypeScript interfaces for SystemStatus, ServiceStatus, KafkaStatus), SignalR Integration (connection lifecycle, dual-speed broadcast 5s fast / 30s slow, connection recovery), Tabs Overview (Services, Kafka, Traces, Events, Pipeline), Connection Status (visual indicator showing connected/disconnected/reconnecting), Fallback Behavior (React Query polling when SignalR unavailable), API Reference table (SignalR hub endpoint `/hubs/monitoring`, methods: `ReceiveSystemStatus`, `ReceiveKafkaStatus`, `ReceiveTraces`, `ReceiveEvents`, `ReceivePipelineStatus`)

**Create `device-health.mdx`** in MDX format matching nas-devices.mdx style:
- Frontmatter: `sidebar_position: 6`, `title: Device Health`, `description: Device health monitoring for NAS devices and admin servers`, `last_verified: 2026-03-17`
- Import `Tabs` and `TabItem` from `@theme/Tabs`
- Sections: Features, Data Model (TypeScript interfaces for DeviceHealthStatus, HealthCheckResult -- fields: DeviceName, DeviceType, Status enum Healthy/Degraded/Down, Latency, LastChecked, ConsecutiveFailures), Health Check Configuration (Quartz.NET job every 30s, consecutive failure tracking: 0=Healthy, 1-2=Degraded, 3+=Down, NAS degraded threshold 2000ms, AdminServer degraded threshold 5000ms), UI Components (status cards with color coding, summary bar, React Query polling at 15s), API Reference table (`GET /api/v1/health/devices` returns all device health statuses, `GET /api/v1/health/devices/{id}` returns single device)

**Update `components/index.md`:** Add entries for System Monitoring and Device Health to the component list, with brief descriptions matching the existing entry format.

**Update `sidebars.js`:**
1. Remove `(Legacy)` from Architecture category label (change to just `'Architecture'`)
2. Remove `(Legacy)` from Deployment category label (change to just `'Deployment'`)
3. Add `'components/system-monitoring'` and `'components/device-health'` to the Components category items array (after `'components/nas-devices'`)
4. Add a new Monitoring category AFTER Components:
```javascript
{
  type: 'category',
  label: 'Monitoring',
  collapsed: false,
  items: [
    'components/system-monitoring',
    'components/device-health',
  ],
},
```
5. Remove the commented-out Guides category block (lines 64-74 of current file)
  </action>
  <verify>
    <automated>cd C:/Users/UserC/source/repos/EZ/release-package/docs-docusaurus && node -e "const s = require('./sidebars.js'); const items = JSON.stringify(s); console.log('has system-monitoring:', items.includes('system-monitoring')); console.log('has device-health:', items.includes('device-health')); console.log('no Legacy:', !items.includes('Legacy'));"</automated>
  </verify>
  <acceptance_criteria>
    - release-package/docs-docusaurus/docs/components/system-monitoring.mdx exists and contains `import Tabs from '@theme/Tabs'` and `SignalR` and `sidebar_position: 5`
    - release-package/docs-docusaurus/docs/components/device-health.mdx exists and contains `import Tabs from '@theme/Tabs'` and `DeviceHealth` and `sidebar_position: 6`
    - release-package/docs-docusaurus/sidebars.js contains `'components/system-monitoring'` and `'components/device-health'`
    - release-package/docs-docusaurus/sidebars.js does NOT contain the string `(Legacy)`
    - release-package/docs-docusaurus/sidebars.js contains `label: 'Monitoring'`
    - release-package/docs-docusaurus/docs/components/index.md contains `system-monitoring` and `device-health`
  </acceptance_criteria>
  <done>Two new component doc pages created matching existing MDX style, sidebar restructured with Monitoring category and no Legacy labels</done>
</task>

<task type="auto">
  <name>Task 2: Update admin guide, architecture, changelog, release notes, and Hebrew guide</name>
  <files>
    release-package/docs-docusaurus/docs/admin.md,
    release-package/docs-docusaurus/docs/architecture/system-architecture.md,
    release-package/docs-docusaurus/docs/changelog.md,
    release-package/docs-docusaurus/docs/release-notes.md,
    release-package/docs-docusaurus/docs/user-guide-he.mdx
  </files>
  <read_first>
    release-package/docs-docusaurus/docs/admin.md,
    release-package/docs-docusaurus/docs/architecture/system-architecture.md,
    release-package/docs-docusaurus/docs/changelog.md,
    release-package/docs-docusaurus/docs/release-notes.md,
    release-package/docs-docusaurus/docs/user-guide-he.mdx,
    .planning/STATE.md,
    .planning/phases/19-documentation-update/19-CONTEXT.md
  </read_first>
  <action>
**Update `admin.md`:** Add three new sections to the Table of Contents and document body:

1. **SignalR Real-Time Monitoring** (after Monitoring & Alerts section):
   - SignalR hub at `/hubs/monitoring` with MonitoringBroadcaster hosted service
   - Dual-speed broadcast: 5-second interval for fast-changing data (services, kafka), 30-second for slow data (traces, events)
   - CORS configuration: `SetIsOriginAllowed` + `AllowCredentials` for WebSocket
   - Short-lived Kafka AdminClient per health check call (avoids connection pooling issues)
   - Frontend connection status dot in ClusterHeader
   - PascalCase mapper functions for backend-to-frontend data bridge
   - React Query conditional polling fallback when SignalR disconnected

2. **Device Health Monitoring** (after SignalR section):
   - Quartz.NET health check job running every 30 seconds
   - Consecutive failure tracking: 0 = Healthy, 1-2 = Degraded, 3+ = Down
   - NAS degraded threshold: 2000ms latency
   - AdminServer degraded threshold: 5000ms latency
   - MongoDB TTL history for health check results
   - REST API: `GET /api/v1/health/devices`, `GET /api/v1/health/devices/{id}`
   - Frontend: Device Health tab (HeartOutlined icon) between overview and pods tabs, 15s React Query polling

3. **OTEL Observability** (after Device Health section):
   - All 8 deployed microservices verified with 3 OTEL signals (24/24 checks)
   - Structured logs via Serilog -> OTEL Collector -> Elasticsearch (index: `dataprocessing-logs`)
   - Distributed traces via OTEL Collector -> Jaeger (port 16686)
   - Metrics via OTEL Collector -> Prometheus System (9090) + Prometheus Business (9091)
   - All k8s deployments include `OpenTelemetry__OtlpEndpoint` env var
   - DataSourceManagement rebuilt as v0.2.0-otel with Serilog logging

Update the **Last Updated** date to `March 2026` and version to `0.2.0`.

**Update `system-architecture.md`:** Add v0.2 architecture changes in-place:
- Add SignalR hub to service architecture diagram/description: `MonitoringHub` receives broadcasts from `MonitoringBroadcaster` hosted service
- Add OTEL pipeline description: Services -> OTEL Collector (4317/4318) -> Prometheus/Elasticsearch/Jaeger
- Add Device Health subsystem: Quartz.NET job -> DeviceHealthService -> MongoDB TTL history -> REST API -> Frontend polling
- Add two-tier logging: Infrastructure (Fluent Bit -> ES) vs Application (Serilog -> OTEL Collector -> ES/Jaeger/Prometheus)
- Update service count if needed and mention SignalR as communication pattern alongside REST/Kafka/RabbitMQ

**Rewrite `changelog.md` v0.2.0 section** from scratch. Keep the `[0.2.0]` header and Keep a Changelog format. Add these MISSING sections under `### Added`:

```markdown
#### OTEL Observability (Phase 14)
- All 8 deployed microservices verified: structured logs, distributed traces, metrics
- DataSourceManagement service rebuilt with Serilog OTEL logging (v0.2.0-otel)
- Automated OTEL verification script: 24/24 checks pass (8 services x 3 signals)
- OpenTelemetry__OtlpEndpoint env var added to all k8s deployments

#### Device Health Monitoring (Phase 15)
- Quartz.NET health check job (30-second interval)
- Consecutive failure tracking: Healthy (0) / Degraded (1-2) / Down (3+)
- Configurable latency thresholds: NAS 2000ms, AdminServer 5000ms
- MongoDB TTL storage for health check history
- REST API: GET /api/v1/health/devices
- Frontend Device Health tab with status cards, summary bar, 15s polling
- HeartOutlined icon in System Monitoring page tab bar

#### Protocol File Operations Testing (Phase 15.1)
- Backend file operation REST proxy endpoints (List/Read/Write) for all server protocols
- Playwright test infrastructure for protocol x format matrix testing
- Static device testing (Scenario A): all protocols tested against file-simulator
- Dynamic device testing (Scenario B): FTP, SFTP, NAS created via simulator API
- Protocol x format matrix report with pass/fail per combination

#### SignalR Real-Time Updates (Phase 16)
- MonitoringBroadcaster singleton hosted service with dual-speed broadcast (5s/30s)
- SignalR hub at /hubs/monitoring for WebSocket connections
- CORS: SetIsOriginAllowed + AllowCredentials for SignalR WebSocket
- Short-lived Kafka AdminClient per call (avoids connection pooling)
- Frontend useMonitoringHub hook with connection recovery
- Connection status dot indicator in ClusterHeader
- React Query conditional polling fallback when SignalR disconnected
- PascalCase mapper functions for backend-frontend data bridge

#### File-Simulator Integration (Phase 17)
- DemoDataGenerator --use-simulator mode for multi-protocol testing
- FileSimulatorClient for REST API communication
- FileGeneratorService for test data generation
- FileUploadService for multi-protocol file upload
- ServerCredentials.FromBsonDocument for credential resolution
- Dynamic server creation via --create-servers flag

#### E2E Validation (Phase 18)
- Playwright E2E test infrastructure with beforeAll/afterAll setup modules
- Protocol-aware test fixtures matching DemoDataGenerator SimpleTransaction schema
- Ant Design RTL-safe tab interaction (dispatchEvent pattern)
- Flexible assertions for loading/empty/data states
- E2E validation report (E2E-VALIDATION-REPORT.md)
- Pipeline completion and output comparison tests
```

Keep existing NAS, Protocol Testing, CI/CD, Documentation, and Testing sections but update dates and add any missing items.

**Rewrite `release-notes.md` v0.2.0 section** to include all phases:
- Update the Highlights paragraph to mention: NAS device management, OTEL observability verification, device health monitoring, SignalR real-time dashboard, file-simulator integration, and E2E validation
- Add new feature sections for each phase (14-18) following the existing format (heading + bullet points)
- Add new API endpoints table rows for: `/api/v1/health/devices` (GET), `/hubs/monitoring` (SignalR hub)
- Update the status line to `Status: Production Release`

**Update `user-guide-he.mdx`:** Add Hebrew sections for:
- System Monitoring (ניטור מערכת): SignalR real-time updates, connection status, tabs overview
- Device Health (בריאות מכשירים): health status cards, latency thresholds, degraded/down indicators
- OTEL Observability: brief mention of log/trace/metric visibility in Grafana/Jaeger
- Keep all technical terms (SignalR, OTEL, Kafka, etc.) in English within Hebrew text as is the existing pattern in the file
  </action>
  <verify>
    <automated>cd C:/Users/UserC/source/repos/EZ && grep -c "SignalR" release-package/docs-docusaurus/docs/admin.md && grep -c "Phase 16" release-package/docs-docusaurus/docs/changelog.md && grep -c "Device Health" release-package/docs-docusaurus/docs/release-notes.md && grep -c "ניטור" release-package/docs-docusaurus/docs/user-guide-he.mdx</automated>
  </verify>
  <acceptance_criteria>
    - release-package/docs-docusaurus/docs/admin.md contains `## SignalR Real-Time Monitoring` and `## Device Health Monitoring` and `## OTEL Observability`
    - release-package/docs-docusaurus/docs/admin.md contains `MonitoringBroadcaster` and `Quartz.NET` and `24/24`
    - release-package/docs-docusaurus/docs/changelog.md contains `Phase 14` and `Phase 15` and `Phase 16` and `Phase 17` and `Phase 18`
    - release-package/docs-docusaurus/docs/changelog.md contains `SignalR Real-Time Updates` and `Device Health Monitoring` and `OTEL Observability`
    - release-package/docs-docusaurus/docs/release-notes.md contains `Device Health Monitoring` and `SignalR` and `OTEL`
    - release-package/docs-docusaurus/docs/release-notes.md contains `/api/v1/health/devices`
    - release-package/docs-docusaurus/docs/architecture/system-architecture.md contains `SignalR` and `MonitoringHub`
    - release-package/docs-docusaurus/docs/user-guide-he.mdx contains `ניטור מערכת` (System Monitoring in Hebrew) and `בריאות מכשירים` (Device Health in Hebrew)
  </acceptance_criteria>
  <done>All documentation content updated: admin guide has 3 new sections (SignalR, Device Health, OTEL), changelog covers phases 14-18, release notes reflect complete v0.2.0, system architecture includes v0.2 changes, Hebrew guide includes monitoring features</done>
</task>

</tasks>

<verification>
1. All doc files exist and are non-empty
2. Sidebar references resolve to existing files (system-monitoring.mdx, device-health.mdx)
3. Changelog covers all 6 missing phase features (14, 15, 15.1, 16, 17, 18)
4. No "(Legacy)" labels remain in sidebar
5. Hebrew user guide mentions monitoring features
</verification>

<success_criteria>
- DOC-06 satisfied: All Docusaurus content reflects v0.2.0 features
- 2 new component docs created (system-monitoring.mdx, device-health.mdx)
- Sidebar restructured with Monitoring category, no Legacy labels
- Changelog and release notes cover phases 14-18 completely
- Hebrew user guide updated with v0.2.0 features
</success_criteria>

<output>
After completion, create `.planning/phases/19-documentation-update/19-01-SUMMARY.md`
</output>
