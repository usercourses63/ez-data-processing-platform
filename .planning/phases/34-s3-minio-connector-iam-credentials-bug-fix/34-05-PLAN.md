---
phase: 34-s3-minio-connector-iam-credentials-bug-fix
plan: 05
type: tdd
wave: 3
depends_on: ["34-03"]
files_modified:
  - src/Frontend/src/pages/admin/components/ServerModal.test.tsx
  - src/Frontend/tests/e2e/s3-server.spec.ts
  - src/Frontend/tests/webapp-testing/s3_server_comprehensive_test.py
autonomous: true
requirements: [SC-02, SC-07]
must_haves:
  truths:
    - "A Vitest test proves the ServerModal S3 branch renders a masked Secret Key field and that handleSubmit packs PascalCase TypeSpecificConfig"
    - "A Playwright spec creates an S3 server through the UI, enters Access/Secret keys, runs Test Connection, and asserts a success result"
    - "A webapp-testing Python Playwright suite (s3_server_comprehensive_test.py) verifies masked-secret DOM rendering, edit-blank-keeps-secret, cancel-discards-credentials, Test-Connection messaging, and field tooltips — the edge cases unit/E2E miss"
  artifacts:
    - path: src/Frontend/src/pages/admin/components/ServerModal.test.tsx
      provides: "Vitest unit coverage of the S3 branch field rendering + submit packing"
      contains: "TypeSpecificConfig"
    - path: src/Frontend/tests/e2e/s3-server.spec.ts
      provides: "E2E create S3 server → Test Connection → success"
      contains: "test-connection"
    - path: src/Frontend/tests/webapp-testing/s3_server_comprehensive_test.py
      provides: "Python Playwright comprehensive UI verification of the S3 server form masked-credential edge cases"
      contains: "s3_server_comprehensive_test"
  key_links:
    - from: "ServerModal.test.tsx"
      to: "handleSubmit TypeSpecificConfig"
      via: "assert mutation called with PascalCase AccessKey/SecretKey/Bucket"
      pattern: "AccessKey"
    - from: "s3_server_comprehensive_test.py"
      to: "Secret Key input rendering"
      via: "DOM inspection asserts input type=password and value not exposed in plaintext attributes"
      pattern: "password"
---

<objective>
Frontend test tier — three complementary layers: (1) Vitest unit coverage of the S3 branch (masked secret rendering + PascalCase `TypeSpecificConfig` packing), (2) a Playwright E2E that creates an S3 server through the UI and runs Test Connection to success, and (3) a `webapp-testing` Python Playwright comprehensive suite covering the masked-credential edge cases (DOM masking, edit-blank-keeps-secret, cancel discards, Test-Connection messaging, tooltips) that unit/E2E do not exercise.

Purpose: SC-02 (UI fields render correctly), SC-07 (E2E passes). CLAUDE.md mandates BOTH testing tools for any frontend UI work: `/gsd:add-tests` (Vitest + Playwright) AND the `webapp-testing` Python skill (comprehensive UI verification with edge cases). This plan satisfies both.

Output: `ServerModal.test.tsx` (Vitest), `tests/e2e/s3-server.spec.ts` (Playwright), and `tests/webapp-testing/s3_server_comprehensive_test.py` (webapp-testing skill).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-RESEARCH.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-VALIDATION.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-03-SUMMARY.md
@src/Frontend/src/pages/admin/components/ServerModal.tsx
@src/Frontend/tests/e2e/sanity.spec.ts
@src/Frontend/tests/webapp-testing/clone_comprehensive_test.py
</context>

<feature>
  <name>ServerModal S3 branch unit + E2E coverage</name>
  <files>src/Frontend/src/pages/admin/components/ServerModal.test.tsx, src/Frontend/tests/e2e/s3-server.spec.ts</files>
  <behavior>
    Vitest (ServerModal.test.tsx):
    - Selecting ServerType "s3" renders an Access Key input, a masked Secret Key field (input[type=password] / Input.Password), Bucket, Region, and the path-style switch — all carrying className "ltr-field" on the technical inputs.
    - Submitting the s3 form calls the create mutation with requestData.TypeSpecificConfig containing exactly { AccessKey, SecretKey, Bucket, Region, ForcePathStyle, UseHttp } (PascalCase).
    - In edit mode, the Secret Key field shows the masked sentinel "********" and is NOT pre-filled with any real value.

    Playwright (s3-server.spec.ts):
    - Navigate to System Settings → Input Servers, open Create Server, choose type s3, fill Name/Host/Port/AccessKey/SecretKey/Bucket/Region, save.
    - Trigger Test Connection on the created server and assert a success indication (no "IAM credentials is missing" text; success toast/status).
    - Clean up the created server via API in try/finally (pattern from sanity.spec.ts).
  </behavior>
  <implementation>
    Author `ServerModal.test.tsx` with Vitest + @testing-library/react (mirror any existing *.test.tsx near components; mock `@tanstack/react-query` mutations and `getServerTypes` to include an s3 type). Assert on `requestData.TypeSpecificConfig` by spying the `createServer` mutation arg. Author `s3-server.spec.ts` in `tests/e2e/` reusing `antTabClick` / `extractId` helpers from `sanity.spec.ts`; baseURL `http://localhost:7000` (playwright.config.ts). The E2E "Test Connection success" assertion REQUIRES live backends + MinIO — gate it to skip when the test-connection endpoint is unreachable (conditional skip, like the phase-29/31 data-dependent E2E pattern) so this plan stays green pre-live; the authoritative live pass is 34-06. Follow RED→GREEN with atomic commits.
  </implementation>
</feature>

<feature>
  <name>webapp-testing comprehensive UI verification of S3 server form (masked-credential edge cases)</name>
  <files>src/Frontend/tests/webapp-testing/s3_server_comprehensive_test.py</files>
  <read_first>
    - `src/Frontend/tests/webapp-testing/clone_comprehensive_test.py` — established webapp-testing skeleton: `report(test_name, passed, detail)` collector, `sync_playwright()` launch, BASE_URL constant, per-group print banners, final pass/fail summary + non-zero exit on any failure.
    - `src/Frontend/tests/webapp-testing/recon.py` — reconnaissance-first DOM-discovery helper conventions.
    - `src/Frontend/src/pages/admin/components/ServerModal.tsx` — actual field labels, `data-testid`s, Input.Password usage, masked sentinel `"********"`, Test Connection button, and tooltip/help wiring for the S3 branch (authored in 34-03). Use the real selectors from this file, not guesses.
    - `src/Frontend/tests/e2e/sanity.spec.ts` — navigation path to System Settings → Input Servers and the API cleanup pattern (port 7000 frontend; reuse the same base host).
  </read_first>
  <behavior>
    Reconnaissance-first: open the Create Server modal, select type s3, and dump the resolved selectors for the S3 fields BEFORE asserting (mirror recon.py), so the test binds to real DOM.

    Edge-case coverage (the cases Vitest/E2E do not exercise):
    - T1 Secret masking (DOM): the Secret Key input renders as `type="password"` (or Ant `Input.Password` with the eye toggle); inspect the input's attributes and assert the typed secret value is NOT present in any plaintext attribute (no `value=`, `placeholder=`, `aria-*`, or `title=` exposing the secret) while the input is masked.
    - T2 Edit keeps stored secret on blank: create an s3 server with a secret, reopen it in Edit mode, leave the Secret Key field blank (untouched, showing the `********` sentinel), save, then re-fetch the server via API and assert the stored secret is unchanged (server still test-connects / secret is not blanked). The form MUST NOT overwrite the stored secret with an empty string.
    - T3 Cancel discards credentials: open Create (or Edit), type Access/Secret keys, click Cancel; reopen the modal and assert the previously typed credentials are gone (fields empty / no leaked state).
    - T4 Test-Connection messaging: trigger Test Connection and assert the success path shows a success indicator AND the failure path (e.g. bad secret) shows a clear error message and never the regression string "IAM credentials is missing". Gate the live-backend portion to skip cleanly when the test-connection endpoint is unreachable (conditional skip), keeping the suite green pre-live; authoritative live run is 34-06.
    - T5 Tooltip/help coverage: hover the new S3 field labels/help icons (Access Key, Secret Key, Bucket, Region, path-style) and assert each surfaces tooltip/help text (`.ant-tooltip-inner` non-empty), so the new fields are documented in-UI.
  </behavior>
  <implementation>
    Author `src/Frontend/tests/webapp-testing/s3_server_comprehensive_test.py` following the clone_comprehensive_test.py structure exactly: stdout UTF-8 wrapper, `RESULTS` list + `report()` helper, grouped print banners (Group 1 Masking, Group 2 Edit-keeps-secret, Group 3 Cancel, Group 4 Test-Connection, Group 5 Tooltips), `with sync_playwright()` headless chromium, viewport 1400x900. BASE_URL points at the running frontend (http://localhost:7000 per playwright.config baseURL / CLAUDE.md). Use throwaway dummy keys (minioadmin/minioadmin123 — already-public fixtures) — NO production secret in the file. Create/clean up servers via the API in try/finally (sanity.spec.ts pattern). At the end, print a summary and `sys.exit(1)` if any reported test failed, else `sys.exit(0)`. Conditional-skip the live Test-Connection assertion when backends/MinIO are down so the suite exits 0 pre-live.
  </implementation>
  <acceptance_criteria>
    - `python tests/webapp-testing/s3_server_comprehensive_test.py` (run from `src/Frontend/`) prints the grouped results and exits 0 when all reachable checks pass (live-backend Test-Connection success-path skipped cleanly if MinIO/backends are down).
    - T1 asserts the Secret Key input is masked and the typed secret is absent from all plaintext DOM attributes.
    - T2 asserts editing with a blank Secret Key field preserves the stored secret (API re-fetch confirms unchanged; no empty overwrite).
    - T3 asserts Cancel discards typed credentials (reopened modal shows empty fields).
    - T4 asserts Test-Connection success messaging and that the failure path never shows "IAM credentials is missing".
    - T5 asserts each new S3 field/help icon surfaces non-empty tooltip text.
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && python tests/webapp-testing/s3_server_comprehensive_test.py</automated>
  </verify>
  <done>The webapp-testing suite runs, covers T1–T5 with concrete DOM/API assertions, and exits 0 (live-backend Test-Connection success gated to skip cleanly when backends are unreachable).</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E browser → API | Exercises the real create + test-connection path with a secret |
| webapp-testing browser → DOM | Inspects masked-secret rendering; must never echo a real secret into test output/CI logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-14 | Information Disclosure | Unit/E2E/webapp-testing hardcodes a real secret that leaks into repo/CI logs | mitigate | Tests use throwaway dummy keys (e.g. minioadmin/minioadmin123 already public in fixtures); no production secret in specs |
| T-34-15 | Information Disclosure | E2E/webapp-testing asserts secret visible in UI on edit | mitigate | Vitest asserts edit shows masked sentinel; webapp-testing T1 asserts secret absent from all plaintext DOM attributes and T2 asserts blank-edit keeps stored secret (SC-02 write-only) |
| T-34-SC | Tampering | npm/pip installs | accept | No new npm packages; Vitest + Playwright already in package.json. Python Playwright already used by existing webapp-testing suites (clone/import/completeness) — no new pip dependency |
</threat_model>

<verification>
- `cd src/Frontend; npx vitest run ServerModal` exits 0.
- `cd src/Frontend; npx playwright test s3-server` passes or skips cleanly when backends/MinIO are down (authoritative live run in 34-06).
- `cd src/Frontend; python tests/webapp-testing/s3_server_comprehensive_test.py` exits 0; T1–T5 pass with live Test-Connection success gated to skip when backends are unreachable.
</verification>

<success_criteria>
- SC-02/SC-07 frontend tier: Vitest proves field rendering + PascalCase packing + masked-on-edit; Playwright E2E covers create → Test Connection → success.
- CLAUDE.md dual-tool mandate satisfied: webapp-testing Python suite covers the masked-credential edge cases (DOM masking, edit-blank-keeps-secret, cancel discards, Test-Connection messaging, tooltips) that unit/E2E miss, exiting 0.
</success_criteria>

<output>
Create `.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-05-SUMMARY.md` when done.
</output>
