---
phase: 34-s3-minio-connector-iam-credentials-bug-fix
plan: 06
type: execute
wave: 4
depends_on: ["34-01", "34-02", "34-03", "34-04", "34-05"]
files_modified:
  - .planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-LIVE-VALIDATION.md
autonomous: false
requirements: [SC-06, SC-08]
user_setup:
  - service: file-simulator-minio
    why: "Live S3 endpoint to validate authentication and file discovery against (owned by Session B)"
    dashboard_config:
      - task: "Request a MinIO bucket name + Access Key + Secret Key from Session B by posting a request row in CLUSTER-COORDINATION.md; do NOT touch the file-simulator cluster directly"
        location: "C:\\Users\\Brian\\.claude\\shared\\CLUSTER-COORDINATION.md"
must_haves:
  truths:
    - "The ez-platform backends (DataSourceManagementService + FileDiscoveryService) are Running (not CrashLooping) so the UI test-connection and discovery paths execute"
    - "Creating an S3 server with the Session-B MinIO credentials and running Test Connection returns success — no 'IAM credentials is missing'"
    - "A file discovery / pull from the S3 source returns at least one object using the new credentials"
  artifacts:
    - path: .planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-LIVE-VALIDATION.md
      provides: "Recorded evidence of the live end-to-end run (test-connection result + discovery result + screenshots/log refs)"
      contains: "Test Connection"
  key_links:
    - from: "S3 AdminServer (UI-created, real creds)"
      to: "file-simulator MinIO 172.24.80.23:30900"
      via: "test-connection + file discovery"
      pattern: "test-connection"
---

<objective>
Live end-to-end validation against the file-simulator MinIO — the final SC-08 acceptance gate. Bring the ez-platform backends up (resolve the documented Kafka clusterId + RabbitMQ CrashLoop), obtain real MinIO bucket+keys from Session B via CLUSTER-COORDINATION.md, create an S3 server through the fixed UI, run Test Connection to success, and confirm a real file discovery/pull with the new credentials.

Purpose: SC-06 (live MinIO request succeeds) and SC-08 (end-to-end run confirms an S3 source can be discovered/pulled). This is the only plan that needs both clusters healthy and the cross-session credential handoff, so it is autonomous: false.

Output: `34-LIVE-VALIDATION.md` recording the test-connection result, the discovery result, the exact bucket/endpoint used, and confirmation the IAM-missing error is gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-RESEARCH.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-VALIDATION.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-04-SUMMARY.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>Cross-session credential handoff request (Session B owns file-simulator MinIO).</what-built>
  <how-to-verify>
    1. Read `C:\Users\Brian\.claude\shared\CLUSTER-COORDINATION.md` — confirm the current file-simulator status row, S3 API endpoint, and whether the IP (172.24.80.23) has drifted; prefer the `*.file-simulator.local` hostname (Pitfall 5).
    2. Post a request row asking Session B for: a MinIO bucket name + Access Key + Secret Key usable for read/list against the S3 API.
    3. Wait for Session B to provide bucket + keys (do NOT run any `kubectl`/`minikube` against the `file-simulator` profile — Session A owns `minikube`/`ez-platform` only, CLAUDE.md).
  </how-to-verify>
  <resume-signal>Type "creds: bucket=&lt;name&gt; endpoint=&lt;host:port&gt;" once Session B has posted the bucket + Access/Secret Key, or "use-fixture" to fall back to the public fixture values (ez-data / minioadmin / minioadmin123) if Session B confirms those are live.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Bring ez-platform backends up (resolve CrashLoop)</name>
  <files>.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-LIVE-VALIDATION.md</files>
  <read_first>
    - .planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-RESEARCH.md (Environment Availability — ez-platform backends CrashLooping: Kafka InconsistentClusterIdException + RabbitMQ not deployed)
    - C:\Users\Brian\.claude\shared\CLUSTER-COORDINATION.md (current ez-platform cluster status; Session A owns minikube/ez-platform)
  </read_first>
  <action>
    Pin context for EVERY command: `kubectl --context=minikube -n ez-platform ...` (NEVER bare kubectl, NEVER `kubectl config use-context` — CLAUDE.md). Diagnose and resolve the documented CrashLoop: for the Kafka `InconsistentClusterIdException`, clear the stale broker meta (delete the Kafka PVC/data dir or reset the cluster-id per the broker's meta.properties) and restart; ensure RabbitMQ is deployed (MassTransit uses RabbitMQ per Phase 32-03 STATE note, not Kafka, for pipeline messaging). Roll out DataSourceManagementService + FileDiscoveryService and wait for Ready. Start port-forwards via `scripts/start-port-forwards.ps1` (CLAUDE.md). Confirm `curl http://localhost:5001/health` returns healthy. Record the steps + final pod status in 34-LIVE-VALIDATION.md. If backends cannot be brought up, STOP and record the blocker rather than fabricating a pass.
  </action>
  <verify>
    <human-check>DataSourceManagementService and FileDiscoveryService pods are Running/Ready (`kubectl --context=minikube -n ez-platform get pods`); http://localhost:5001/health is healthy; port-forwards active.</human-check>
  </verify>
  <done>ez-platform backends Running; health endpoint green; port-forwards up; status recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Live S3 create → Test Connection → discovery using the new credential path and Session-B MinIO bucket/keys.</what-built>
  <how-to-verify>
    1. Open http://localhost:7000 → System Settings → Input Servers → Create Server, type s3. Enter the Session-B bucket/Access Key/Secret Key/Host/Port (path-style + http on for MinIO). Save.
    2. Run Test Connection on the new server. EXPECT: success — and specifically NO "IAM credentials is missing" message (SC-05/SC-06).
    3. Create a datasource selecting this S3 server, trigger a file discovery/poll (or run the S3EndToEnd integration test live with FILE_SIMULATOR_IP set), and confirm at least one object is discovered/pulled from the bucket (SC-08).
    4. In the browser network tab, confirm the GET /api/v1/servers/{id} response masks SecretKey ("********") — write-only contract still holds live (SC-03).
    5. Record results (pass/fail per criterion, endpoint/bucket used, screenshot or log refs) in 34-LIVE-VALIDATION.md.
  </how-to-verify>
  <resume-signal>Type "validated" if Test Connection succeeds and discovery pulls an object with no IAM-missing error, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session A ↔ Session B | Credential handoff for the file-simulator MinIO; Session A must not touch the file-simulator cluster |
| live S3 server config → MinIO | Real Access/Secret Key transmitted to the live endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 | Information Disclosure | Real MinIO keys recorded in 34-LIVE-VALIDATION.md or commits | mitigate | Record only the bucket name + endpoint + pass/fail; redact the Secret Key (record AccessKey prefix at most), matching the redacted ToString contract |
| T-34-17 | Elevation / over-broad scope | Session-B key scoped wider than needed (write/delete on all buckets) | accept | Admin-configured trusted-role creds; request read/list scope from Session B where possible; documented in CLUSTER-COORDINATION request |
| T-34-18 | Tampering | Operating on the wrong cluster (file-simulator) and disrupting Session B | mitigate | Every command pinned `--context=minikube`; never `kubectl config use-context`; never run kubectl/minikube against file-simulator (CLAUDE.md) |
</threat_model>

<verification>
- ez-platform backends Running + health green (human-check).
- Live Test Connection succeeds with NO "IAM credentials is missing" (human-verify).
- A discovery/pull returns ≥1 object using the new credentials (human-verify, SC-08).
- GET masks SecretKey live (human-verify, SC-03).
</verification>

<success_criteria>
- SC-06: live MinIO request (test-connection + listing) succeeds against the file-simulator S3 endpoint.
- SC-08: end-to-end run (simulation + backends) confirms an S3 source is discovered/pulled with the new credentials; the IAM-missing error no longer occurs.
</success_criteria>

<output>
Create `.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-06-SUMMARY.md` and `34-LIVE-VALIDATION.md` when done.
</output>
