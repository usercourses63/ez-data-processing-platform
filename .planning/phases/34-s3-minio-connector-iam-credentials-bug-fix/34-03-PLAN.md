---
phase: 34-s3-minio-connector-iam-credentials-bug-fix
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/Frontend/src/pages/admin/components/ServerModal.tsx
  - src/Frontend/src/i18n/locales/he.json
  - src/Frontend/src/i18n/locales/en.json
autonomous: true
requirements: [SC-02]
must_haves:
  truths:
    - "When ServerType = s3, ServerModal renders Access Key (Input), Secret Key (Input.Password masked), Bucket, Region inputs and a path-style switch, all technical fields LTR"
    - "On submit for an s3 server, handleSubmit packs AccessKey/SecretKey/Bucket/Region/ForcePathStyle/UseHttp (PascalCase) into requestData.TypeSpecificConfig"
    - "On edit, the masked secret sentinel pre-fills the Secret Key field and is sent back unchanged unless the admin types a new value"
  artifacts:
    - path: src/Frontend/src/pages/admin/components/ServerModal.tsx
      provides: "S3 credential branch with masked secret + TypeSpecificConfig packing"
      contains: "Input.Password"
  key_links:
    - from: "ServerModal S3 branch fields"
      to: "requestData.TypeSpecificConfig"
      via: "handleSubmit packs PascalCase keys when ServerType === 's3'"
      pattern: "TypeSpecificConfig"
---

<objective>
Frontend Guard — add the S3/MinIO credential fields to `ServerModal.tsx` so an admin can enter Access Key + Secret Key (masked) plus Bucket / Region / path-style, and serialize them as PascalCase keys into `TypeSpecificConfig`. This closes the actual configuration gap that produced "IAM credentials is missing" (RESEARCH: the S3 branch today sends no `TypeSpecificConfig`).

Purpose: SC-02 — UI exposes Access/Secret Key when connection type = S3/MinIO; technical fields LTR; secret masked. Without this, the connector receives empty keys and the AWS SDK throws.

Output: An `isS3` branch in `ServerModal` rendering the masked credential fields + path-style/http switches, `handleSubmit` packing the PascalCase `TypeSpecificConfig`, edit-mode masked-secret pre-fill, and he/en i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-RESEARCH.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-01-SUMMARY.md
@src/Frontend/src/pages/admin/components/ServerModal.tsx
@src/Frontend/src/services/servers-api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add S3 credential fields to ServerModal (masked secret, LTR, path-style)</name>
  <files>src/Frontend/src/pages/admin/components/ServerModal.tsx, src/Frontend/src/i18n/locales/he.json, src/Frontend/src/i18n/locales/en.json</files>
  <read_first>
    - src/Frontend/src/pages/admin/components/ServerModal.tsx (requiresHostPort branch ~227, the Kafka branch ~283 as the analog for a type-specific section, CredentialSecretRef field ~256 as the existing technical-field pattern with className="ltr-field")
    - src/Frontend/src/services/servers-api-client.ts (CreateServerRequest/UpdateServerRequest — TypeSpecificConfig is Record&lt;string,unknown&gt;)
    - src/Frontend/src/i18n/locales/he.json + en.json (admin.servers.fields.* key style for new labels)
  </read_first>
  <action>
    Add a const `isS3 = serverType?.toLowerCase() === 's3'`. Inside the `requiresHostPort` block (or a dedicated `{isS3 && (...)}` section under the connection divider), render: `Access Key` as `<Input className="ltr-field" />` (name `AccessKey`), `Secret Key` as `<Input.Password className="ltr-field" />` (name `SecretKey`, masked, write-only), `Bucket` as `<Input className="ltr-field" />`, `Region` as `<Input className="ltr-field" />` (default `us-east-1`), and a path-style `<Switch />` (name `UsePathStyle`, `valuePropName="checked"`, default checked) plus a `UseHttp` switch (default checked for the http-only file-simulator endpoint — Pitfall 4). All technical fields use `className="ltr-field"` (CLAUDE.md RTL rule). Add he/en i18n keys under `admin.servers.fields` (e.g. `accessKey`, `secretKey`, `bucket`, `region`, `usePathStyle`, `useHttp`) with Hebrew labels matching the existing field style. Do NOT touch the datasource ConnectionTab (anti-pattern). Per SC-02.
  </action>
  <verify>
    <automated>cd src/Frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" -Quiet; if ($LASTEXITCODE -eq 0) { "typecheck OK" }</automated>
  </verify>
  <done>S3 server type renders masked Secret Key (Input.Password), Access Key/Bucket/Region as LTR inputs, and path-style/http switches; he+en i18n keys added; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Pack PascalCase TypeSpecificConfig on submit + masked-secret edit handling</name>
  <files>src/Frontend/src/pages/admin/components/ServerModal.tsx</files>
  <read_first>
    - src/Frontend/src/pages/admin/components/ServerModal.tsx (handleSubmit ~105 — currently only sets TypeSpecificConfig for Kafka via KafkaConfig; the useEffect form-prefill ~80 flattens KafkaConfig — mirror for S3 TypeSpecificConfig)
  </read_first>
  <action>
    In `handleSubmit`, when `values.ServerType === 's3'`, set `requestData.TypeSpecificConfig = { AccessKey: values.AccessKey, SecretKey: values.SecretKey, Bucket: values.Bucket, Region: values.Region, ForcePathStyle: values.UsePathStyle ?? true, UseHttp: values.UseHttp ?? true }` — exact PascalCase keys per the 34-01 contract (Pitfall 2: casing mismatch). In the `useEffect` open-prefill, when editing an s3 server, flatten `server.TypeSpecificConfig` back onto the form fields (AccessKey/Bucket/Region/UsePathStyle/UseHttp) and pre-fill `SecretKey` with the masked sentinel `"********"` returned by the API so the field shows a placeholder; if the admin leaves it as the sentinel, send it unchanged (the backend 34-02 keep-existing logic retains the stored secret). Do NOT echo a real secret — the API never returns one.
  </action>
  <verify>
    <automated>cd src/Frontend; npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" -Quiet; if ($LASTEXITCODE -eq 0) { "typecheck OK" }</automated>
  </verify>
  <done>Submitting an s3 server posts a TypeSpecificConfig with exact PascalCase keys; editing pre-fills non-secret fields and the masked secret sentinel; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser form → API | Admin enters Access/Secret Key; secret must be masked in the UI and never displayed in plaintext on edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Information Disclosure | Secret Key shown in plaintext in the form / DOM | mitigate | Task 1 uses `Input.Password` (masked) for SecretKey; edit pre-fills the masked sentinel, never a real secret |
| T-34-09 | Tampering | camelCase keys written → backend reads empty creds → "IAM missing" persists | mitigate | Task 2 packs exact PascalCase keys matching ServerCredentials.FromBsonDocument (Pitfall 2) |
| T-34-10 | Tampering | MinIO virtual-host addressing fails on raw IP | mitigate | UsePathStyle/ForcePathStyle defaults true, UseHttp defaults true (Pitfall 4) |
</threat_model>

<verification>
- `npx tsc --noEmit` in src/Frontend passes.
- Field rendering + TypeSpecificConfig packing asserted by the Vitest unit test in 34-05.
</verification>

<success_criteria>
- SC-02: UI exposes masked Access/Secret Key (LTR) plus Bucket/Region/path-style when ServerType = s3; submit serializes PascalCase TypeSpecificConfig.
</success_criteria>

<output>
Create `.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-03-SUMMARY.md` when done.
</output>
