---
phase: 34-s3-minio-connector-iam-credentials-bug-fix
plan: 04
type: tdd
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - tests/Shared.Tests/Services/ServerCredentialsTests.cs
  - tests/DataSourceManagement.Tests/Controllers/ServersControllerSecretTests.cs
  - tests/IntegrationTests/Connectors/S3ConnectorTests.cs
autonomous: true
requirements: [SC-04, SC-05, SC-06, SC-07]
must_haves:
  truths:
    - "A unit test proves ServerCredentials.FromBsonDocument reads PascalCase AccessKey/SecretKey and that ToString() redacts the secret"
    - "An integration test proves POST /api/v1/servers (s3) then GET masks SecretKey while AccessKey/Bucket persist in Mongo TypeSpecificConfig"
    - "An integration test proves the empty-credentials path fails (regression for IAM-missing) and the populated path authenticates to MinIO"
  artifacts:
    - path: tests/Shared.Tests/Services/ServerCredentialsTests.cs
      provides: "PascalCase key contract + redaction unit tests"
      contains: "FromBsonDocument"
    - path: tests/DataSourceManagement.Tests/Controllers/ServersControllerSecretTests.cs
      provides: "controller GET-masks-secret + round-trip integration"
      contains: "SecretKey"
    - path: tests/IntegrationTests/Connectors/S3ConnectorTests.cs
      provides: "empty-creds regression + controller→Mongo→connector→MinIO end-to-end"
      contains: "S3EndToEnd"
  key_links:
    - from: "ServersControllerSecretTests"
      to: "GET response body"
      via: "assert SecretKey == \"********\" and AccessKey present"
      pattern: "\\*\\*\\*\\*\\*\\*\\*\\*"
---

<objective>
Backend test tier — prove the credential mapping, write-only masking, encrypt-at-rest redaction, and live MinIO authentication. This is the Wave 0 backend gap-closure: the unit + integration tests that every prior backend task's `<verify>` referenced.

Purpose: SC-07 (unit + integration pass), SC-04 (secret redacted/encrypted), SC-05 (keys reach the connector — empty-creds regression), SC-06 (live MinIO request succeeds). TDD because each assertion is `expect(mapping(input)).toBe(output)`-shaped.

Output: `ServerCredentialsTests` (Shared), `ServersControllerSecretTests` (DataSourceManagement), and extended `S3ConnectorTests` with an empty-creds regression + `Category=S3EndToEnd` controller→Mongo→connector flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-RESEARCH.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-VALIDATION.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-01-SUMMARY.md
@.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-02-SUMMARY.md
</context>

<feature>
  <name>Backend credential test suite (RED → GREEN against shipped 34-01/34-02 code)</name>
  <files>tests/Shared.Tests/Services/ServerCredentialsTests.cs, tests/DataSourceManagement.Tests/Controllers/ServersControllerSecretTests.cs, tests/IntegrationTests/Connectors/S3ConnectorTests.cs</files>
  <behavior>
    Unit (Shared.Tests):
    - FromBsonDocument over { AccessKey, SecretKey, Bucket, Region, ForcePathStyle, UseHttp } yields AccessKey/SecretKey non-null and equal (PascalCase contract, Pitfall 2).
    - ServerCredentials with S3 keys → ToString() contains "S3Auth(key=mini***)" and does NOT contain the full SecretKey (redaction, SC-04).
    - ServerCredentialProtector.Protect(secret) round-trips via Unprotect; ciphertext != plaintext; legacy plaintext reads transparently.

    Integration (DataSourceManagement.Tests):
    - POST /api/v1/servers with an s3 body (TypeSpecificConfig AccessKey/SecretKey/Bucket) returns 201; the subsequent GET /api/v1/servers/{id} response body contains AccessKey and Bucket but SecretKey == "********" (write-only, SC-03).
    - The persisted Mongo document's TypeSpecificConfig.SecretKey is ciphertext ("enc:v1:" prefix), not the submitted plaintext (SC-04).
    - PUT with SecretKey == "********" keeps the stored encrypted secret (keep-existing, Pitfall 3).

    Integration live (IntegrationTests, SkippableFact):
    - Empty-credentials regression: TestConnectionAsync with a server whose TypeSpecificConfig has no AccessKey/SecretKey returns IsSuccess == false (the IAM-missing path) — documents the bug.
    - Category=S3EndToEnd: build credentials from TypeSpecificConfig via FromBsonDocument and call S3Connector.TestConnectionAsync against the file-simulator MinIO → IsSuccess == true (SC-05/SC-06).
  </behavior>
  <implementation>
    Author `ServerCredentialsTests` in `tests/Shared.Tests/Services/` (xUnit + FluentAssertions; mirror existing Shared.Tests structure). Author `ServersControllerSecretTests` in `tests/DataSourceManagement.Tests/Controllers/` — use the project's existing API/Mongo test harness (mirror `tests/IntegrationTests/Fixtures/ApiClientFixture.cs` + `MongoDbFixture.cs` patterns; reuse `[Collection("Integration")]` if these tests live against live Mongo). Extend `S3ConnectorTests.cs` (do NOT recreate it) with a `[Trait("Category","S3EndToEnd")]` SkippableFact reusing `FileSimulatorFixture` (creds `minioadmin`/`minioadmin123`, bucket `ez-data`, `S3Port` 30900, `SkipIfFileSimulatorUnavailable` guard, `FILE_SIMULATOR_IP` env override — Pitfall 5) and an empty-creds regression fact. Follow RED→GREEN→REFACTOR with atomic commits.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → API/Mongo | Verifies the write-only + encrypt-at-rest guards actually hold end-to-end |
| connector → MinIO | Verifies real keys authenticate (no IAM-missing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-11 | Information Disclosure | Regression: secret leaks back in GET undetected | mitigate | ServersControllerSecretTests asserts SecretKey == "********" in GET body (SC-03) |
| T-34-12 | Information Disclosure | Regression: plaintext secret persisted | mitigate | Test asserts Mongo TypeSpecificConfig.SecretKey has "enc:v1:" prefix (SC-04) |
| T-34-13 | Tampering | Regression: empty-creds path silently "succeeds" | mitigate | Empty-creds SkippableFact asserts IsSuccess == false (documents the IAM-missing bug, SC-05) |
| T-34-SC | Tampering | NuGet installs | accept | No new packages; test deps already referenced |
</threat_model>

<verification>
- `dotnet test tests/Shared.Tests --filter "FullyQualifiedName~ServerCredentials"` exits 0.
- `dotnet test tests/DataSourceManagement.Tests --filter "FullyQualifiedName~ServersControllerSecret"` exits 0.
- `dotnet test tests/IntegrationTests --filter "Protocol=S3"` exits 0 (live tests skip cleanly if MinIO unreachable).
</verification>

<success_criteria>
- SC-04/SC-05/SC-06/SC-07 backend tier: credential mapping, redaction, write-only masking, encrypt-at-rest, and live MinIO auth all covered and green (live tier may Skip when file-simulator is down — confirmed live in 34-06).
</success_criteria>

<output>
Create `.planning/phases/34-s3-minio-connector-iam-credentials-bug-fix/34-04-SUMMARY.md` when done.
</output>
