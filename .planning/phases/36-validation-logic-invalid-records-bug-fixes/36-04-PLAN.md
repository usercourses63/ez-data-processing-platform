---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Frontend/src/utils/fileAnalyzer/__tests__/schemaInferrer.optional.test.ts
  - src/Frontend/src/utils/__tests__/schemaAutoSuggest.optionalDate.test.ts
autonomous: true
requirements: [B1, B2, B3]
must_haves:
  truths:
    - "A failing Vitest spec exists for B1: generateJsonSchema must NOT force an intended-optional field into required[]"
    - "A failing Vitest spec exists for B2: an optional date field must produce an empty-tolerant schema (empty value valid, populated date still checked)"
    - "Specs assert populated-date still gets format checking (no regression of the intended auto-suggest)"
  artifacts:
    - path: "src/Frontend/src/utils/fileAnalyzer/__tests__/schemaInferrer.optional.test.ts"
      provides: "B1 RED — required derived from intent not sample completeness; optional empty-tolerant"
    - path: "src/Frontend/src/utils/__tests__/schemaAutoSuggest.optionalDate.test.ts"
      provides: "B2 RED — optional date empty-tolerant; populated date still format-checked"
  key_links:
    - from: "schemaInferrer.optional.test.ts"
      to: "generateJsonSchema"
      via: "import + assertion on required[]/property shape"
      pattern: "generateJsonSchema"
---

<objective>
Author the genuine frontend RED Vitest specs that fail before the 36-05 schema-generation fix: B1 — `generateJsonSchema` forces any sample-complete column into `required[]` (`schemaInferrer.ts:150-155`), so an intended-optional field becomes required; B2 — `schemaAutoSuggest` adds `format:'date'` unconditionally to any `date`/`תאריך` field (`schemaAutoSuggest.ts:93-104`) with no empty-tolerance. These specs encode the corrected behavior and MUST fail today.

Purpose: Per CONTEXT "reproduce before fixing" — B1 and B2's user-facing trigger lives in frontend schema generation; these are the failing tests 36-05 turns green.
Output: Two Vitest specs (one per generator file) under the frontend `__tests__` dirs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B1 RED — generateJsonSchema must honor optional intent</name>
  <files>src/Frontend/src/utils/fileAnalyzer/__tests__/schemaInferrer.optional.test.ts</files>
  <behavior>
    - generateJsonSchema with an intended-optional field whose sample rows are fully populated must NOT place that field in required[]
    - An optional field's property must permit empty/absent (empty-tolerant shape: type ["string","null"] or equivalent — assert the agreed shape from 36-01 target tests)
    - A genuinely-required field (per intent) still appears in required[]
  </behavior>
  <read_first>
    - src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts:137-164 (generateJsonSchema; required[] from sample-emptiness at :150-155; additionalProperties:false at :162)
    - src/Frontend/src/utils/__tests__/schemaAutoSuggest.test.ts (existing Vitest patterns/imports to mirror)
    - src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs target-shape from 36-01 (align the empty-tolerant shape so frontend output matches what Corvus accepts)
  </read_first>
  <action>
    Create `schemaInferrer.optional.test.ts`. Drive `generateJsonSchema` with a fixture that distinguishes optional vs required intent. Today the signature is `(fieldNames, rows, applySuggestions)` with NO optional-intent input — so the spec must express HOW optional intent will be supplied after the fix (an added optional-fields/required-fields parameter, or a per-field flag) and assert the corrected output: the intended-optional field (e.g. `note`) is absent from `required[]` even though every sample row populated it; the intended-required field (e.g. `id`) remains in `required[]`. Assert the optional property is empty-tolerant matching the 36-01 agreed shape. Since the parameter does not exist yet, this spec FAILS to satisfy the assertion (RED) — name it clearly and document in SUMMARY the exact signature change 36-05 must make. Use `npm run test:unit -- schemaInferrer.optional` to run just this spec.
  </action>
  <acceptance_criteria>
    - Spec asserts an intended-optional, sample-complete field is NOT in required[]
    - Spec asserts an intended-required field IS in required[]
    - Spec asserts the optional property is empty-tolerant (agreed shape)
    - `npm run test:unit -- schemaInferrer.optional` runs and the B1 spec is RED before 36-05
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npm run test:unit -- schemaInferrer.optional</automated>
  </verify>
  <done>B1 corrected-generator behavior is pinned by a failing Vitest spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B2 RED — optional date field empty-tolerant, populated still checked</name>
  <files>src/Frontend/src/utils/__tests__/schemaAutoSuggest.optionalDate.test.ts</files>
  <behavior>
    - An OPTIONAL field named `date`/`תאריך` produces an empty-tolerant schema (empty string valid)
    - A POPULATED date value still gets format:'date' enforcement (intended auto-suggest preserved, no regression)
    - A REQUIRED/populated date field keeps strict format:'date'
  </behavior>
  <read_first>
    - src/Frontend/src/utils/schemaAutoSuggest.ts:93-104 (unconditional format:'date' for date/תאריך) and :275-277 (applySuggestionsToSchema)
    - src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts:169-235 (inferPropertySchema applies suggestions at :233-235)
    - src/Services/ValidationService.Tests/CorvusDateFormatTests.cs target shape from 36-01 (align empty-tolerant date shape)
  </read_first>
  <action>
    Create `schemaAutoSuggest.optionalDate.test.ts`. Assert the corrected behavior: when a `date`/`תאריך` field is optional, the produced property is empty-tolerant (e.g. `anyOf:[{type:"string",maxLength:0},{type:"string",format:"date"}]` or `type:["string","null"]` — match the 36-01 agreed shape) so an empty value is valid; when the date field is required/populated, `format:'date'` is still asserted. Express how optionality is signaled (same parameter approach as Task 1). RED before 36-05 because today format:'date' is added unconditionally with no empty escape hatch. Run with `npm run test:unit -- schemaAutoSuggest.optionalDate`.
  </action>
  <acceptance_criteria>
    - Optional date field spec asserts empty value valid (empty-tolerant shape present)
    - Required/populated date spec asserts format:'date' still enforced
    - `npm run test:unit -- schemaAutoSuggest.optionalDate` runs and the B2 spec is RED before 36-05
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npm run test:unit -- schemaAutoSuggest.optionalDate</automated>
  </verify>
  <done>B2 corrected empty-tolerant date behavior is pinned by a failing Vitest spec while populated-date enforcement is protected.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npm run test:unit -- schemaInferrer.optional schemaAutoSuggest.optionalDate` runs both specs; both are RED before the 36-05 fix and define the corrected optional/date-empty modeling
</verification>

<success_criteria>
B1 and B2 have genuine failing frontend specs encoding the corrected generator behavior (optional not forced required; optional date empty-tolerant; populated date still checked) before any fix.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-04-SUMMARY.md` when done
</output>
