---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 09
type: execute
wave: 3
depends_on: ["36-05", "36-06", "36-07", "36-08"]
files_modified:
  - src/Frontend/tests/e2e/invalid-records.spec.ts
  - src/Frontend/tests/webapp-testing/invalid_records_b4_comprehensive_test.py
autonomous: false
requirements: [B1, B2, B3, B4]
must_haves:
  truths:
    - "Unit + integration + E2E suites for the touched areas run and pass"
    - "Each closed bug (B1-B4) has a regression test that fails before the fix and passes after"
    - "The Invalid Records page shows new invalid records WITHOUT manual reload and across all groups (E2E)"
    - "B4 before/after latency numbers are recorded"
    - "The Phase-35 MinIO MVP flow still passes after these validation changes (no regression)"
  artifacts:
    - path: "src/Frontend/tests/e2e/invalid-records.spec.ts"
      provides: "E2E: realtime appearance + cross-group completeness for B4"
    - path: "src/Frontend/tests/webapp-testing/invalid_records_b4_comprehensive_test.py"
      provides: "comprehensive UI verification for B4 show-all + latency"
  key_links:
    - from: "invalid-records.spec.ts"
      to: "InvalidRecordsManagement realtime + grouping"
      via: "process file → records appear without reload"
      pattern: "entity-changed|EntityChanged"
---

<objective>
Close the phase with the full test loop required by CONTEXT: run unit + integration + E2E for all touched areas, prove each bug's regression test flips red→green, add the B4 E2E (realtime appearance + cross-group completeness) and a webapp-testing comprehensive pass, record B4 before/after latency numbers, and confirm the Phase-35 MinIO MVP flow still passes (no regression).

Purpose: CONTEXT mandates "run unit + integration + E2E … loop until green … no regressions to the Phase-35 MinIO MVP flow." This is the phase gate.
Output: New/updated E2E + webapp-testing for B4; a green full-suite run; recorded latency numbers; Phase-35 regression confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: B4 E2E — realtime appearance + cross-group completeness</name>
  <files>src/Frontend/tests/e2e/invalid-records.spec.ts</files>
  <read_first>
    - src/Frontend/tests/e2e/invalid-records.spec.ts and invalid-records-bulk.spec.ts (existing patterns: reachability-gated test.skip, API context, cleanup)
    - src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx (grouping/pageSize/entity-changed after 36-08)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B4 Reproduce / Test layers)
  </read_first>
  <action>
    Add E2E specs to `invalid-records.spec.ts` (Playwright, baseURL http://localhost:7000): (a) process/seed a file that yields invalid records and assert they appear on the page WITHOUT a manual reload (relies on the 36-08 EntityChanged broadcast); (b) assert records across MULTIPLE data-source groups are all visible/reachable (no collapsed-group hiding) and that page size shows them. Use the existing reachability-gated `test.skip` so the spec skips cleanly when the cluster/frontend is offline (mirror Phase-31/35 specs). Use API context for setup/cleanup with try/finally.
  </action>
  <acceptance_criteria>
    - `npm run test:e2e -- invalid-records` lists/runs the new specs; they skip cleanly offline and assert realtime + cross-group visibility when the cluster is up
    - No reliance on manual reload in the realtime assertion
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npx playwright test invalid-records --list</automated>
  </verify>
  <done>B4 realtime + completeness behavior has E2E coverage that runs against the cluster and skips cleanly offline.</done>
</task>

<task type="auto">
  <name>Task 2: webapp-testing comprehensive B4 + full unit/integration sweep</name>
  <files>src/Frontend/tests/webapp-testing/invalid_records_b4_comprehensive_test.py</files>
  <read_first>
    - src/Frontend/tests/webapp-testing/ (existing comprehensive test pattern — reconnaissance-first, e.g. minio_mvp_flow_comprehensive_test.py)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md (Sampling Rate, Manual-Only latency verification)
  </read_first>
  <action>
    Author `invalid_records_b4_comprehensive_test.py` (Python Playwright, reconnaissance-first): verify the Invalid Records page shows all existing records (all groups expanded, count matches the API totalCount), that a newly produced invalid record appears without reload, and capture a coarse page-load/API timing for the before/after latency note. Gate to skip cleanly when the frontend/backends are down (`py_compile` must pass offline). Then run the consolidated quick suites for all touched backend test projects and the frontend unit suite to confirm the red→green flip for B1-B4 regression tests: `dotnet test` for ValidationService.Tests + InvalidRecordsService.Tests + FileProcessorService.Tests, and `npm run test:unit`.
  </action>
  <acceptance_criteria>
    - `python -m py_compile src/Frontend/tests/webapp-testing/invalid_records_b4_comprehensive_test.py` exits 0
    - `dotnet test src/Services/ValidationService.Tests` + `...InvalidRecordsService.Tests` + `...FileProcessorService.Tests` all green
    - `cd src/Frontend && npm run test:unit` green (B1/B2 + InvalidRecordsManagement specs)
    - Each B1-B4 regression test is confirmed red-before / green-after in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/ValidationService.Tests; dotnet test src/Services/InvalidRecordsService.Tests; dotnet test src/Services/FileProcessorService.Tests</automated>
  </verify>
  <done>Comprehensive B4 UI test exists and the full unit+integration suite for the touched areas is green with documented red→green regression evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete B1-B4 fix set, verified live: clean validation of the real koko CSV, optional/empty + optional-date handling, Invalid Records show-all + reduced latency, and the Phase-35 MinIO MVP flow regression check.</what-built>
  <how-to-verify>
    1. Against the live minikube ez-platform cluster (context-pinned `kubectl --context=minikube -n ez-platform`), run the koko data source over the real MinIO CSV (`ez-phase34-input/mvp-deploy-111138.csv`) and confirm ZERO false-invalids (B3) and that an optional/empty + optional-empty-date file produces 0 invalid records (B1/B2).
    2. Open the Invalid Records page (http://localhost:7000) and confirm all existing records show across groups and a newly produced invalid record appears WITHOUT manual reload (B4 show-all). Record the before/after list/API latency numbers (B4 latency).
    3. Re-run the Phase-35 MinIO MVP flow end-to-end (UI create input+output server + datasource → run → fresh JSON object in the MinIO output bucket) and confirm it still passes (no regression).
  </how-to-verify>
  <resume-signal>Type "approved" with the B4 before/after latency numbers and Phase-35 regression result, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All touched backend test projects + frontend unit suite green; E2E specs list and run against the cluster, skip cleanly offline
- Live checkpoint confirms B1-B4 fixed on real data + Phase-35 MVP flow still green + latency numbers recorded
</verification>

<success_criteria>
The full unit+integration+E2E loop is green; every bug has a red→green regression test; B4 latency improvement is quantified; the Phase-35 MinIO MVP flow still passes.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-09-SUMMARY.md` when done
</output>
