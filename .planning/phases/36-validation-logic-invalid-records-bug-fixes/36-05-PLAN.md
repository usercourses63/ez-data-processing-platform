---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: ["36-01", "36-04"]
files_modified:
  - src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts
  - src/Frontend/src/utils/schemaAutoSuggest.ts
autonomous: true
requirements: [B1, B2]
must_haves:
  truths:
    - "An optional field with no value produces NO required/'missing data' error"
    - "An optional date field with an empty value passes validation (no auto date-format error)"
    - "A populated date value still receives format:'date' checking (intended behavior preserved)"
    - "The B1/B4 frontend RED specs (36-04) and the B1/B2 backend contract (36-01) pass after this fix"
  artifacts:
    - path: "src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts"
      provides: "required[] derived from optional intent; optional fields modeled empty-tolerant"
      contains: "generateJsonSchema"
    - path: "src/Frontend/src/utils/schemaAutoSuggest.ts"
      provides: "date format suggestion is empty-tolerant for optional fields"
      contains: "format"
  key_links:
    - from: "schemaInferrer.generateJsonSchema"
      to: "schemaAutoSuggest.suggestConstraintsForField"
      via: "applySuggestions empty-tolerant path"
      pattern: "applySuggestions"
---

<objective>
Fix the shared B1+B2 root cause in the frontend schema generator: optional fields are not modeled to permit empty/absent values. `generateJsonSchema` (`schemaInferrer.ts:150-155`) derives `required[]` from SAMPLE completeness instead of user/field intent, and `schemaAutoSuggest` (`schemaAutoSuggest.ts:93-104`) applies `format:'date'` unconditionally. After this fix an optional field (including an optional `date`) with an empty value validates as valid, while populated dates keep format enforcement.

Purpose: Close B1 and B2 (CONTEXT confirms shared root cause) by fixing optional/empty modeling at the schema-authoring layer — the single source of truth Corvus executes.
Output: Corrected `schemaInferrer.ts` + `schemaAutoSuggest.ts`; 36-04 frontend specs and 36-01 backend contract green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive required[] from intent + model optional fields empty-tolerant (B1)</name>
  <files>src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts</files>
  <behavior>
    - generateJsonSchema no longer forces a sample-complete field into required[] when it is intended optional
    - Optional fields are modeled empty-tolerant (agreed shape from 36-01: type ["string","null"] or anyOf empty escape) so empty/absent validates valid
    - Required fields still appear in required[]; populated values unaffected
  </behavior>
  <read_first>
    - src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts:137-235 (generateJsonSchema + inferPropertySchema; required at :150-155; additionalProperties:false at :162)
    - src/Frontend/src/pages/schema/SchemaBuilderNew.tsx:54-59 (manual schema editor — RESEARCH Q3: verify whether its required[] authoring shares the same optional-intent defect as the import wizard)
    - src/Frontend/src/utils/fileAnalyzer/__tests__/schemaInferrer.optional.test.ts (the B1 RED spec from 36-04 — implement to its contract)
    - src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs (the target empty-tolerant shape Corvus accepts — emit exactly this)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B1 Minimal-fix direction options 1-3)
  </read_first>
  <action>
    Implement RESEARCH B1 option 1+2 (per CONTEXT Claude's-discretion): add an optional/required-intent input to `generateJsonSchema` (a `requiredFields`/`optionalFields` param or per-field flag — match the signature the 36-04 spec expects) and build `required[]` from that intent, NOT from `hasEmpty` sample-emptiness at :150-155. For any field not required, model it empty-tolerant using the exact shape the 36-01 Corvus target test accepts (type ["string","null"] or anyOf with an empty escape). Keep the sample-emptiness heuristic only as a default SUGGESTION when no explicit intent is supplied (so existing callers still behave). Re-evaluate the `additionalProperties:false` default at :162 only as far as B1 requires — do not broaden scope into schema-editor UX (deferred). Update existing callers of `generateJsonSchema` in the import wizard to pass the intent. Additionally (RESEARCH Q3), confirm whether the manual schema editor `SchemaBuilderNew.tsx:54-59` authors `required[]` with the same sample-completeness defect: if yes, apply the same optional-intent modeling to that path and add it to files_modified; if no (it authors required[] from explicit user toggles), document in the SUMMARY why SC-2's "optional field → no missing-data error" already holds for the manual path and it is out of scope. Then run the B1 spec to green.
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- schemaInferrer.optional` passes (B1 spec green)
    - Intended-optional sample-complete field is absent from required[]; intended-required field present
    - Optional property shape matches the 36-01 Corvus-accepted empty-tolerant shape
    - No TypeScript build break: `npm run build` (or tsc) for the import-wizard callers still compiles
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npm run test:unit -- schemaInferrer.optional</automated>
  </verify>
  <done>generateJsonSchema honors optional intent and models optional fields empty-tolerant; B1 spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Empty-tolerant date format for optional fields (B2)</name>
  <files>src/Frontend/src/utils/schemaAutoSuggest.ts</files>
  <behavior>
    - An optional date/תאריך field yields an empty-tolerant property (empty value valid)
    - A populated/required date still asserts format:'date'
    - Invalid populated date strings still fail (format enforcement intact)
  </behavior>
  <read_first>
    - src/Frontend/src/utils/schemaAutoSuggest.ts:93-104 (date block) and :275-277 (applySuggestionsToSchema)
    - src/Frontend/src/utils/__tests__/schemaAutoSuggest.optionalDate.test.ts (the B2 RED spec from 36-04)
    - src/Services/ValidationService.Tests/CorvusDateFormatTests.cs (target empty-tolerant date shape)
  </read_first>
  <action>
    Make the date suggestion empty-tolerant when the field is optional: instead of unconditionally setting `suggestions.format='date'` at :96, emit (for optional fields) the empty-tolerant shape Corvus accepts per the 36-01 date target test — e.g. wrap format in `anyOf:[{type:"string",maxLength:0},{type:"string",format:"date"}]` or pair `type:["string","null"]` with format only on the non-null branch. Keep strict `format:'date'` for required date fields (populated-date enforcement is intended behavior, NOT a bug — preserve it). Thread the optional signal through `applySuggestionsToSchema`/`inferPropertySchema` consistently with Task 1. Run the B2 spec to green.
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- schemaAutoSuggest.optionalDate` passes (B2 spec green)
    - Optional date empty value valid; required/populated date still format-checked; invalid date string still fails
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npm run test:unit -- schemaAutoSuggest.optionalDate</automated>
  </verify>
  <done>Optional date fields tolerate empty values while populated dates retain format checking; B2 spec green.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm B1/B2 backend Corvus contract green over generated shape</name>
  <files>src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs, src/Services/ValidationService.Tests/CorvusDateFormatTests.cs</files>
  <read_first>
    - src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs and CorvusDateFormatTests.cs (the 36-01 target-shape tests)
    - The empty-tolerant shapes now emitted by schemaInferrer.ts / schemaAutoSuggest.ts (Task 1-2)
  </read_first>
  <action>
    Verify the empty-tolerant shapes produced by the fixed generator are exactly the shapes the 36-01 target Corvus tests assert as valid. If the chosen shape differs from the 36-01 placeholder (e.g. you picked anyOf over ["string","null"]), update the 36-01 target-shape assertions to match the SHIPPED shape so backend and frontend agree — but never weaken the broken-shape characterization assertions. Run the full ValidationService.Tests B1+B2 filters green.
  </action>
  <acceptance_criteria>
    - `dotnet test src/Services/ValidationService.Tests --filter "B1|B2"` passes
    - The shipped frontend optional/date shape equals the shape the backend target tests validate as valid
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/ValidationService.Tests --filter "B1|B2"</automated>
  </verify>
  <done>Frontend-generated optional/date shapes and backend Corvus contract agree and are green.</done>
</task>

</tasks>

<verification>
- `cd src/Frontend && npm run test:unit -- schemaInferrer.optional schemaAutoSuggest.optionalDate` green
- `dotnet test src/Services/ValidationService.Tests --filter "B1|B2"` green
- No regression to existing `schemaAutoSuggest.test.ts` / import-wizard build
</verification>

<success_criteria>
B1 and B2 fixed at the schema-generation layer: optional fields (including optional dates) with empty/absent values validate valid; populated dates still checked; backend + frontend tests agree and pass.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-05-SUMMARY.md` when done
</output>
