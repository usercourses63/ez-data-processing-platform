---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Services/InvalidRecordsService.Tests/InvalidRecordsService.Tests.csproj
  - src/Services/InvalidRecordsService.Tests/RepositoryPagingTests.cs
  - src/Services/InvalidRecordsService.Tests/InvalidRecordIndexTests.cs
  - src/Services/InvalidRecordsService.Tests/CreatedRecordBroadcastTests.cs
autonomous: true
requirements: [B4]
must_haves:
  truths:
    - "A failing/characterizing xUnit test exists for B4 latency (server-side paging + indexes) BEFORE the fix"
    - "A failing test exists asserting that creating an invalid record must emit an EntityChanged broadcast (currently absent) BEFORE the fix"
  artifacts:
    - path: "src/Services/InvalidRecordsService.Tests/InvalidRecordsService.Tests.csproj"
      provides: "xUnit test project for InvalidRecordsService (did not exist before)"
    - path: "src/Services/InvalidRecordsService.Tests/RepositoryPagingTests.cs"
      provides: "B4 latency repro: GetPagedAsync must page server-side, not in-memory"
    - path: "src/Services/InvalidRecordsService.Tests/CreatedRecordBroadcastTests.cs"
      provides: "B4 missing-rows repro: invalid-record create must broadcast EntityChanged"
  key_links:
    - from: "RepositoryPagingTests"
      to: "InvalidRecordRepository.GetPagedAsync"
      via: "paging assertion"
      pattern: "GetPagedAsync"
---

<objective>
Scaffold the missing `InvalidRecordsService.Tests` xUnit project and author RED tests for both halves of B4: (latency) `GetPagedAsync` currently loads the entire matching collection and pages in memory with zero Mongo indexes; (missing-rows) the validation write path creates `DataProcessingInvalidRecord` with no SignalR `EntityChanged` broadcast, violating the CLAUDE.md CRUD mandate.

Purpose: Per CONTEXT "reproduce before fixing" — B4 needs executable repros (paging behavior + missing-broadcast) before fixes 36-07 and 36-08.
Output: New `src/Services/InvalidRecordsService.Tests/` project + paging/index/broadcast RED tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold InvalidRecordsService.Tests + repository paging RED test</name>
  <files>src/Services/InvalidRecordsService.Tests/InvalidRecordsService.Tests.csproj, src/Services/InvalidRecordsService.Tests/RepositoryPagingTests.cs</files>
  <read_first>
    - src/Services/FileProcessorService.Tests/FileProcessorService.Tests.csproj (mirror the csproj layout)
    - src/Services/InvalidRecordsService/Repositories/InvalidRecordRepository.cs:40-127 (GetPagedAsync in-memory paging at :62-71, GetStatisticsAsync full-scan at :99-127)
    - src/Services/InvalidRecordsService/Models/Requests/InvalidRecordListRequest.cs (Page/PageSize/Status/Search contract)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B4 Latency causes + Don't Hand-Roll table)
  </read_first>
  <action>
    Create `InvalidRecordsService.Tests.csproj` mirroring FileProcessorService.Tests (net10.0, xUnit + FluentAssertions + Moq, ProjectReference to ../InvalidRecordsService and ../Shared). Author `RepositoryPagingTests.cs` that asserts the SERVER-SIDE-paging contract the 36-07 fix must satisfy: seed N=25 `DataProcessingInvalidRecord` (N > pageSize 10) and assert `GetPagedAsync(page:1,pageSize:10)` returns exactly 10 records, page:3 returns 5, and `totalCount==25` with `OrderByDescending(CreatedAt)` ordering preserved. Where a live Mongo replica set is unavailable offline, gate the DB-backed assertions behind a SkippableFact (mirror the Phase-34/35 SkippableFact pattern) and additionally add a pure source/structure assertion (e.g. reflection or a sort-order unit over an in-memory list) so the test class always compiles and runs. The current in-memory implementation will satisfy correctness but NOT the latency/index contract — capture that gap in the SUMMARY so 36-07 closes it.
  </action>
  <acceptance_criteria>
    - `dotnet build src/Services/InvalidRecordsService.Tests` exits 0
    - `RepositoryPagingTests` asserts page1=10, page3=5, totalCount=25, descending CreatedAt
    - DB-dependent assertions skip cleanly (green) when Mongo is unreachable; class still compiles and runs offline
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Paging</automated>
  </verify>
  <done>InvalidRecordsService.Tests builds and pins the server-side paging contract for B4 latency.</done>
</task>

<task type="auto">
  <name>Task 2: B4 index-presence RED test</name>
  <files>src/Services/InvalidRecordsService.Tests/InvalidRecordIndexTests.cs</files>
  <read_first>
    - src/Services/Shared/Entities/DataProcessingInvalidRecord.cs (currently no [Index] attributes / no index init)
    - src/Services/InvalidRecordsService/Program.cs (no index creation today)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B4 Latency cause #2 — no Mongo indexes)
  </read_first>
  <action>
    Author `InvalidRecordIndexTests.cs` that encodes the index contract 36-07 must create: assert that index-definition metadata exists on `DataProcessingInvalidRecord` for keys `{DataSourceId}`, `{CreatedAt}`, `{ErrorType}`, and a compound `{IsIgnored,IsDeleted,CreatedAt}` (the common filter+sort). Implement via whichever signal the codebase exposes — `[Index]`/MongoDB.Entities index attributes by reflection, OR an `InitIndexesAsync` registration list. If indexes are created imperatively in Program.cs/startup, assert the presence of that registration call by source-scan. Before the 36-07 fix this test is RED (no index metadata). Keep it offline-runnable (reflection/source assertion, no live Mongo required).
  </action>
  <acceptance_criteria>
    - Test enumerates the four required index key sets and asserts each is declared
    - Test is RED before 36-07 (no indexes declared today) and runs without a live cluster
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Index</automated>
  </verify>
  <done>The required Mongo index set for invalid records is pinned as a failing contract before the fix.</done>
</task>

<task type="auto">
  <name>Task 3: B4 missing-rows RED — invalid-record create must broadcast EntityChanged</name>
  <files>src/Services/InvalidRecordsService.Tests/CreatedRecordBroadcastTests.cs</files>
  <read_first>
    - src/Services/ValidationService/Services/ValidationService.cs:302-308 (SaveAsync with NO SignalR broadcast)
    - src/Services/InvalidRecordsService/Hubs/InvalidRecordsHub.cs (empty — only OnConnectedAsync)
    - src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx:351-362 (page listens for entity-changed where entityType==='InvalidRecord')
    - CLAUDE.md (SignalR EntityChanged MANDATORY on all CRUD)
  </read_first>
  <action>
    Author `CreatedRecordBroadcastTests.cs` encoding the broadcast contract 36-08 must satisfy: when an invalid record is created by the pipeline, an `EntityChanged` message with `EntityType=="InvalidRecord"` and `Action=="created"` must be emitted to the InvalidRecords SignalR hub. Since the broadcast path does not exist yet, assert against the intended seam the 36-08 fix introduces — e.g. a `Mock<IHubContext<InvalidRecordsHub>>`/`IBroadcaster` abstraction verifying `Clients.All.SendAsync("EntityChanged", <payload with EntityType=InvalidRecord, Action=created>)` is invoked, OR (if 36-08 uses a relay consumer) assert a new `InvalidRecordCreatedEvent` consumer relays to the hub. Where the production seam is absent today, write the test so it FAILS to compile-link against the missing member is avoided — instead assert the absence via a documented xUnit `Assert.Fail`/skip that flips to a real verification once 36-08 adds the seam; clearly mark it RED-for-B4-missing-rows in the test name and SUMMARY so 36-08 wires it green.
  </action>
  <acceptance_criteria>
    - Test names the exact payload contract: EntityType=="InvalidRecord", Action=="created"
    - Test is RED before 36-08 (no broadcast seam exists) and documents the expected SignalR mandate
    - `dotnet test src/Services/InvalidRecordsService.Tests --filter Broadcast` compiles and runs (red/skip as designed)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Broadcast</automated>
  </verify>
  <done>The missing EntityChanged broadcast on invalid-record creation is pinned as a failing contract before the fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → InvalidRecords API | client-supplied filter/search/page params reach the Mongo query (relevant to 36-07 fix this plan pins) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | GetPagedAsync full-collection load | mitigate | 36-07 pushes Sort/Skip/Limit + indexes server-side; this plan pins the paging contract that caps materialized docs |
| T-36-02 | Tampering | `request.Search` Contains filter | accept | MongoDB.Entities parameterizes Match; no string concatenation into a query — low risk, no PII exposure |
</threat_model>

<verification>
- `dotnet build src/Services/InvalidRecordsService.Tests` exits 0
- Paging, Index, and Broadcast filters each select runnable tests; latency + index + broadcast contracts are RED before fixes 36-07/36-08
</verification>

<success_criteria>
InvalidRecordsService.Tests exists and pins all three B4 contracts (server-side paging, required indexes, EntityChanged-on-create) as failing/characterizing tests before any fix.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-02-SUMMARY.md` when done
</output>
