---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Services/FileProcessorService.Tests/KokoHeaderFieldMismatchTests.cs
  - src/Services/FileProcessorService.Tests/Fixtures/koko-mvp-deploy-111138.csv
autonomous: true
requirements: [B3]
must_haves:
  truths:
    - "A failing integration test exists for B3 using the VERBATIM MinIO koko CSV bytes BEFORE the fix"
    - "The test proves the structural cause: CsvToJsonConverter keys records by header 'date', schema requires 'tararich' → required-missing + additionalProperties failures"
  artifacts:
    - path: "src/Services/FileProcessorService.Tests/KokoHeaderFieldMismatchTests.cs"
      provides: "B3 RED repro over converter + Corvus against real bytes"
    - path: "src/Services/FileProcessorService.Tests/Fixtures/koko-mvp-deploy-111138.csv"
      provides: "verbatim MinIO fixture (id,name,amount,date + 3 rows)"
  key_links:
    - from: "KokoHeaderFieldMismatchTests"
      to: "CsvToJsonConverter.ConvertToJsonAsync"
      via: "HasHeader:true conversion"
      pattern: "ConvertToJsonAsync"
---

<objective>
Author the B3 RED integration test in the existing `FileProcessorService.Tests` project using the VERBATIM MinIO koko CSV bytes captured in RESEARCH. The test proves the confirmed structural root cause: headed CSV keys records by the file's own header (`id,name,amount,date`), `SchemaPropertyNames` is only applied when headerless, so records become `{id,name,amount,date}` and validation against the live koko schema (`required:[id,name,amount,tararich]`, `additionalProperties:false`) yields `required 'tararich' missing` + `date` unexpected-property failures.

Purpose: Per CONTEXT "debug with the real MinIO test files" + "reproduce before fixing" — B3 must fail over the actual bytes before 36-06 fixes it.
Output: New fixture + RED converter+Corvus test in FileProcessorService.Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verbatim koko CSV fixture</name>
  <files>src/Services/FileProcessorService.Tests/Fixtures/koko-mvp-deploy-111138.csv</files>
  <read_first>
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B3 section — the literal 4-line CSV bytes: header `id,name,amount,date` + rows 1/Alpha/100.50/2026-06-10, 2/Bravo/250.75/2026-06-10, 3/Charlie/99.99/2026-06-10)
    - src/Services/FileProcessorService.Tests/CsvToJsonConverterHeaderlessTests.cs (how existing tests load fixture content / build the input stream)
  </read_first>
  <action>
    Create `Fixtures/koko-mvp-deploy-111138.csv` with the EXACT bytes from RESEARCH B3 — clean UTF-8, NO BOM, no trailing whitespace, header line `id,name,amount,date` followed by the three data rows. Ensure the csproj copies the fixture to output (CopyToOutputDirectory / content include) following the pattern other FileProcessorService.Tests fixtures use. The byte-fidelity matters: this is the live B3 reproduction input.
  </action>
  <acceptance_criteria>
    - File first line is exactly `id,name,amount,date`
    - File has 4 lines total, no BOM (first byte is `i` 0x69), 99 bytes matching RESEARCH
    - Fixture is present in test output directory after build
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Services/FileProcessorService.Tests</automated>
  </verify>
  <done>The verbatim koko MinIO CSV is available as a test fixture with byte fidelity.</done>
</task>

<task type="auto">
  <name>Task 2: B3 RED — converter header mapping + Corvus mismatch over real bytes</name>
  <files>src/Services/FileProcessorService.Tests/KokoHeaderFieldMismatchTests.cs</files>
  <read_first>
    - src/Services/Shared/Converters/CsvToJsonConverter.cs:73-80 (headed path keys records by CSV header verbatim) and :93/:123 (empty cell emitted as "")
    - src/Services/FileProcessorService/Consumers/FileDiscoveredEventConsumer.cs:497-504 (SchemaPropertyNames applied ONLY when headerless)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B3 ROOT CAUSE CONFIRMED + the live koko schema JSON)
  </read_first>
  <action>
    Author `KokoHeaderFieldMismatchTests.cs` with two assertions over the fixture stream:
    (1) Conversion proof: call `CsvToJsonConverter.ConvertToJsonAsync(stream, metadata{HasHeader:true})` and assert each output record contains key `date` and does NOT contain key `tararich` — the structural mismatch.
    (2) Validation proof: run the converted records through Corvus against the verbatim koko schema (reuse the same schema string as `ValidationService.Tests/Fixtures/KokoSchema.cs` — copy the literal JSON, do not add a project dependency between test projects) and assert every row is `IsValid==false` with BOTH a `required`-`tararich`-missing error AND a `date` additionalProperties violation. These are the live B3 failures. Mark the test class so the 36-06 fix flips it: after the fix the SAME bytes must validate (0 failures). Name with a `B3` filter token. No production-code edits.
  </action>
  <acceptance_criteria>
    - Converted records assert key `date` present, key `tararich` absent
    - Corvus over koko schema asserts `IsValid==false` with required-`tararich` AND additionalProperties-`date` errors for each of the 3 rows
    - `dotnet test src/Services/FileProcessorService.Tests --filter B3` runs and the RED repro documents the current failure
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/FileProcessorService.Tests --filter B3</automated>
  </verify>
  <done>B3's structural header↔field mismatch is reproduced over the real koko bytes as a RED test ready for the 36-06 fix to flip green.</done>
</task>

</tasks>

<verification>
- `dotnet test src/Services/FileProcessorService.Tests --filter B3` exercises the verbatim koko bytes; converter yields `date` not `tararich`; Corvus reports the required+additionalProperties failures (RED before 36-06)
</verification>

<success_criteria>
The B3 false-invalid is reproduced as a failing integration test over the exact MinIO CSV bytes + live koko schema, with the structural cause asserted, before any fix.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-03-SUMMARY.md` when done
</output>
