---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 06
type: execute
wave: 1
depends_on: ["36-03"]
files_modified:
  - src/Services/Shared/Converters/CsvToJsonConverter.cs
  - src/Services/FileProcessorService/Consumers/FileDiscoveredEventConsumer.cs
autonomous: false
requirements: [B3]
must_haves:
  truths:
    - "The koko required field is satisfied by the data that exists in the CSV (zero false-invalids)"
    - "The verbatim koko CSV bytes (id,name,amount,date) validate to 0 failures against the koko schema after the fix"
    - "The B3 RED integration test (36-03) passes after the fix"
    - "The root-cause decision (data rename vs code reconciliation) is recorded"
  artifacts:
    - path: "src/Services/Shared/Converters/CsvToJsonConverter.cs"
      provides: "header↔schema-field reconciliation for headed CSV (if code path chosen)"
      contains: "ConvertToJsonAsync"
    - path: "src/Services/FileProcessorService/Consumers/FileDiscoveredEventConsumer.cs"
      provides: "SchemaPropertyNames / header reconciliation wiring for headed CSV"
  key_links:
    - from: "FileDiscoveredEventConsumer.BuildConversionMetadata"
      to: "CsvToJsonConverter header mapping"
      via: "SchemaPropertyNames / synonym mapping for headed CSV"
      pattern: "SchemaPropertyNames"
---

<objective>
Fix the confirmed B3 structural cause: headed CSV keys records by the file header (`date`) while the koko schema requires `tararich`; `SchemaPropertyNames` is applied only for headerless CSV (`FileDiscoveredEventConsumer.cs:497-504`), so the required field never gets the data and `additionalProperties:false` rejects `date`. The chosen fix must make the verbatim koko bytes validate with zero failures while not breaking legitimate name-based headed files.

Purpose: Close B3 (CONTEXT: root-cause first, fix the actual cause, test over real koko data) — RESEARCH confirms this is a header↔field name mismatch, not encoding/BOM/RTL/trim.
Output: A header↔schema reconciliation (or the agreed data fix) so the koko file validates clean; the 36-03 RED test flips green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which B3 fix path: data-side schema rename, or code-side header↔field reconciliation?</decision>
  <context>
    RESEARCH Open Question #1: the koko schema field `tararich` (transliteration of תאריך="date") never matches the CSV header `date`. Two viable fixes with different blast radius. This choice determines which files Task 2 edits and whether recurrence is prevented for all future sources.
  </context>
  <options>
    <option id="data-rename">
      <name>Data-side: rename schema field tararich → date (+ re-evaluate additionalProperties)</name>
      <pros>Smallest change; matches the real file exactly; no converter risk; one schema edit via API/Mongo</pros>
      <cons>Fixes only koko; any other source with a header↔field mismatch recurs; not a code-level guarantee</cons>
    </option>
    <option id="code-reconciliation">
      <name>Code-side: headed-CSV header↔schema-field reconciliation (map by order to SchemaPropertyNames or opt-in synonym alias)</name>
      <pros>Prevents recurrence for all sources; addresses the architectural gap (SchemaPropertyNames only used when headerless)</pros>
      <cons>Risk of breaking legitimate name-based headed files if mapping is automatic; needs explicit, opt-in/positional mapping rules</cons>
    </option>
  </options>
  <resume-signal>Select: data-rename, code-reconciliation, or describe a hybrid</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the chosen B3 fix and flip the RED test green</name>
  <files>src/Services/Shared/Converters/CsvToJsonConverter.cs, src/Services/FileProcessorService/Consumers/FileDiscoveredEventConsumer.cs</files>
  <read_first>
    - src/Services/Shared/Converters/CsvToJsonConverter.cs:73-80 (headed path keys by header verbatim)
    - src/Services/FileProcessorService/Consumers/FileDiscoveredEventConsumer.cs:454 (BuildConversionMetadata) and :497-504 (SchemaPropertyNames applied only when headerless)
    - src/Services/FileProcessorService.Tests/KokoHeaderFieldMismatchTests.cs (the 36-03 RED test to flip green)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B3 Minimal-fix direction options)
  </read_first>
  <action>
    Implement the option selected in Task 1.
    If code-reconciliation: in `BuildConversionMetadata`/headed path, when the CSV header differs from the schema property names, apply `SchemaPropertyNames` reconciliation for headed CSV too — positional mapping gated by an explicit opt-in (e.g. only when header count == schema property count AND a reconciliation flag/known-synonym set is present), so legitimate name-based files are untouched. Ensure converted records key by the schema field (`tararich`) and drop the stray `date` additionalProperties violation.
    If data-rename: apply the schema field rename `tararich`→`date` to the koko data source via the DataSourceManagement API (record the exact call/_id 6a280d5cba225ef3a6bc07a6) and adjust the 36-03 test schema fixture to reflect the corrected schema; re-evaluate `additionalProperties` only as needed.
    Either way, update/confirm `KokoHeaderFieldMismatchTests` so the SAME verbatim bytes validate to 0 failures after the fix. Do NOT regress headerless behavior covered by `CsvToJsonConverterHeaderlessTests`.
  </action>
  <acceptance_criteria>
    - `dotnet test src/Services/FileProcessorService.Tests --filter B3` passes (0 validation failures over the koko bytes post-fix)
    - `dotnet test src/Services/FileProcessorService.Tests` full run green (no headerless regression)
    - The root-cause decision and exact change are recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/FileProcessorService.Tests</automated>
  </verify>
  <done>The koko required field is satisfied by the existing CSV data; B3 RED test is green; no headerless regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested CSV header → schema field mapping | untrusted file header names drive property keys consumed by validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | header→field positional reconciliation | mitigate | Only reconcile under explicit opt-in / exact column-count match; never silently remap name-based headed files (prevents a crafted header from masquerading as a different field) |
</threat_model>

<verification>
- `dotnet test src/Services/FileProcessorService.Tests` green including the B3 filter
- Headerless converter tests still pass (no regression)
</verification>

<success_criteria>
B3 root cause fixed via the user-selected path; the real koko CSV bytes validate with zero false-invalids; reproducing test passes; no headerless regression.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-06-SUMMARY.md` when done
</output>
