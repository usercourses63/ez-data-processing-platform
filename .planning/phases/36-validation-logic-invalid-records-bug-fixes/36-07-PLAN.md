---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 07
type: execute
wave: 1
depends_on: ["36-02"]
files_modified:
  - src/Services/InvalidRecordsService/Repositories/InvalidRecordRepository.cs
  - src/Services/Shared/Entities/DataProcessingInvalidRecord.cs
  - src/Services/InvalidRecordsService/Program.cs
autonomous: false
requirements: [B4]
must_haves:
  truths:
    - "GetPagedAsync pages server-side via Mongo Sort/Skip/Limit + a separate count (no full-collection materialization)"
    - "Mongo indexes exist for {DataSourceId}, {CreatedAt}, {ErrorType}, and compound {IsIgnored,IsDeleted,CreatedAt}"
    - "Statistics are computed via Mongo aggregation, not load-all + LINQ GroupBy"
    - "InvalidRecords list/API latency is measurably lower (before/after captured)"
    - "The B4 paging + index RED tests (36-02) pass after this fix"
  artifacts:
    - path: "src/Services/InvalidRecordsService/Repositories/InvalidRecordRepository.cs"
      provides: "server-side paging + aggregation statistics"
      contains: "Skip"
    - path: "src/Services/Shared/Entities/DataProcessingInvalidRecord.cs"
      provides: "index declarations for the common filter+sort keys"
    - path: "src/Services/InvalidRecordsService/Program.cs"
      provides: "index initialization at startup"
  key_links:
    - from: "InvalidRecordRepository.GetPagedAsync"
      to: "MongoDB.Entities Find().Sort().Skip().Limit()"
      via: "server-side query"
      pattern: "\\.Skip\\(|\\.Limit\\("
---

<objective>
Fix the B4 latency cause: `InvalidRecordRepository.GetPagedAsync` (`:62-71`) loads the ENTIRE matching collection and pages in C#, `GetStatisticsAsync`/`GetActiveCountAsync` do the same full scan, and the entity has ZERO Mongo indexes. Rewrite to push filters + sort + paging into Mongo, add indexes, and compute statistics via aggregation. Capture before/after latency numbers (CONTEXT requires them).

Purpose: Close the latency half of B4; the page fires 3 of these calls per filter change, so server-side paging + indexes are the high-leverage fix.
Output: Server-side paging repo + indexed entity + aggregation statistics; 36-02 paging/index tests green; latency numbers recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mongo indexes to DataProcessingInvalidRecord + startup init</name>
  <files>src/Services/Shared/Entities/DataProcessingInvalidRecord.cs, src/Services/InvalidRecordsService/Program.cs</files>
  <read_first>
    - src/Services/Shared/Entities/DataProcessingInvalidRecord.cs (entity fields: DataSourceId, CreatedAt, ErrorType, IsIgnored, IsDeleted)
    - src/Services/InvalidRecordsService/Program.cs (startup/DI; MongoDB.Entities DB init; consumer registration at :52-54)
    - src/Services/InvalidRecordsService.Tests/InvalidRecordIndexTests.cs (the 36-02 index contract to satisfy)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B4 Latency cause #2 + Don't Hand-Roll table)
  </read_first>
  <action>
    Declare indexes on `DataProcessingInvalidRecord` using the MongoDB.Entities index API the codebase already uses (attribute `[Index]` or `DB.Index<DataProcessingInvalidRecord>()...CreateAsync()`): single-key `{DataSourceId}`, `{CreatedAt}`, `{ErrorType}`, and a compound `{IsIgnored, IsDeleted, CreatedAt(desc)}` matching the common active-filter + CreatedAt-sort access pattern. Initialize/ensure indexes at InvalidRecordsService startup (mirror how other services init indexes; keep it idempotent). Match the exact key-set the 36-02 `InvalidRecordIndexTests` asserts.
  </action>
  <acceptance_criteria>
    - `dotnet test src/Services/InvalidRecordsService.Tests --filter Index` passes
    - All four declared index key-sets present; startup index init is idempotent
    - `dotnet build src/Services/InvalidRecordsService` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Index</automated>
  </verify>
  <done>Invalid-record indexes are declared and initialized at startup; 36-02 index test green.</done>
</task>

<task type="auto">
  <name>Task 2: Server-side paging + aggregation statistics in GetPagedAsync</name>
  <files>src/Services/InvalidRecordsService/Repositories/InvalidRecordRepository.cs</files>
  <read_first>
    - src/Services/InvalidRecordsService/Repositories/InvalidRecordRepository.cs:30-165 (GetPagedAsync :62-71 in-memory paging; GetStatisticsAsync :99-127 full scan; GetActiveCountAsync :150-165; status switch :48-59)
    - src/Services/InvalidRecordsService.Tests/RepositoryPagingTests.cs (the 36-02 paging contract)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B4 Latency fix direction + Don't Hand-Roll)
  </read_first>
  <action>
    Rewrite `GetPagedAsync` to push filters (Search, Status, DataSourceId) into the MongoDB.Entities `Find<>()` query, then `.Sort(CreatedAt desc).Skip((Page-1)*PageSize).Limit(PageSize)` server-side; get `totalCount` via a separate `.Match(...).CountAsync()` (or CountEstimatedAsync) instead of `allMatchingRecords.Count`. Replace `GetStatisticsAsync` load-all + LINQ GroupBy with a Mongo aggregation `$group` (by DataSourceId, ErrorType, Severity, plus reviewed/ignored counts). Convert `GetActiveCountAsync` to a server-side `CountAsync`. Reconcile the status-filter switch (:48-59) with the documented `InvalidRecordListRequest.Status` values (New/InProgress/Corrected/Ignored) so an unmatched status does not silently apply no filter (RESEARCH B4 latent cause #4). Do NOT change the controller/DTO response shape. Preserve descending-CreatedAt ordering.
  </action>
  <acceptance_criteria>
    - `dotnet test src/Services/InvalidRecordsService.Tests --filter Paging` passes (page1=10, page3=5, totalCount=25, desc order)
    - GetPagedAsync no longer calls `.ExecuteAsync()` to materialize the whole filtered set before paging; uses Skip/Limit + CountAsync
    - Statistics computed via aggregation (no load-all GroupBy)
    - Status switch handles the documented status enum values (no silent no-op)
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Paging</automated>
  </verify>
  <done>Pagination, count, and statistics run server-side and index-friendly; 36-02 paging test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Server-side paging + Mongo indexes + aggregation statistics for the InvalidRecords list/API, against the live ez-platform cluster.</what-built>
  <how-to-verify>
    1. Deploy/run InvalidRecordsService against the live minikube ez-platform cluster (context-pinned: `kubectl --context=minikube -n ez-platform`).
    2. BEFORE/AFTER comparison: time `GET http://localhost:5007/api/v1/invalid-records?pageSize=10` (and the stats calls) on the pre-fix build vs the fixed build; record P50/P95 numbers.
    3. Run a Mongo `explain` (via the API path or a diagnostic) and confirm the query uses an index (IXSCAN), not a full collection scan (COLLSCAN).
    4. Confirm the list still returns correct records + totalCount.
  </how-to-verify>
  <resume-signal>Type "approved" with the before/after latency numbers, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → InvalidRecords list API | client filter/search/page params reach the Mongo query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | unbounded in-memory load of the invalid-records collection | mitigate | Server-side Skip/Limit caps materialized docs per request; indexes keep filter+sort index-backed |
| T-36-04 | Tampering | Status/Search filter params | accept | MongoDB.Entities `.Match()` parameterizes predicates; reconciled status enum avoids silent filter bypass; no string concatenation |
</threat_model>

<verification>
- `dotnet test src/Services/InvalidRecordsService.Tests` green (Paging + Index)
- Live checkpoint records before/after latency and IXSCAN confirmation
</verification>

<success_criteria>
B4 latency fixed: server-side paging + indexes + aggregation statistics; measurable latency drop recorded; 36-02 paging/index tests pass.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-07-SUMMARY.md` when done
</output>
