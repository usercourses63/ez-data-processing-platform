---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Services/ValidationService.Tests/ValidationService.Tests.csproj
  - src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs
  - src/Services/ValidationService.Tests/CorvusDateFormatTests.cs
  - src/Services/ValidationService.Tests/Fixtures/KokoSchema.cs
autonomous: true
requirements: [B1, B2]
must_haves:
  truths:
    - "A failing/characterizing xUnit test exists for B1 (optional field, empty/absent) BEFORE the B1 fix"
    - "A failing/characterizing xUnit test exists for B2 (optional date field, empty value) BEFORE the B2 fix"
    - "The exact Corvus mechanism (format:date asserts on empty string) is proven by an executable test (confirms assumption A1)"
  artifacts:
    - path: "src/Services/ValidationService.Tests/ValidationService.Tests.csproj"
      provides: "xUnit test project for ValidationService (did not exist before)"
      contains: "Corvus.Json.Validator"
    - path: "src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs"
      provides: "B1 contract test over Corvus optional/required semantics"
    - path: "src/Services/ValidationService.Tests/CorvusDateFormatTests.cs"
      provides: "B2 contract test: format:date asserts on empty string"
  key_links:
    - from: "ValidationService.Tests"
      to: "Corvus.Json.Validator"
      via: "schema.Validate(record, Detailed)"
      pattern: "Corvus\\.Json"
---

<objective>
Scaffold the missing `ValidationService.Tests` xUnit project and author the RED/contract tests that nail the B1 and B2 mechanisms at the Corvus layer BEFORE any fix. These tests (a) prove assumption A1 — that `format:date` is asserted (so an empty string `""` fails) — and (b) lock the TARGET schema shape that an optional field must produce so that empty/absent values validate as valid.

Purpose: Per CONTEXT locked process "reproduce before fixing" — B1/B2 must have an executable test pinning the mechanism before the schema-generation fix (36-05) lands.
Output: New `src/Services/ValidationService.Tests/` project + two test classes + a koko schema fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ValidationService.Tests xUnit project</name>
  <files>src/Services/ValidationService.Tests/ValidationService.Tests.csproj, src/Services/ValidationService.Tests/Fixtures/KokoSchema.cs</files>
  <read_first>
    - src/Services/FileProcessorService.Tests/FileProcessorService.Tests.csproj (copy the xUnit + FluentAssertions + Moq + TargetFramework net10.0 layout verbatim)
    - src/Services/ValidationService/Services/ValidationService.cs (lines 200-230 — confirm the Corvus package + `schema.Validate(record, ValidationLevel.Detailed)` usage to mirror in tests)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (Validation Architecture + Reference fixtures sections)
  </read_first>
  <action>
    Create `src/Services/ValidationService.Tests/ValidationService.Tests.csproj` mirroring `FileProcessorService.Tests.csproj`: net10.0, xUnit + FluentAssertions + Moq package refs, and a ProjectReference to `../ValidationService/ValidationService.csproj` (and Shared if needed for Corvus). Add `Fixtures/KokoSchema.cs` exposing the verbatim live koko schema JSON as a const string: object with properties id(integer), name(string,minLength:2), amount(number,minimum:0), tararich(string); required ["id","name","amount","tararich"]; additionalProperties:false — copied exactly from RESEARCH B3. Use the same Corvus.Json.Validator entry point ValidationService.cs uses so tests run the real validator, not a re-implementation. First confirm `dotnet --version` is the .NET 10 SDK (assumption A3) and record it in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `dotnet build src/Services/ValidationService.Tests` exits 0
    - The csproj contains `Corvus.Json.Validator` (transitively via ValidationService ProjectReference) and references xUnit + FluentAssertions
    - `KokoSchema.cs` const string parses as valid JSON and declares required ["id","name","amount","tararich"] and additionalProperties:false
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Services/ValidationService.Tests</automated>
  </verify>
  <done>The ValidationService.Tests project builds and exposes the koko schema fixture.</done>
</task>

<task type="auto">
  <name>Task 2: B1 RED/contract — optional field empty/absent must validate</name>
  <files>src/Services/ValidationService.Tests/CorvusOptionalFieldTests.cs</files>
  <read_first>
    - src/Services/ValidationService/Services/ValidationService.cs (validation execution path ~lines 200-230)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B1 section, Reproduce RED test)
    - src/Frontend/src/utils/fileAnalyzer/schemaInferrer.ts:150-163 (the required[]-from-sample defect the generated schema embodies)
  </read_first>
  <action>
    Author `CorvusOptionalFieldTests.cs` running the real Corvus validator over two schema shapes for a field `note`:
    (1) BROKEN-shape characterization: schema with `note` in required[] and `additionalProperties:false`; record `{"id":1}` (note absent) → assert Corvus reports `IsValid==false` with a `required` error mentioning `note` (this is the "missing data" mechanism that B1 must eliminate).
    (2) TARGET-shape contract: schema where `note` is NOT in required[] and is modeled empty-tolerant (`type:["string","null"]` OR not-required string); records `{"id":1}` (absent) AND `{"id":1,"note":""}` (present-empty) → assert `IsValid==true` for both. These TARGET assertions define what the 36-05 frontend fix must emit. Name tests so a `--filter B1` selects them. Do NOT modify any production code.
  </action>
  <acceptance_criteria>
    - Broken-shape test asserts `IsValid==false` and the error references the `required` keyword / `note` property
    - Target-shape test asserts `IsValid==true` for both absent and present-empty optional `note`
    - `dotnet test src/Services/ValidationService.Tests --filter B1` runs and the suite compiles; the broken-shape test documents the current "missing data" behavior
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/ValidationService.Tests --filter B1</automated>
  </verify>
  <done>B1 optional-field behavior is pinned by executable Corvus tests defining both the broken and target schema shapes.</done>
</task>

<task type="auto">
  <name>Task 3: B2 RED — optional date empty value asserts format failure (proves A1)</name>
  <files>src/Services/ValidationService.Tests/CorvusDateFormatTests.cs</files>
  <read_first>
    - src/Frontend/src/utils/schemaAutoSuggest.ts:93-104 (the unconditional format:'date' the generated schema embodies)
    - .planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md (B2 section + Assumptions Log A1)
    - src/Services/InvalidRecordsService/Services/InvalidRecordService.cs:199-205 (error parser that handles "should have been 'date'")
  </read_first>
  <action>
    Author `CorvusDateFormatTests.cs`: schema `{properties:{date:{type:"string",format:"date"}}, required:[]}`; run real Corvus over record `{"date":""}` and assert the CURRENT result is `IsValid==false` with a `format`/`date` error — proving assumption A1 (Corvus asserts format, empty string fails). Add a second assertion that record `{"date":"2026-06-10"}` is `IsValid==true` (populated dates must keep passing — the bug is ONLY empty+optional). Add a TARGET-shape test: an empty-tolerant optional date shape (e.g. `anyOf:[{type:"string",maxLength:0},{type:"string",format:"date"}]` or `type:["string","null"]`) → record `{"date":""}` is `IsValid==true` AND `{"date":"2026-06-10"}` still valid AND `{"date":"not-a-date"}` invalid. Name with `B2` filter token. No production-code edits.
  </action>
  <acceptance_criteria>
    - Empty-string-against-format:date test asserts `IsValid==false` (A1 confirmed)
    - Populated valid date asserts `IsValid==true`; invalid date string asserts `IsValid==false`
    - Target empty-tolerant shape asserts `{"date":""}` valid while `{"date":"not-a-date"}` invalid
    - `dotnet test src/Services/ValidationService.Tests --filter B2` compiles and runs
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/ValidationService.Tests --filter B2</automated>
  </verify>
  <done>B2 mechanism (format:date asserts on empty) and the empty-tolerant target shape are both pinned by Corvus tests; assumption A1 is proven true/false in code.</done>
</task>

</tasks>

<verification>
- `dotnet build src/Services/ValidationService.Tests` exits 0 (project scaffolded)
- `dotnet test src/Services/ValidationService.Tests` runs both B1 and B2 classes; broken-shape tests document current invalid behavior; target-shape tests define the contract the 36-05 fix must satisfy
</verification>

<success_criteria>
ValidationService.Tests exists and executes Corvus over real schema shapes, pinning B1 (optional empty/absent) and B2 (optional empty date) mechanisms and target shapes before any fix.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-01-SUMMARY.md` when done
</output>
