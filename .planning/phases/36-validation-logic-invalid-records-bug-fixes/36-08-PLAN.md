---
phase: 36-validation-logic-invalid-records-bug-fixes
plan: 08
type: execute
wave: 2
depends_on: ["36-02", "36-07"]
files_modified:
  - src/Services/Shared/Messages/InvalidRecordCreatedEvent.cs
  - src/Services/ValidationService/Services/ValidationService.cs
  - src/Services/InvalidRecordsService/Consumers/InvalidRecordCreatedEventConsumer.cs
  - src/Services/InvalidRecordsService/Program.cs
  - src/Services/InvalidRecordsService/Hubs/InvalidRecordsHub.cs
  - src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx
  - src/Frontend/src/pages/invalid-records/__tests__/InvalidRecordsManagement.test.tsx
autonomous: true
requirements: [B4]
must_haves:
  truths:
    - "Creating invalid records in the validation write path broadcasts EntityChanged{EntityType:InvalidRecord, Action:created}"
    - "Newly produced invalid records appear on the page WITHOUT a manual reload"
    - "All invalid records across data-source groups are visible/reachable (no rows hidden by collapsed grouping)"
    - "The B4 broadcast RED test (36-02) passes after this fix"
  artifacts:
    - path: "src/Services/Shared/Messages/InvalidRecordCreatedEvent.cs"
      provides: "event contract for pipeline-created invalid records"
      contains: "IDataProcessingMessage"
    - path: "src/Services/InvalidRecordsService/Consumers/InvalidRecordCreatedEventConsumer.cs"
      provides: "relay that broadcasts EntityChanged to InvalidRecordsHub"
    - path: "src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx"
      provides: "expanded groups + adequate page size so all records show"
  key_links:
    - from: "ValidationService.StoreValidationResultsAsync"
      to: "InvalidRecordsHub EntityChanged broadcast"
      via: "InvalidRecordCreatedEvent → consumer → IHubContext"
      pattern: "EntityChanged"
---

<objective>
Fix the B4 missing-rows cause: the validation write path (`ValidationService.cs:302-308`) creates `DataProcessingInvalidRecord` via `SaveAsync` with NO SignalR `EntityChanged` broadcast (a CLAUDE.md CRUD-mandate violation), so the page only updates on manual reload; and the page hides rows in collapsed Collapse groups (`InvalidRecordsManagement.tsx:969`, only first group auto-expanded) with `pageSize=10` (`:55`). Add the broadcast and make all records visible.

Purpose: Close the show-all half of B4 — newly produced invalid records must appear in realtime and no records may be hidden by grouping/page size.
Output: New event + ValidationService publish + InvalidRecordsService relay consumer + hub broadcast + frontend grouping/page-size fix; 36-02 broadcast test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: InvalidRecordCreatedEvent contract + ValidationService publish</name>
  <files>src/Services/Shared/Messages/InvalidRecordCreatedEvent.cs, src/Services/ValidationService/Services/ValidationService.cs</files>
  <read_first>
    - src/Services/Shared/Messages/SchemaUpdatedEvent.cs and src/Services/Shared/Messages/IDataProcessingMessage.cs (event contract pattern: CorrelationId/Timestamp/PublishedBy/MessageVersion)
    - src/Services/ValidationService/Services/ValidationService.cs:280-314 (StoreValidationResultsAsync — SaveAsync at :304, no broadcast)
    - src/Services/InvalidRecordsService.Tests/CreatedRecordBroadcastTests.cs (the 36-02 broadcast contract)
  </read_first>
  <action>
    Create `Shared/Messages/InvalidRecordCreatedEvent.cs` implementing `IDataProcessingMessage` (mirror `SchemaUpdatedEvent`): carry DataSourceId, FileName, CorrelationId, and the created count (and/or record IDs). In `ValidationService.StoreValidationResultsAsync`, after the `invalidRecords.SaveAsync()` at :304, publish `InvalidRecordCreatedEvent` via the MassTransit `IPublishEndpoint`/`IBus` already injected for the service (follow how ValidationService publishes `ValidationCompletedEvent`). Inject the publisher via constructor if not already present. Do NOT call SignalR directly from ValidationService (cross-service hub); publish the event for InvalidRecordsService to relay.
  </action>
  <acceptance_criteria>
    - `InvalidRecordCreatedEvent` implements IDataProcessingMessage with the standard fields
    - ValidationService publishes the event after invalid-record SaveAsync (only when invalidRecords.Any())
    - `dotnet build src/Services/ValidationService` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build src/Services/ValidationService</automated>
  </verify>
  <done>The validation write path emits InvalidRecordCreatedEvent on invalid-record creation.</done>
</task>

<task type="auto">
  <name>Task 2: InvalidRecordsService relay consumer broadcasts EntityChanged</name>
  <files>src/Services/InvalidRecordsService/Consumers/InvalidRecordCreatedEventConsumer.cs, src/Services/InvalidRecordsService/Program.cs, src/Services/InvalidRecordsService/Hubs/InvalidRecordsHub.cs</files>
  <read_first>
    - src/Services/InvalidRecordsService/Consumers/SchemaUpdatedEventConsumer.cs (consumer pattern + IHubContext usage)
    - src/Services/InvalidRecordsService/Program.cs:52-54 (AddMassTransit + AddConsumer registration) and :144 (MapHub /hubs/invalid-records)
    - src/Services/InvalidRecordsService/Hubs/InvalidRecordsHub.cs (currently empty)
    - CLAUDE.md (EntityChanged payload shape: EntityType/EntityId/Action/Version)
  </read_first>
  <action>
    Add `InvalidRecordCreatedEventConsumer` (inherit the service consumer base) that, on receiving `InvalidRecordCreatedEvent`, broadcasts via `IHubContext<InvalidRecordsHub>` `Clients.All.SendAsync("EntityChanged", { EntityType="InvalidRecord", EntityId=<id or empty>, Action="created", Version=... })` — exactly the shape the frontend listens for (entityType==='InvalidRecord'). Register it in Program.cs next to the existing `AddConsumer<SchemaUpdatedEventConsumer>()` at :54. Keep `InvalidRecordsHub` as the broadcast target (optionally add a typed method, but `Clients.All.SendAsync("EntityChanged", ...)` matches the existing useEntitySync contract). Flip the 36-02 broadcast test green by wiring it to the real seam introduced here.
  </action>
  <acceptance_criteria>
    - Consumer broadcasts EntityChanged with EntityType=="InvalidRecord", Action=="created"
    - Consumer registered in Program.cs AddMassTransit block
    - `dotnet test src/Services/InvalidRecordsService.Tests --filter Broadcast` passes
  </acceptance_criteria>
  <verify>
    <automated>dotnet test src/Services/InvalidRecordsService.Tests --filter Broadcast</automated>
  </verify>
  <done>Pipeline-created invalid records broadcast EntityChanged to all connected clients; 36-02 broadcast test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend — show all records (expand groups + page size)</name>
  <files>src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx, src/Frontend/src/pages/invalid-records/__tests__/InvalidRecordsManagement.test.tsx</files>
  <behavior>
    - All data-source groups are expanded (or grouping no longer hides rows) so records in non-first groups are visible
    - Page size is raised/parameterized so grouping does not fragment visibility
    - The existing entity-changed listener (entityType==='InvalidRecord') refreshes the list on broadcast
  </behavior>
  <read_first>
    - src/Frontend/src/pages/invalid-records/InvalidRecordsManagement.tsx:55 (pageSize=10), :351-362 (entity-changed listener), :375-379 (fetch effect), :969 (defaultActiveKey first-group-only)
    - src/Frontend/src/services/invalidrecords-api-client.ts (list query params)
    - src/Frontend/tests/e2e/invalid-records.spec.ts (existing E2E patterns; full E2E assertion is in 36-09)
  </read_first>
  <action>
    Change `defaultActiveKey={Object.keys(groupedRecords).slice(0,1)}` at :969 to expand ALL groups (use all group keys, or make the Collapse default-open) so no records are hidden by collapse. Raise/parameterize `pageSize` at :55 (e.g. 25/50 or a user-selectable page size) so grouping does not under-show. Confirm the entity-changed listener (:351-362) already triggers a refetch on `EntityType==='InvalidRecord'` broadcasts from Task 2 — adjust the handler if needed so a `created` action refreshes the current view without manual reload. Create `src/Frontend/src/pages/invalid-records/__tests__/InvalidRecordsManagement.test.tsx` (the file the verify command targets) asserting: (a) the Collapse `defaultActiveKey` contains ALL group keys (not just the first), (b) `pageSize` is > 10, and (c) dispatching the `entity-changed` event with `entityType==='InvalidRecord'`/`action==='created'` triggers a list refetch (spy on the fetch/list call). Keep RTL/Hebrew intact.
  </action>
  <acceptance_criteria>
    - Collapse defaults to all groups expanded (no first-group-only)
    - pageSize raised/parameterized above 10
    - entity-changed handler triggers a refetch on InvalidRecord created
    - `npm run test:unit -- InvalidRecordsManagement` (or the added spec) passes
  </acceptance_criteria>
  <verify>
    <automated>cd src/Frontend && npm run test:unit -- InvalidRecordsManagement</automated>
  </verify>
  <done>All invalid records across groups are visible and the page refreshes in realtime on new invalid records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ValidationService → InvalidRecordsService (MassTransit) → SignalR clients | a new event crosses the service bus and is relayed to all browser sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Information Disclosure | EntityChanged broadcast payload | mitigate | Broadcast only metadata (EntityType/EntityId/Action/Version) — NOT the invalid-record raw data; clients re-fetch via the authorized API |
| T-36-06 | Denial of Service | high-volume invalid-record creation → broadcast storm | accept | One event per StoreValidationResults batch (not per record); low cardinality; acceptable for current volumes |
</threat_model>

<verification>
- `dotnet test src/Services/InvalidRecordsService.Tests --filter Broadcast` green
- `cd src/Frontend && npm run test:unit -- InvalidRecordsManagement` green
- `dotnet build src/Services/ValidationService` + `dotnet build src/Services/InvalidRecordsService` exit 0
</verification>

<success_criteria>
B4 missing-rows fixed: invalid-record creation broadcasts EntityChanged, the page refreshes in realtime, and all records across groups are visible; 36-02 broadcast test green.
</success_criteria>

<output>
Create `.planning/phases/36-validation-logic-invalid-records-bug-fixes/36-08-SUMMARY.md` when done
</output>
