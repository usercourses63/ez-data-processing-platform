---
phase: 24-v030-release-package-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Frontend/package.json
  - CLAUDE.md
  - release-package/CHANGELOG.md
  - release-package/docs-docusaurus/docs/release-notes.md
autonomous: true
requirements:
  - REL-03
  - REL-04

must_haves:
  truths:
    - "package.json version field reads 0.3.0"
    - "CLAUDE.md version reference reads v0.3.0"
    - "release-package/CHANGELOG.md has a [v0.3.0] entry above the [v0.2.0] entry"
    - "Docusaurus release-notes.md has a v0.3.0 section documenting NAS UX, monitoring fixes, and pipeline fixes"
  artifacts:
    - path: "src/Frontend/package.json"
      provides: "Frontend version 0.3.0"
      contains: "\"version\": \"0.3.0\""
    - path: "release-package/CHANGELOG.md"
      provides: "v0.3.0 changelog entry"
      contains: "[v0.3.0]"
    - path: "release-package/docs-docusaurus/docs/release-notes.md"
      provides: "v0.3.0 release notes in Docusaurus"
      contains: "v0.3.0"
  key_links:
    - from: "src/Frontend/vite.config.ts"
      to: "src/Frontend/package.json"
      via: "__APP_VERSION__ reads pkg.version"
      pattern: "v.*pkg\\.version"
---

<objective>
Update all version references from v0.2.0 to v0.3.0 and write release documentation for the v0.3.0 features (NAS UX improvements, monitoring fixes, pipeline fixes).

Purpose: REL-03 requires the frontend footer to display v0.3.0. REL-04 requires Docusaurus documentation covering all v0.3.0 changes.

Output: Updated package.json, CLAUDE.md, CHANGELOG.md, and Docusaurus release-notes.md.
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-v030-release-package-and-deploy/24-CONTEXT.md

<interfaces>
<!-- Version injection pipeline -->
From src/Frontend/vite.config.ts:
  Line 23: __APP_VERSION__: JSON.stringify(process.env.VERSION || `v${pkg.version}`)
  This reads version from package.json and injects it at build time.

From src/Frontend/src/config/version.ts:
  VERSION_INFO.version = __APP_VERSION__ (injected by Vite define)
  AppFooter.tsx imports VERSION_INFO and displays it in footer.

From src/Frontend/package.json (current):
  "version": "0.2.0"

From CLAUDE.md (current):
  **Status:** Production Ready - v0.1.1-rc3 (Week 5 Validation Phase)

From release-package/CHANGELOG.md (current):
  ## [v0.2.0] - 2026-03-18 (first entry)
  ## [v0.1.1-rc2] - 2025-01-08 (second entry)

From REQUIREMENTS.md (v0.3 features to document):
  NAS UX: NAS-12 through NAS-16 (SignalR broadcasts, delete deprovision, status i18n, role enum)
  Pipeline: PIPE-06 through PIPE-10 (FileServerId, NasDeviceId, FTP URL parsing, credentials, NAS connectionstring)
  Monitoring: MON-15 through MON-19 (Device Health polling, case mappers, RabbitMQ, pipeline IDs, traces)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update version to 0.3.0 in package.json and CLAUDE.md</name>
  <files>src/Frontend/package.json, CLAUDE.md</files>

  <read_first>
    - src/Frontend/package.json (find the "version" field on line 3, change from "0.2.0" to "0.3.0")
    - CLAUDE.md (find the Status line near the top, update version reference)
  </read_first>

  <action>
In `src/Frontend/package.json`:
- Change `"version": "0.2.0"` to `"version": "0.3.0"` (line 3)
- Do NOT change any other field

In `CLAUDE.md`:
- Find the line `**Status:** Production Ready - v0.1.1-rc3 (Week 5 Validation Phase)` near line 7
- Replace with `**Status:** Production Ready - v0.3.0`
- Find the line `**Last Updated:** January 12, 2026` and update to `**Last Updated:** March 20, 2026`
- Do NOT change any other content in CLAUDE.md
  </action>

  <verify>
    <automated>grep '"version": "0.3.0"' src/Frontend/package.json && echo "PASS: package.json version" || echo "FAIL"
grep "v0.3.0" CLAUDE.md && echo "PASS: CLAUDE.md version" || echo "FAIL"
node -e "require('./src/Frontend/package.json')" && echo "PASS: valid JSON" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - `src/Frontend/package.json` has `"version": "0.3.0"` (grep confirms)
    - `CLAUDE.md` contains `v0.3.0` in the Status line
    - `CLAUDE.md` Last Updated reads March 20, 2026
    - `package.json` is valid JSON (node -e "require('./src/Frontend/package.json')" exits 0)
  </acceptance_criteria>
  <done>Frontend version is 0.3.0; CLAUDE.md references v0.3.0; both files valid</done>
</task>

<task type="auto">
  <name>Task 2: Write v0.3.0 CHANGELOG entry and update Docusaurus release-notes.md</name>
  <files>release-package/CHANGELOG.md, release-package/docs-docusaurus/docs/release-notes.md</files>

  <read_first>
    - release-package/CHANGELOG.md (prepend new [v0.3.0] entry above existing [v0.2.0] entry)
    - release-package/docs-docusaurus/docs/release-notes.md (add v0.3.0 section above existing v0.2.0 section)
    - .planning/REQUIREMENTS.md (reference for all v0.3.0 features to document)
  </read_first>

  <action>
In `release-package/CHANGELOG.md`, prepend the following BEFORE the `## [v0.2.0]` line:

```markdown
## [v0.3.0] - 2026-03-20

### Added
- **SignalR Entity Broadcasts**: Servers and Categories controllers broadcast EntityChanged events on create/update/delete/toggle, enabling real-time multi-client UI sync (NAS-12, NAS-13)
- **NAS Delete Auto-Deprovision**: Deleting a NAS device automatically removes Kubernetes PV/PVC resources before soft-delete (NAS-14)
- **NAS Status i18n Tags**: Connection status tags (connected/disconnected) use i18n keys for proper Hebrew/English display (NAS-15)
- **NAS Pipeline Test**: NAS datasource creation via UI with file discovery through NFS mount verified end-to-end

### Changed
- **NAS Role Enum**: Backend accepts string values from frontend via JsonStringEnumConverter, eliminating numeric enum mismatch (NAS-16)
- **FileServerId Extraction**: FileServerId and NasDeviceId properly extracted from ConfigurationSettings on datasource create/update (PIPE-06, PIPE-07)
- **FTP URL Parsing**: FTP connector now parses ftp:// URLs to extract host/port instead of using entire URL as hostname (PIPE-08)
- **Server Credentials**: Auto-populated from AdminServer.TypeSpecificConfig when creating datasources (PIPE-09)
- **NAS ConnectionString**: Made optional for NAS datasources since mount path comes from provisioned PV (PIPE-10)

### Fixed
- **Device Health Tab**: Loads via HTTP polling, no longer blocked by SignalR connection failures (MON-15)
- **SignalR Case Mapping**: Mappers handle both camelCase and PascalCase property names from backend (MON-16)
- **Queue Monitoring**: RabbitMQ queue monitoring replaces Kafka topic monitoring (MON-17)
- **Pipeline Flow IDs**: Service IDs in pipeline flow visualization match actual K8s deployment names (MON-18)
- **Traces Tab**: Queries all pipeline services with correct relative span timing (MON-19)

---

```

Preserve all existing content below (the [v0.2.0] and earlier entries).

---

In `release-package/docs-docusaurus/docs/release-notes.md`, insert a new v0.3.0 section ABOVE the existing `## v0.2.0 - Production Release` section (after the `# EZ Platform - Release Notes` header and `---` separator). Insert:

```markdown
## v0.3.0 - NAS UX + Monitoring + Pipeline Fixes (March 2026)

**Status:** Production Release
**Type:** Feature Enhancement - NAS UX improvements, monitoring stability, pipeline reliability

### Highlights

This release delivers **NAS device UX improvements** with SignalR-based real-time sync across browser tabs, **monitoring stability fixes** for Device Health and pipeline visualization, and **pipeline reliability fixes** for server credential handling and protocol parsing.

### NAS UX Improvements

- **SignalR Entity Broadcasts (NAS-12, NAS-13)**: Creating, updating, deleting, or toggling Servers and Categories in any browser tab instantly propagates changes to all connected clients via SignalR EntityChanged events
- **Delete Auto-Deprovision (NAS-14)**: Deleting a NAS device automatically cleans up Kubernetes PV/PVC resources before performing soft-delete, preventing orphaned cluster resources
- **Status Tags i18n (NAS-15)**: NAS connection status tags (connected/disconnected) now use i18n translation keys, displaying correctly in both Hebrew and English
- **Role Enum Fix (NAS-16)**: Backend NAS Role enum accepts string values ("Input", "Output", "Both", "Backup") from frontend via JsonStringEnumConverter, fixing the numeric enum mismatch

### Data Pipeline Fixes

- **FileServerId Extraction (PIPE-06)**: FileServerId correctly extracted from ConfigurationSettings when creating or updating datasources
- **NasDeviceId Extraction (PIPE-07)**: NasDeviceId and NasSubPath properly extracted from ConfigurationSettings
- **FTP URL Parsing (PIPE-08)**: FTP connector parses `ftp://` URLs to extract host and port separately, instead of using the entire URL as hostname
- **Server Credentials (PIPE-09)**: Credentials auto-populated from AdminServer.TypeSpecificConfig when creating file discovery and processing connections
- **NAS ConnectionString Optional (PIPE-10)**: ConnectionString field no longer required for NAS datasources since mount path is derived from provisioned PV

### Monitoring Fixes

- **Device Health HTTP Fallback (MON-15)**: Device Health tab loads data via HTTP polling when SignalR connection is unavailable, preventing blank tab on WebSocket failures
- **SignalR Case Mapping (MON-16)**: SignalR data mappers handle both camelCase and PascalCase property names, fixing data display issues when backend serialization changes
- **RabbitMQ Queue Monitoring (MON-17)**: Queue monitoring queries RabbitMQ management API instead of Kafka, matching the actual message broker used by pipeline services
- **Pipeline Flow Service IDs (MON-18)**: Service identifiers in pipeline flow visualization match actual Kubernetes deployment names (e.g., `fileprocessor` not `FileProcessorService`)
- **Traces Relative Timing (MON-19)**: Traces tab queries all pipeline services and displays spans with correct relative timing from trace start

### Migration from v0.2.0

**Backward compatible** — no breaking changes. Standard Helm upgrade:

```bash
helm upgrade ez-platform ./helm/ez-platform -f values-local.yaml --namespace ez-platform
```

No database migration required. All changes are backward-compatible service updates.

---

```

Preserve all existing v0.2.0 and earlier content below this new section.
  </action>

  <verify>
    <automated>grep -q "\[v0.3.0\]" release-package/CHANGELOG.md && echo "PASS: v0.3.0 in CHANGELOG" || echo "FAIL"
grep -q "\[v0.2.0\]" release-package/CHANGELOG.md && echo "PASS: v0.2.0 preserved" || echo "FAIL"
grep -q "v0.3.0" release-package/docs-docusaurus/docs/release-notes.md && echo "PASS: v0.3.0 in release-notes" || echo "FAIL"
grep -q "NAS-12" release-package/CHANGELOG.md && echo "PASS: NAS-12 documented" || echo "FAIL"
grep -q "MON-15" release-package/CHANGELOG.md && echo "PASS: MON-15 documented" || echo "FAIL"
grep -q "PIPE-06" release-package/CHANGELOG.md && echo "PASS: PIPE-06 documented" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - `release-package/CHANGELOG.md` has `## [v0.3.0] - 2026-03-20` as the first version entry
    - CHANGELOG v0.3.0 entry has Added, Changed, Fixed sections covering all 15 implemented requirements
    - Existing `[v0.2.0]` and `[v0.1.1-rc2]` entries are preserved verbatim
    - `release-package/docs-docusaurus/docs/release-notes.md` has a `## v0.3.0` section above the existing `## v0.2.0` section
    - Docusaurus release notes cover: NAS UX (NAS-12 through NAS-16), pipeline fixes (PIPE-06 through PIPE-10), monitoring fixes (MON-15 through MON-19)
    - Migration section states backward compatible with standard Helm upgrade
  </acceptance_criteria>
  <done>CHANGELOG v0.3.0 entry written with all 15 implemented requirements; Docusaurus release-notes.md updated with detailed v0.3.0 section</done>
</task>

</tasks>

<verification>
```bash
# Version in package.json
grep "version" src/Frontend/package.json | head -1

# CHANGELOG versions present
grep "^\#\# \[v" release-package/CHANGELOG.md

# Docusaurus v0.3.0 section exists
grep "## v0.3.0" release-package/docs-docusaurus/docs/release-notes.md

# All requirement IDs documented
for req in NAS-12 NAS-13 NAS-14 NAS-15 NAS-16 PIPE-06 PIPE-07 PIPE-08 PIPE-09 PIPE-10 MON-15 MON-16 MON-17 MON-18 MON-19; do
  grep -q "$req" release-package/CHANGELOG.md && echo "PASS: $req" || echo "FAIL: $req missing"
done

# CLAUDE.md updated
grep "v0.3.0" CLAUDE.md
```
</verification>

<success_criteria>
- Frontend package.json version is 0.3.0 (vite will inject as v0.3.0 in footer at build time)
- CLAUDE.md references v0.3.0 as current version
- CHANGELOG.md has complete v0.3.0 entry with all 15 pre-phase requirements documented
- Docusaurus release-notes.md has detailed v0.3.0 section with NAS UX, pipeline, and monitoring categories
- All existing documentation content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/24-v030-release-package-and-deploy/24-01-SUMMARY.md`
</output>
